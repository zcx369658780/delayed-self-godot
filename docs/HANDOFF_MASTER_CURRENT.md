# Handoff master (CURRENT)

## Current entry point

Read `AGENTS.md`, current startup/active-task documents, Task 0015R, `docs/reports/0015R_LEVEL_7_ECHO_DIVERGENCE_FEEDBACK_REPAIR_REPORT.md`, the accepted repair plan, and Task 0015D GPT acceptance. Verify live Git truth before acting. Task 0015R implementation is ready for GPT review; no owner retest or Level 8 task is authorized.

Level 7 `two_echo_convergence` has shared-spawn E2/E4, Plate A `[6,3]`, Plate B `[7,5]`, one unavoidable A+B Door `[7,2]`, and EXIT `[7,1]`. Exact result: `L*=19`, `N*=6 EXACT`, visited 61,975, frontier 21,449, one-turn required convergence, formal SHA-256 `772410d3b8bb807e35472ee2a7d07b703de66e1e0e92ffb4d1ff8433a345d5fd`. Both ECHOs and Plates are individually necessary; equal-delay/no-ECHO/single-ECHO/relocation controls are complete unsolved. All six shortest solutions use the E2-on-A/E4-on-B open-snapshot crossing.

The catalog has seven entries; Level 7 is sole final and unlocks only after Level 6. Generic A/triangle, B/circle, A+B, E2 solid, E4 double-dashed, overlap, and grayscale cues are implemented only in reusable Gameplay. Focused tests and eight captures pass. Aggregate exits 0 with 367 assertions / 9 vectors and all nine markers; only expected `LEVEL_ID_MISMATCH` appears on stderr.

Carry forward: Task 0015R implements `TRANSITION_DERIVED_ECHO_DIVERGENCE_FEEDBACK` only in reusable Gameplay. It derives actual E2/E4 action/outcome and before/after spacing from the returned transition, shows a compact top-gap cue only on change, and clears on stable turns/restart/load/completion. Ten native 960×540 captures, reduced-motion, grayscale, and 391 assertions / 9 vectors with ten markers pass. Formal hash/metrics remain exact. The blocking P2 is not closed until a separately authorized owner-only retest; geometry fallback and Level 8 remain blocked.
