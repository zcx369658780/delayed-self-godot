# Handoff master (CURRENT)

## Current entry point

Read `AGENTS.md`, current startup/active-task documents, Task 0015D, `docs/level_design/LEVEL_7_FALSE_UNSOLVABLE_REPAIR_PLAN_CURRENT.md`, its diagnostic report, and the Task 0015V GPT adjudication. Verify live Git truth before acting. Task 0015D planning is ready for GPT review; no repair or Level 8 task is authorized.

Level 7 `two_echo_convergence` has shared-spawn E2/E4, Plate A `[6,3]`, Plate B `[7,5]`, one unavoidable A+B Door `[7,2]`, and EXIT `[7,1]`. Exact result: `L*=19`, `N*=6 EXACT`, visited 61,975, frontier 21,449, one-turn required convergence, formal SHA-256 `772410d3b8bb807e35472ee2a7d07b703de66e1e0e92ffb4d1ff8433a345d5fd`. Both ECHOs and Plates are individually necessary; equal-delay/no-ECHO/single-ECHO/relocation controls are complete unsolved. All six shortest solutions use the E2-on-A/E4-on-B open-snapshot crossing.

The catalog has seven entries; Level 7 is sole final and unlocks only after Level 6. Generic A/triangle, B/circle, A+B, E2 solid, E4 double-dashed, overlap, and grayscale cues are implemented only in reusable Gameplay. Focused tests and eight captures pass. Aggregate exits 0 with 367 assertions / 9 vectors and all nine markers; only expected `LEVEL_ID_MISMATCH` appears on stderr.

Carry forward: Task 0015 is GPT accepted at `INTERNAL_PLAYABLE`. Task 0015D reconstructed the 19-turn witness and the exact owner screenshot state; both are solvable. The opening lane preserves a two-cell vector and reinforces the false invariant, while later history/terrain transitions change Manhattan spacing `2→0→1→3` without explicit feedback. The selected primary bundle is `TRANSITION_DERIVED_ECHO_DIVERGENCE_FEEDBACK`, preserving formal data/hash/metrics; geometry-only early divergence is the sole fallback after a failed primary retest. Do not implement either bundle without a new GPT-authorized task. Level 8 remains blocked.
