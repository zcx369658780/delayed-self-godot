# DeepSeek file-worker pilot project-source delta

`project_source_import_status = READY_FOR_GPT_PROJECT_SOURCE_IMPORT`

The accepted user-level baseline is a frozen `deepseek-v4-pro` file-only worker reached only through the User-scope credential wrapper. MCP is not a dependency. GPT-5.6 retains planning, path design, review, testing, Git and acceptance; DeepSeek can only edit explicitly allowed non-sensitive files under a bounded task.

0024AO passed one real isolated smoke using an absolute isolated workspace and workspace-relative `target.md`/`context.md`. No production/gameplay/Profile surface changed. The project uses Path B documentation-only wrapper alignment; native GPT provider/model and shell filtering are unchanged. Import into GPT Project Sources remains a human/advisor action.
