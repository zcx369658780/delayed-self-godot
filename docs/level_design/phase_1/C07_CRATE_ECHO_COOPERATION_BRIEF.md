# C07 — 递给回声 / Hand Off

`candidate_slot = C07`; `family = crate`; `phase = PHASE_1_PROOF_BATCH`; `status = MACHINE_VALIDATED_FOR_GPT_REVIEW`; `production_sequence = UNASSIGNED`; `catalog_status = NOT_AUTHORIZED`; `owner_review_status = NOT_REVIEWED`; `machine_validation_status = PASS_FOR_GPT_REVIEW`.

Teaching goal: make a readable YOU-to-ECHO crate handoff. Objective: YOU places crate to create a later ECHO-only usable condition, then ECHO performs the non-substitutable action. Prerequisites C03 and role cues; reasoning is ordered handoff, not crate solo. Seed relationship: expansion beyond sequence-9 obstruction; anti-duplication: neither crate-only nor echo-only route may solve; non-goals: optional ECHO or pre-positioned crate.

YOU positions crate; ECHO performs delayed sensor/plate action; crate purpose is topology handoff; it is nonoptional because no crate, displaced crate, or pre-positioning invalidates intended necessity.

Dependencies: `SUPPORTED_AS_IS`; v2 crates plus ECHO/plate-or-sensor/barrier; existing v2 blocked-crate and sensor/door paths plus BFS; no missing contract; presentation needs handoff arrows, identities, crate and state cue; forbidden shortcuts: body substitutes for ECHO, static blocker, alternate route.

`topology_status = AUTHORED_UNCATALOGUED_LEVEL_DATA`.
```text
YOU SETS CRATE -> ECHO RECEIVES LANE -> ECHO ACTS -> YOU EXIT
```
Machine result: `L*=14`, exact `N*=1`, all-shortest `1/1`, omissions zero. Ordered turns are crate handoff 2, ECHO first-barrier crossing 7, ECHO-only trigger 9, YOU second-barrier crossing 12 and EXIT 14. `candidate_solution_status = MACHINE_VALIDATED_FOR_GPT_REVIEW`.

Budget: Synthesis proof; target `L*=14–22`, hard `26`; states `150k/400k`; elapsed `45/120s`; enum `128`; witnesses ordered handoff/role necessity; timeout `UNVERIFIED`.

Controls: intended witness; no crate; no echo; crate pre-positioned; body substitutes for echo; displaced crate; topology audit; restart; trivial rejection. Presentation: badges, crate handoff marker, action target/barrier cue, timeline, readable stalled handoff and restart, redundant shape/text/color-independent cues, v2 evidence, owner question: “Did the crate feel handed to the ECHO rather than merely pushed?” Cut on duplicate, optional mechanic, budget overrun, unclear handoff, unbounded change, opaque failure, execution-heavy route, schedule cost.
