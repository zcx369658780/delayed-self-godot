# S01 — 分工初见 / Separate Roles

`candidate_slot = S01`; `family = body_shadow`; `phase = PHASE_1_PROOF_BATCH`; `status = MACHINE_VALIDATED_FOR_GPT_REVIEW`; `production_sequence = UNASSIGNED`; `catalog_status = NOT_AUTHORIZED`; `owner_review_status = NOT_REVIEWED`; `machine_validation_status = PASS_FOR_GPT_REVIEW`; `presentation_status = MACHINE_CONTRACT_READY_OWNER_REVIEW_PENDING`.

Task 0024AX retained `L*=12`, exact `N*=8`, all-shortest `8/8` with zero omissions. No-ECHO, swapped eligibility and both route-removal controls are complete-unsolved. Manifest `e01bba09-6304-4344-b16a-4bfb9d364655` is consumed; GPT acceptance remains pending.

Teaching goal: reveal distinct body and ECHO responsibilities before advanced synthesis. Objective: read role badges, put each actor on its own necessary condition, then exit. Prerequisites are delayed ECHO and basic door reading; reasoning is identity eligibility, not mirrored movement; seed relationship is the body/shadow direction; anti-duplication is explicit typed targets. Non-goals: advanced latch combinations.

Body reaches a YOU-only condition; ECHO reaches an ECHO-only condition. Roles cannot substitute because eligibility-swapped, body-only, echo-only, and symmetric-path controls must defeat the intended proof.

Dependencies: `SUPPORTED_AS_IS`; v2 typed sensors/barrier groups; current `_activator_pressed`, sensor state and shared BFS; no missing contract; presentation needs role badges, typed sensor glyphs, barrier state and delay cue; forbidden shortcuts are `ANY_ACTOR`, symmetric paths, or alternate route.

`topology_status = AUTHORED_MACHINE_VALIDATED_OWNER_REVIEW_PENDING`.
```text
YOU BADGE -> YOU SENSOR --+-> BARRIER -> EXIT
ECHO BADGE -> ECHO SENSOR -+
```
Solution: establish non-substitutable body and delayed ECHO positions; barrier opens on their typed witness; body exits. Witness is both typed activations; critical decision is choosing roles before movement. `candidate_solution_status = MACHINE_VERIFIED_FOR_GPT_REVIEW`.

Budget: Intro; target `L*=10–16`, hard `20`; states `50k/150k`; elapsed `15/60s`; enum `128`; witnesses typed roles/barrier/exit; timeout `UNVERIFIED`.

Controls: body-only; echo-only; eligibility swapped; symmetric paths; intended witness; shortcut audit; restart; trivial rejection. Presentation: body/ECHO badges, typed sensor silhouettes, barrier/group mark, delay cue, readable wrong-role failure/restart, shape/text redundancy independent of color, v2 evidence, owner question: “Did you understand each role before trying it?” Cut on duplicate, optional role, budget overrun, unclear cue, unbounded change, opaque failure, execution-heavy route, schedule cost.
