# Bridge curriculum repair plan (CURRENT)

## Status and evidence boundary

Status: `PLANNING_ONLY / BRIDGE_CURRICULUM_NOT_ACCEPTED_PENDING_BOUNDED_REPAIR`.

The accepted formal bridge remains byte-identical and machine-valid: SHA-256 `805cdc05ea9b3805189ba7be4938cb7ede4e7056b9ae9a7e911df96a801d58ee`, `L*=16`, `N*=12 EXACT`, 19,059 visited states, and frontier 3,734. This plan does not change it. The owner-only evidence is `AUTHENTICATED_OWNER_NOT_FRESH_NOT_INDEPENDENT_BRIDGE_SEQUENCE_REVIEW`; it is not population, fresh, blind, balance, or fun evidence.

The preserved lesson is: a closed Door or terrain can make one delayed ECHO `BLOCKED` while the other `MOVED`; the changed phase permits E2 on A and E4 on B; both ECHOs hold A+B while YOU remains free to enter on the following open snapshot.

## Failure-state diagnosis

The owner already read E2/E4, A/B, A+B, outcome labels, and next-input Door entry. Those surfaces must not be redesigned as though they were unreadable.

The failure is compounded curriculum burden, not a formal contradiction:

1. Level 6 was completed in about four seconds; the bridge first caused more than 20 restarts and an unassisted give-up.
2. The reusable `ECHO SPACING CHANGED · E2 MOVED · E4 BLOCKED` cue truthfully exposes transition facts, but it does not explain why changed spacing is useful or constrain the next experiment.
3. Existing Help explains generic Door/Plate state and Timeline indexing, not the blocker experiment or the requirement that YOU remain free. Timeline was used and judged unhelpful, so it remains optional.
4. The compact geometry makes the owner simultaneously discover the useful blocker phase, derive fixed E2/A and E4/B roles, avoid trapping an ECHO above the Door, and preserve YOU's staging lane. The owner eventually solved it in 37 turns after more than 30 reported restarts but still could not explain anti-substitution.

External diagnostic replay confirms that tested witness-prefix states are recoverable rather than mathematically dead: prefixes 8, 10, 12, and 14 have exact recovery lengths 8, 6, 4, and 2 respectively. This does not prove an owner can see those recoveries.

## Candidate comparison

Weights: continuity 20%, observed-failure correction 20%, mechanical integrity 15%, non-route recovery 15%, implementation/revalidation risk 10%, 960x540/grayscale 10%, reuse 5%, catalog stability 5%. Scores are planning judgments, not human evidence.

| Direction | Score / 5 | Judgment |
|---|---:|---|
| A. Byte-identical room + causal Help | 3.25 | Low risk, but prior Help was ineffective and cannot reduce the current four-part discovery load alone. |
| B. Byte-identical room + recovery/presentation affordance | 2.90 | The relevant state-derived outcome cue already exists; further feedback risks route/dead-state implication. |
| C. Simplified geometry only | 3.80 | Can reduce burden, but leaves the causal anti-substitution model unstated. |
| D. Minimal geometry + contextual causal Help | 4.45 | Selected: separates discovery from role explanation while retaining one bounded lesson and no catalog growth. |
| E. Additional preliminary level/sequence split | 2.20 | Rejected as a primary: adds formal, catalog, validation, review, and production cost before proving a smaller bridge cannot teach. |

## Selected repair bundle

### Primary: D — `MINIMAL_BRIDGE_GEOMETRY_PLUS_CONTEXTUAL_CAUSAL_HELP`

A later implementation task may re-author only `echo_spacing_bridge` and add one bridge-scoped contextual Help section. It may not alter schema v1, Simulation, solver, Timeline semantics, catalog count, existing formal levels, or the shifted `two_echo_convergence` file.

Geometry target: reveal a required early `BLOCKED/MOVED` event before the two-Plate staging loop; remove at least one misleading upward-pocket/overshoot branch; retain shared-spawn E2/E4 delays 2/4, two distinct Plates, one A+B Door, player-only EXIT, E2->A/E4->B, next-input crossing, and both-ECHO necessity. Planning bands are `11<=L*<=14`, `4<=N*<=64 EXACT`, 1,000–12,000 visited, 100–2,500 frontier, 2–3 meaningful decisions, and at least three recoverable alternatives. These replace neither formal proof nor owner evidence.

Help is manual (`H`), zero-turn, modal, available only for a multi-ECHO A+B room, and begins with the existing board identities rather than a route. Candidate content for review, not approved implementation copy:

> One ECHO can be blocked while the other moves, changing their spacing. Try observing a closed Door or wall. A+B needs two ECHOs to stay on separate Plates. Only YOU can finish, so keep YOU free to cross after the Door opens.

It must contain no action sequence, coordinate, target spacing, turn count, next action, solver state, branch recommendation, screenshot route, or dead/alive label. Timeline stays optional and unchanged. Existing label/shape/pip and E2/E4 badge/outline cues remain primary; always-on dotted lines are not added. Any later dotted-line reduction is secondary and must preserve grayscale readability.

### Fallback: C — `SIMPLIFIED_GEOMETRY_WITHOUT_HELP_CHANGE`

Use only if a capture/review of the primary demonstrates that the causal Help adds density without enabling a credible experiment. It retains the same formal proof package and prospective metric bands, but freezes Help/Timeline source and copy. It is not permission to choose a new level or add a catalog entry.

## Required proof and retest

Any formal variant requires a new SHA-256, schema/load/solve/replay/restart proof, exact count, all-shortest required divergence and fixed-role traces, complete no-ECHO/E2-only/E4-only controls, equal-delay, dependency, Door-removal, blocker/Plate relocation, overlap, substitution, articulation, EXIT, catalog/progression, 960x540, reduced-motion, grayscale, focused, aggregate, and seven-other-hash checks.

Any Help component requires trigger/copy/placement proof, zero-turn and canonical-key preservation, no-route-disclosure review, focused plus aggregate regression, and the same owner-only retest. Future retest classification is `AUTHENTICATED_OWNER_NOT_FRESH_NOT_INDEPENDENT_BRIDGE_REPAIR_RETEST`. Promotion requires P0/P1=0, no initial same false/restart loop, explanation of spacing and two-ECHO/YOU roles, Help-supported experiment without a route, no Timeline requirement, materially lower burden, and bridge-to-Level-8 teaching-to-synthesis judgment. Prior exposure remains disclosed.

## Rollback, stop conditions, and disposition

Rollback is to the accepted byte-identical bridge and existing presentation. Stop and return to GPT if no candidate preserves both-ECHO necessity and the teaching contract; Help must become a walkthrough; a repair needs a forbidden mechanic/schema/solver rule; a formal variant is not materially below shifted Level 8; presentation needs route knowledge; or one primary bundle cannot be selected.

`FINALE_PLANNING_BLOCKED_PENDING_REPAIR` remains in force. This plan does not authorize implementation, retest, finale, production, export, release, or submission.

## Non-claims

No fresh/independent evidence, human difficulty acceptance, balance/fun claim, formal replacement, owner acceptance, candidate readiness, export, release, or submission readiness is established.
