# Codex startup snapshot (CURRENT)

- Status: `CURRENT / TASK 0024AO READY / RELATIVE-PATH DEEPSEEK PILOT CONTINUATION`
- Repository/root/branch: `zcx369658780/delayed-self-godot` / `D:\Delayed_Self` / `main`
- Active task: `tasks/0024AO_deepseek_relative_path_contract_alignment_and_project_pilot_completion.md`
- Active status: `TASK_0024AO_READY`
- Task publication commit: `625314efc1fab7b06014683907db478f91428f89`
- Active-pointer commit: `bdc14bb15eed75db61605ab4f660a684c1e34b9f`
- Historical attempted tasks: `0024AM`, `0024AN`
- 0024AM root cause: current-process credential visibility was an invalid presence gate
- 0024AN root cause: absolute `allowed_paths`/`context_files` conflicted with the frozen worker's relative-path input contract
- Required predecessor checkpoint in ancestry: `8369cd1beb1e1c4a662081cb7657f01c4a99fb6a`
- Previous GPT gameplay acceptance: `TASK_0024AL_PROCESS_ISOLATED_WRITE_AHEAD_SCHEMA_V2_MINIMUM_OWNER_REVIEW_PRESENTATION_ACCEPTED`
- User-level external-worker baseline: `ACCEPTED_FOR_USER_LEVEL_CLI_FALLBACK_PILOT`
- Windows User-scope credential: `PRESENT` from the 0024AN observation
- Current-process visibility: `FILTERED_EXPECTED`
- Authorized transport: `USER-SCOPE WRAPPER → FROZEN CLI WORKER`
- Required path design: absolute `workspace`; frozen-worker-compatible workspace-relative `allowed_paths` and `context_files`
- Required GPT ownership: worker-contract extraction, planning, task JSON, review, validation, Git and final acceptance remain with `GPT-5.6 Sol/Terra`
- Production baseline: exactly eight cataloged levels; sequence 8 remains the sole formal finale
- Owner route: `MACHINE_QUALIFIED_ISOLATED_STAGING_ROUTE`
- Owner playthrough: `NOT_YET_REVIEWED`
- Presentation: `MACHINE_QUALIFIED_SCHEMA_V2_MINIMUM_OWNER_REVIEW_REQUIRED`
- Catalog admission: `NOT_AUTHORIZED`

Before Task 0024AO work, fetch and fast-forward to current `origin/main`. Require `HEAD == origin/main`, divergence `0/0`, no tracked/staged changes, and only the pre-existing untracked `.codex/config.toml`. Require its SHA-256 `d4c670da3c2404921a1ab177f4e5405449bc8321d713cc0b42b8046201fd33cc` without exposing contents. Require Godot count zero and do not launch Godot.

Read and preserve the user-level wrapper:

```text
C:\Users\zcxve\.codex\helpers\Invoke-DeepSeekFileTask.ps1
```

Task 0024AO must first extract the frozen worker's real task-file path contract from source. It must not modify the wrapper or worker. The real smoke must use the wrapper, an absolute workspace root and workspace-relative path entries. Direct Python, MCP delegate, absolute allowed/context entries, shell-policy weakening and credential disclosure are prohibited.

Read in order: `AGENTS.md`, this file, `docs/CODEX_ACTIVE_TASK_CURRENT.md`, Task 0024AO, historical Tasks 0024AN and 0024AM, the Task 0024AL acceptance handoff/report, model-routing rules, project workflow and current documents.

DeepSeek-V4-Pro may edit only one temporary untracked smoke target. GPT-5.6 must independently review the worker contract, task JSON, output, changed paths, content, secret boundaries and synchronization evidence.

All Task 0024AF–0024AL artifacts and consumed retained identities remain immutable. Do not rerun them. Do not change gameplay, Simulation, solver, levels, catalog, progression, finale, Profile, presentation implementation, scenes, localization, assets, audio, build/export/release/submission surfaces or the owner worksheet.

The possible Task 0024AO recommendation is `DEEPSEEK_PROJECT_PILOT_ENABLED` or `DEEPSEEK_PROJECT_PILOT_BLOCKED`; Codex may return only `PASS_FOR_GPT_REVIEW`, `BLOCKED` or `FAIL`. Actual acceptance remains with the GPT Project Sources advisor.

The next gameplay/product gate remains owner subjective review of sequences 1–12 and must use a later successor task identity after Task 0024AO is adjudicated.