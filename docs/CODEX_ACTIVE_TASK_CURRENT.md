# Codex active task (CURRENT)

- Status: `ISOLATED_PRODUCTION_POLICY_SANDBOX_MATRIX_READY_FOR_CODEX`
- Active task: `tasks/0023ZQ_isolated_production_policy_sandbox_matrix_gate.md`
- Last GPT-accepted gate: `TASK_0023ZP_PRODUCTION_PROFILE_STATIC_BOOTSTRAP_ACCEPTED`
- Required checkpoint: `59c9f77d7a744e34ce55a415878b0e03bf28bd02`
- Accepted implementation under test: `afe8c6fffdc98992b732fce46f206ad0dc8553a1`
- Gate: `PROTECTED PLAYER DATA / ISOLATED APPDATA PRODUCTION POLICY MATRIX + ZERO REAL-USER-DATA CONTACT`
- Formal scope: exactly eight levels
- Formal finale: sequence 8 `two_echo_convergence`
- Current plan: `docs/production/COMPLETE_FLOW_PRODUCTION_CLOSURE_PLAN_CURRENT.md`

Tasks 0023R–0023ZP are GPT-accepted and frozen within their stated scopes. Task 0023ZO is the accepted production-activation design authority, and Task 0023ZP is the accepted static/compile implementation authority.

Task 0023ZP added the non-default memory-only bootstrap composition seam, immutable AppRoot/store production configuration APIs, pure schema classification and bounded production policies while leaving `project.godot`, the memory-only main scene and autoload state unchanged. Its compile smoke, 60-assertion static contract and ten zero-I/O regressions passed. It performed no production configuration runtime call and no player-data/test-root access.

Task 0023ZQ is the only active scope. It may add only the five named isolated-sandbox harness artifacts, its report and authorized CURRENT documentation. Product source, project settings, historical tests/harnesses, catalog/formal data and UI/gameplay remain frozen.

All Task 0023ZQ Profile behavior must run in two process-local external APPDATA sandboxes under `D:\Delayed_Self_Evidence\0023ZQ_isolated_production_policy_matrix\`. Every process must attest that `OS.get_user_data_dir()` is strictly inside the exact sandbox before any Profile/configuration/filesystem operation. Persistent environment changes, symlinks/junctions and caller-controlled production paths are forbidden.

Task 0023ZQ has no authority to access, stat, refresh metadata for or enumerate the ordinary production Profile/user-data directory. It also has no authority to access or enumerate `user://delayed_self_test_profiles/` or any historical fixture. The current project main scene must remain memory-only.

The required matrix covers bootstrap policy selection; read-only missing/valid/completed/sanitizable/malformed/unsupported states; write-on-intent missing/valid/NO_CHANGE/first-write/recovery/unsupported/collision/rollback behavior; and production rejection of test-only reset/failure seams. Two independently absent sandbox attempts must each audit to `PASS_COMPLETE_MATRIX` and complete exact owned cleanup.

Production read or write against the existing Profile, default activation, aggregate registration, Menu New/Continue/Reset, settings, localization, build/export, release, upload and festival submission remain closed.

Do not resume Tasks 0023Y, 0023Z, 0023ZA, 0023ZB, 0023ZE, 0023ZF, 0023ZH, 0023ZI, 0023ZK, 0023ZL, 0023ZJ, 0023ZM, 0023ZN, 0023ZO or 0023ZP, and do not reuse their fixtures. Historical blocked tasks 0023, 0023S and 0023U remain closed. Sequence 9 remains `DEFERRED_CONCEPT_NOT_PRODUCT_SCOPE`.

Expected terminal verdict:

```text
ISOLATED_PRODUCTION_POLICY_SANDBOX_MATRIX_READY_FOR_GPT_REVIEW
BLOCKED_ISOLATED_PRODUCTION_POLICY_SANDBOX_MATRIX
```
