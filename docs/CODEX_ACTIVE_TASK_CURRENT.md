# Codex active task (CURRENT)

- Status: `PRODUCTION_PROFILE_STATIC_BOOTSTRAP_READY_FOR_CODEX`
- Active task: `tasks/0023ZP_production_profile_static_bootstrap_implementation_gate.md`
- Last GPT-accepted gate: `TASK_0023ZO_PRODUCTION_PROFILE_ACTIVATION_DESIGN_ACCEPTED`
- Required checkpoint: `9495043e6e54c5c2cfc0872c033747a2704b1cff`
- Implementation base: `d8fd787d978183836d703d7c0a0e9abfc5a3cb97`
- Gate: `PROTECTED PLAYER DATA / PRODUCTION BOOTSTRAP STATIC IMPLEMENTATION + ZERO-I/O COMPILE CONTRACT`
- Formal scope: exactly eight levels
- Formal finale: sequence 8 `two_echo_convergence`
- Current plan: `docs/production/COMPLETE_FLOW_PRODUCTION_CLOSURE_PLAN_CURRENT.md`

Tasks 0023R–0023ZN are GPT-accepted and frozen within their stated scopes. Task 0023ZO is the accepted production-activation design and authorization authority.

Task 0023ZN closed the normalized configured-session test matrix with two consecutive `PASS_COMPLETE_MATRIX` executions. This does not activate production persistence. The normal AppRoot path remains memory-only.

Task 0023ZO selects a dedicated composition bootstrap that configures AppRoot before tree entry while AppRoot remains the sole router. Internal policies are exactly `MEMORY_ONLY`, `PRODUCTION_READ_ONLY`, and `PRODUCTION_WRITE_ON_INTENT`. Development-direct launches remain memory-only. Production read, recovery-copy write, first target write, default activation and reset are separate explicit player-data authorizations.

Task 0023ZP is the only active scope. It may add the bootstrap script/scene, production configuration APIs in AppRoot/store, a pure codec schema classifier, and two focused compile/static tests. It must leave `project.godot` and `run/main_scene` unchanged and preserve all existing test-mode behavior.

Task 0023ZP has zero player-data authorization. It must not access or refresh production Profile metadata/content, access or enumerate the test root, invoke production configuration at runtime, run fixtures, the canonical runner/full matrix, aggregate, project main scene, export or build.

Authorized dynamic validation is limited to the new parser/compile/static tests and previously accepted zero-I/O focused tests listed in the task. Existing 0023W filesystem behavior and the canonical matrix must not run.

Production Profile runtime read/write, recovery/backup, first intentional write, default activation, aggregate registration, Menu New/Continue/Reset, settings, localization, build/export, release, upload and festival submission remain closed.

Do not resume Tasks 0023Y, 0023Z, 0023ZA, 0023ZB, 0023ZE, 0023ZF, 0023ZH, 0023ZI, 0023ZK, 0023ZL, 0023ZJ, 0023ZM, 0023ZN or 0023ZO, and do not reuse their fixtures. Historical blocked tasks 0023, 0023S and 0023U remain closed. Sequence 9 remains `DEFERRED_CONCEPT_NOT_PRODUCT_SCOPE`.

Expected terminal verdict:

```text
PRODUCTION_PROFILE_STATIC_BOOTSTRAP_READY_FOR_GPT_REVIEW
BLOCKED_PRODUCTION_PROFILE_STATIC_BOOTSTRAP
```
