# Task 0023ZX GPT preflight blocker adjudication

- GPT classification: `TASK_0023ZX_BLOCKED_ON_STALE_SIX_LEVEL_REGRESSION_UID_ADOPTION_AND_RESIDUAL_GODOT_PROCESSES`
- Task result: `BLOCKED_CORE_MECHANICS_SIMULATION_FOUNDATION`
- Repository checkpoint: `b4345fcdb61184b155cf6ed3ef568530553b04df`
- Product/catalog/Profile acceptance from 0023ZX: **none**
- Protected-data boundary breach: **not established**

## Adjudication

Task 0023ZX correctly stopped. The failure does not establish a defect in the new v2 mechanic draft because the first named existing-level regression is itself stale: `tests/levels/task_0012_staggered_doors_validation.gd` still requires a catalog of exactly six entries and hard-codes the six-level sequence/finale state, while the accepted implementation baseline has eight formal levels.

The generated `.uid` files must not be treated as disposable cache by default. Godot 4.4 and later use sidecar UID files for scripts and shaders, and official Godot guidance recommends committing them to version control rather than ignoring them. A future task must inventory every generated `.uid`, verify its source-file pairing and deterministic contents, and adopt only a reviewed exact set through a separate repository-wide UID policy gate.

Four residual Godot processes were also reported. No process termination authority existed in Task 0023ZX, so stopping without termination was correct. Any future process action requires explicit owner authorization to inspect only PID, executable identity/path and command line, classify the complete Godot process set, and terminate only processes unambiguously attributable to the Delayed Self 0023ZX test run. Ambiguous, unrelated, editor, project-manager or manually started processes must remain untouched.

## Local continuation inputs

The owner/Codex report states that uncommitted 0023ZX draft source changes, focused fixtures and generated UID files remain locally. They are not accepted implementation authority and must not be discarded, overwritten, staged, committed or pushed before a bounded continuation task:

1. records the exact dirty-path inventory, tracked state, SHA-256, bytes and lines;
2. copies all continuation inputs byte-identically to an external evidence root;
3. reconciles the local checkout with current remote authority without broad reset, clean or stash operations;
4. classifies UID files separately from authored source/fixture drafts;
5. corrects only the stale six-level assertions to the accepted eight-level baseline;
6. reruns the foundation only after zero Godot processes and exact authorized-path control.

## Required future authorization

Before a continuation may be activated, the owner must explicitly authorize all of the following:

- bounded inspection of current Godot processes and termination only for processes whose Delayed Self 0023ZX ownership is proven;
- preservation and exact reconciliation of the uncommitted 0023ZX drafts and fixtures;
- inventory and reviewed adoption of the generated `.uid` sidecars, without broad deletion;
- correction of the historical six-level catalog/progression/finale assertions to the accepted eight-level baseline;
- one fresh execution of the 0023ZX foundation after reconciliation, with no retry after a new substantive failure.

Until that authorization is supplied, no Codex task is active. Formal catalog/levels, Profile, UI, assets, localization, audio, build and release surfaces remain closed.
