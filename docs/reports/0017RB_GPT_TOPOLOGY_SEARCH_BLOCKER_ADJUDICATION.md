# Task 0017RB GPT bridge topology-search blocker adjudication

- Reviewed commit: `eea01c2f8c0956a69895d63dd14d3207b4a5eda7`
- Reported verdict: `BLOCKED_NO_BOUNDED_STRICT_GATE_CANDIDATE`
- GPT verdict: `TASK_0017RB_BLOCKER_ACCEPTED_SELECT_CONTEXTUAL_HELP_ONLY_REPAIR`
- Selected next direction: `BYTE_IDENTICAL_BRIDGE_PLUS_CONTEXTUAL_CAUSAL_HELP`
- Finale: `FINALE_PLANNING_BLOCKED_PENDING_HELP_REPAIR_RETEST`

## Accepted stop

Task 0017RB stopped correctly. It generated and screened 32 deterministic candidates, advanced six normal-band candidates, completed all five strict controls for four candidates, and exhausted the augmented finite state spaces for those four. No resource-limited result was used as a negative proof.

All four Stage-D candidates require a real mixed `BLOCKED/MOVED` spacing event and satisfy complete-unsolved no-ECHO, E2-only, E4-only, equal-2/2, and equal-4/4 controls. However, each candidate admits completed routes that bypass the required fixed-role following-snapshot chain, use YOU substitution at an A+B-open result, and reverse E2/E4 Plate roles. None may enter product data.

The current accepted bridge remains byte-identical at SHA-256 `805cdc05ea9b3805189ba7be4938cb7ede4e7056b9ae9a7e911df96a801d58ee`.

## Design consequence

The bounded search provides the missing evidence for a policy change in repair method, not a weakening of puzzle gates:

1. Retain all strict equal-delay, single-ECHO, dual-ECHO-necessity, and accepted formal proofs for the current bridge.
2. Do not enlarge the same topology-search family again without a new structural idea.
3. Do not accept a simpler candidate whose complete state space permits substitution, role reversal, or intended-chain bypass.
4. Preserve the accepted formal bridge and repair only the owner-observed missing causal/role explanation in manual Help.

This selects:

```text
BYTE_IDENTICAL_BRIDGE_PLUS_CONTEXTUAL_CAUSAL_HELP
```

The earlier Task 0017RA rejection of Help-only occurred before the larger bounded topology search. Task 0017RB now establishes that the authorized geometry families did not produce a valid simpler replacement. Help-only is therefore the minimum evidence-supported next experiment.

## Help boundary

The later implementation may modify reusable Gameplay Help only. It must use a stable data-derived predicate requiring:

- `metadata.intended_mechanics` contains `echo_spacing_change`;
- at least two ECHOs;
- exactly two Plates;
- a Door depending on both Plates.

It may explain only:

- a closed Door or wall can block one ECHO while another moves, changing spacing;
- compare `MOVED/BLOCKED` and try a different route or loop;
- two ECHOs must remain on separate A/B Plates while YOU remains free to cross after A+B opens;
- only YOU completes.

It must not include exact actions, coordinates, turns, target spacing, next input, exact E2/A–E4/B mapping, branch choice, screenshot route, solver output, remaining distance, or state-solvability claims. `two_echo_convergence` must not receive the bridge-specific section. Timeline and dotted-line presentation remain unchanged.

## Validation and promotion boundary

Because formal data remains byte-identical, no geometry authoring or new solver acceptance is authorized. The implementation must nevertheless prove:

- the bridge and all other formal hashes/metrics remain exact;
- Help open/close is zero-turn and canonical-state preserving;
- trigger and non-trigger levels are correct;
- copy remains causal, role-based, readable, and non-route;
- 960×540, grayscale, reduced-motion, Level Select, progression, focused tests, and aggregate regression remain healthy.

Machine acceptance will not close the curriculum P2. One owner-only repair retest remains required. The key question is whether contextual Help enables a credible experiment and correct two-ECHO/YOU role explanation with materially less restart burden, without revealing a route.

## Non-claims

This adjudication does not repair Help, close the curriculum finding, establish fresh or independent evidence, accept difficulty or fun, authorize finale planning, or establish export, release, or submission readiness.
