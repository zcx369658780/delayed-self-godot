# Task 0016R GPT bridge mechanical-necessity plan acceptance

- Verdict: `TASK_0016R_BRIDGE_MECHANICAL_NECESSITY_PLAN_ACCEPTED`
- Repository: `zcx369658780/delayed-self-godot`
- Accepted planning commit: `09ad4f6347af6d9cb17cb78f6f1cd11a07794d8d`
- Parent adjudication: `TASK_0016_PLAN_REQUEST_CHANGES_SCHEMA_V1_NECESSITY_CONTRADICTION`
- Accepted sequence direction: `INSERT_BRIDGE_BEFORE_CURRENT_LEVEL_7`
- Accepted bridge grammar: `SIMPLIFIED_TWO_PLATE_AND_TUTORIAL`
- Exact next formal target: `echo_spacing_bridge / Diverging Echoes / 错位回声`

## Acceptance

Task 0016R correctly withdraws the contract-infeasible one-Plate bridge and replaces it with a schema-v1-feasible three-role structure:

- shared-spawn E2 and E4 at delays 2 and 4;
- two distinct Plates A and B;
- one initially closed A+B Door;
- one player-only EXIT;
- a compact board no larger than 8×7;
- an early required `BLOCKED/MOVED` spacing-change event.

The contract-level necessity argument is now coherent under `simulation_v1`. A successful crossing requires three simultaneous roles at the committed A+B result and following open snapshot: one body on A, one body on B, and YOU staged to enter the Door. Distinct Plate cells prevent overlap from satisfying both dependencies. Requiring each Plate to be more than one traversable move from the Door prevents YOU from leaving a substituted Plate and entering the Door during the single useful open snapshot. Removing either ECHO therefore leaves only two bodies for three simultaneous roles.

This is feasibility, not proof of an authored room. The later implementation must still establish complete finite-state E2-only, E4-only, and no-ECHO unsolved controls, explicit YOU-substitution failure, all-shortest E2→A/E4→B role traces, and Door articulation. Any surviving single-ECHO or YOU-substitution solution fails the task.

## Accepted sequence and curriculum boundary

The accepted later sequence is:

1. existing Levels 1–6 unchanged;
2. `echo_spacing_bridge` as mandatory sequence 7;
3. byte-identical `two_echo_convergence` shifted to sequence 8 and remaining sole final;
4. future `signal_convergence` reserved as a separately gated sequence-9 finale.

The bridge deliberately repeats the broad two-ECHO/two-Plate/AND grammar only as a smaller curriculum step. It must remain materially below current Level 7 through an earlier visible divergence, at most 8×7, `L*=13–16`, lower exact search burden, only 2–3 meaningful decisions, and simpler recovery. If measured burden approaches or exceeds current Level 7, or if the only valid layout reproduces its full phase-management loop, return to planning.

## Presentation and product boundary

Accepted presentation policy:

```text
LABEL_SHAPE_PIP_PRIMARY_NO_NEW_LINES
```

Board state and transition-derived ECHO outcome feedback remain primary. Timeline is optional and cannot be required. Help remains contextual, zero-turn, and non-route. No new dotted relationship lines, exact walkthrough, target spacing, synchronization count, Gate, persistent Door, push box, or new mechanic is authorized.

## Non-claims

This acceptance does not claim that the bridge exists, solves, meets prospective metrics, is fun, smooths the owner difficulty curve, or closes residual Help/Timeline/clutter findings. It does not authorize `signal_convergence`, production art/audio, export, release, or submission.

The next gate is one bounded bridge implementation and complete machine/capture validation task. Owner review remains separate and authenticated-owner-only after GPT accepts the implementation.
