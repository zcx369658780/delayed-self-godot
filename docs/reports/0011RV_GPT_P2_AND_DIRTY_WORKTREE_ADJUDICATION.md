# Task 0011RV GPT material-P2 and dirty-worktree adjudication

- Status: `BLOCKED_PENDING_BOUNDED_CLEANUP_AND_STANDALONE_RETEST`
- Parent task: `tasks/0011RV_short_owner_window_fill_retest.md`
- Diagnostic task: `tasks/0011RVD_baseline_regression_and_expected_stderr_diagnosis.md`
- Accepted implementation: `a07c4be075aaa5c57e9d48869392f29dadaaa1d5`
- Current remote baseline before this adjudication: `9bb8414d045e098c635d5f9d9d1f25340e9256a6`
- Adjudication date: 2026-07-16
- Adjudicated by: GPT Project Sources advisor

## Accepted diagnostic result

Task 0011RVD established that the repository baseline is healthy:

- two fresh aggregate runs exited `0`;
- all seven required markers were present in both runs;
- `TASK_0003_TESTS_PASS assertions=314 vectors=9` remained exact;
- the previously observed `LEVEL_ID_MISMATCH` was an expected negative-path stderr emission, not the causal result of a regression;
- no state or input regression was observed during the owner check.

The original `BLOCKED_BASELINE_REGRESSION` is therefore superseded by:

```text
FALSE_BLOCKER_EXPECTED_NEGATIVE_PATH_STDERR
```

## Owner observation requiring adjudication

The owner confirmed:

- the former asymmetric right/bottom L-shaped gray remainder is gone;
- resize does not corrupt state or input;
- after maximizing the observed surface, the 960×540 presentation still occupies a relatively small centered region surrounded by substantial dark unused area;
- the owner considers this a material P2 presentation concern but explicitly agrees that the project may proceed toward the planned difficult Level 6, subject to GPT adjudication.

This observation is not yet sufficient to conclude that the shipping-style standalone game client has the same defect. Godot editor embedded-game preview, an editor viewport, a Codex-hosted preview, and a separate native game Window are different surfaces. A large editor or host window may contain a deliberately fixed embedded preview without representing the standalone client-area transform.

## Dirty-worktree incident

During the diagnostic/retest process, a Godot tool process reportedly:

- rewrote tracked `project.godot` unexpectedly; and
- generated multiple untracked `.gd.uid` files.

The parent tasks correctly prohibited restore, deletion, staging, or committing of these unexpected changes. The Codex session therefore stopped without creating review documents or a commit.

No evidence indicates that these files are intended implementation work. However, cleanup must not be performed blindly. The exact status, diff, paths, hashes, and process state must first be recorded outside the repository.

## Decision

Issue `Task 0011RVE — bounded generated-file cleanup, standalone-client discrimination, and owner-review completion`.

The new task may, under strict conditions:

1. require all Godot editor/game/tool processes associated with this repository to be closed before cleanup;
2. capture the exact dirty state and `project.godot` diff to external evidence;
3. restore only tracked `project.godot` to the exact current `HEAD` version when it is the sole unexpected tracked change and no user-authored work is present;
4. delete only individually enumerated, untracked `.gd.uid` files proven absent from `HEAD` and generated by the Godot run;
5. forbid `git clean`, wildcard deletion, broad restore, reset, checkout of directories, rebase, amend, and force operations;
6. launch a standalone native game process directly from the accepted Godot executable with `--path D:\Delayed_Self`, not through editor embedded play, Codex Browser, or another host preview;
7. distinguish standalone-client behavior from embedded-preview behavior;
8. record and commit the owner review only after the repository has been returned to a clean exact baseline and after any second wave of generated files is cleaned with the same bounded procedure.

## P2 decision branches

### A. Standalone client fills/scales correctly

If the native standalone client fills an enlarged 16:9 client area and uses only symmetric bars for non-16:9 shapes, classify the prior concern as:

```text
EMBEDDED_PREVIEW_ONLY_NOT_PRODUCT_WINDOW_BLOCKER
```

The owner-review report may recommend authorization of Level 6 after GPT acceptance.

### B. Standalone client also shows a small centered 960×540 presentation

If the standalone native client reproduces the substantial centered unused area, record a genuine material P2. Because P0=0, P1=0, state/input are healthy, the former asymmetric L-shaped defect is closed, and the authenticated owner explicitly agrees to proceed, the report may recommend:

```text
MATERIAL_P2_ACCEPTED_AS_DEFERRED_PRESENTATION_RISK_PENDING_GPT_FINAL_ADJUDICATION
```

This branch does not itself authorize Level 6. GPT must review the evidence and decide whether to defer further presentation work while allowing content development.

### C. Standalone client is clipped, asymmetric, inconsistent, or state/input regresses

Record the causal P1/P2, do not recommend Level 6, and stop for a separately authorized implementation repair.

## Non-claims

This adjudication does not authorize implementation changes, tracking or ignoring `.gd.uid` files as a permanent repository policy, responsive-layout redesign, asset/font work, Level 6 implementation, Gate, save, export, release, or candidate acceptance. It authorizes only bounded cleanup, surface discrimination, evidence recording, and completion of the already-started owner review.