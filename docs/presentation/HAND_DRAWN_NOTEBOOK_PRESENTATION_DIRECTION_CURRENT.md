# Hand-drawn delayed notebook / ink-ECHO style (CURRENT)

- Status: `PLANNING ONLY / NO ASSET OR FONT AUTHORITY`
- Date: 2026-07-21; task: 0023ZW

## Direction and rationale

Keep the deterministic grid and collision geometry hidden beneath a **HAND-DRAWN DELAYED NOTEBOOK / INK-ECHO STYLE**: warm paper field, irregular ink outlines, sparse correction marks and restrained echo trails. It is a low-cost identity layer that makes delayed causality readable rather than a cosmetic replacement for mechanics.

| Element | Readability contract |
|---|---|
| YOU | solid dark ink body, filled compass/core mark |
| ECHO generations | translucent offset ink, stable `E2`/`E4` badge and distinct outline pattern |
| crate / key / lock | box hatch / key silhouette / matching stamped lock; color is redundant |
| sensor | plate shape plus `ANY`, YOU or ECHO silhouette stamp |
| barrier group / bridge | common colored ink ribbon, group glyph and connective stroke |
| exit | paper-tab destination icon, never a color-only goal |

Title/menu buttons, level cards, HUD labels, Help/Timeline and finale overlay retain their navigation semantics. Use slight line wobble and short state-change pulses only; collision, timing and focus must remain unambiguous. Accessibility requires shape/icon/text redundancy, contrast checks, no rapid flashing, and 1080p text readability.

## Font and asset plan

Evaluate `LXGW WenKai GB / 霞鹜文楷 GB` for UI under **OFL-1.1** only after license-file and coverage verification; it is a candidate, not an acquired dependency. Body text must remain highly legible; any custom/strong title treatment needs separately clear licensing.

Submission-minimum inventory: paper background (1), ink palette/token sheet (1), YOU/ECHO/crate/key/lock/sensor/barrier/exit sprites or vector equivalents (8 groups), six UI surfaces, 6–10 small state marks, and credits/license ledger entries. Prefer owner-created vectors or clearly licensed original-source files; record source, author, license, modification, attribution and destination before import. Cap initial compressed visual additions at 15 MB and keep optional decorative variants outside the core.

Minimum polish is board readability, menu/HUD/Help coherence and finale/credit surfaces. Optional reskin breadth (room-specific backgrounds, animated paper, many themes, elaborate shaders) is cut before core flow.
