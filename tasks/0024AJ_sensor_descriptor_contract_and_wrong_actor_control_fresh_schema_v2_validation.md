# Task 0024AJ — Correct sensor descriptor selection, prove wrong-actor inertness, and complete fresh schema-v2 visual validation

- Status: `READY`
- Gate: `SENSOR DESCRIPTOR CONTRACT / SWAPPED-IDENTITY CONTROL / FRESH SCHEMA-V2 PRESENTATION VALIDATION / MULTI-SUBAGENT REQUIRED`
- Repository/root/branch: `zcx369658780/delayed-self-godot` / `D:\Delayed_Self` / `main`
- Required accepted checkpoint: `934ebefd4747db351db11c7628130a2275fd3786`
- Historical blocked Task 0024AI result: `2df29272b4c1696063aae00ed8e796e28f4f5929`
- Latest GPT verdict: `TASK_0024AI_HONEST_SENSOR_SELECTOR_AND_WRONG_ACTOR_SAMPLING_BLOCKER_ACCEPTED_FRESH_SENSOR_GATE_REQUIRED`
- Provisional presentation implementation baseline: `25d7fda3937fc269cdf730c14698e47bfb209c29`
- Accepted wrapper blob: `5f14eb0feff903b40311c1c7283b20e2eabbffc2`
- Production baseline: exactly eight cataloged levels; sequence 8 remains the sole formal finale
- Accepted uncatalogued candidates: `crate_shadow_timing`, `key_route_commitment`, `identity_shift_bridge`, `lasting_echo_latch`
- Recommended main-agent model: `GPT-5.6 Sol Max`; fallback `GPT-5.6 Sol High`

## 1. Objective

Under a new task identity:

1. preserve all Task 0024AF–0024AI historical artifacts byte-for-byte;
2. preserve the four-candidate public-loader-to-Gameplay preflight proven by Task 0024AI;
3. select sensor descriptors by stable ID and the public level activator contract rather than invented shortened variants;
4. prove wrong-actor inertness through the accepted sequence-11 swapped-identity control contract;
5. establish the exact thirteen-entry dynamic lifecycle-signature registry;
6. validate the provisional product implementation unchanged first;
7. modify presentation product files only if a named retained assertion proves a real product defect;
8. complete two passing repeatable focused qualifications;
9. execute one fresh retained five-case Task 0024AJ cycle;
10. determine whether the minimum schema-v2 presentation is machine-ready for owner review.

This task does not conduct owner subjective playtesting and does not authorize production catalog, progression, finale, or Profile changes.

## 2. Historical immutability

All committed `task_0024af_*`, `task_0024ag_*`, `task_0024ah_*`, and `task_0024ai_*` artifacts are historical and immutable, including their validators, adapters, qualification records, reports, and any final-cycle placeholders.

Do not edit, delete, repair, relabel, reconstruct, or rerun those task identities. Task 0024AI did not declare or consume a retained cycle, but its validator and qualification records remain terminal blocker evidence.

All new validation, probe, adapter, audit, and retained-cycle artifacts must use the `task_0024aj_*` identity.

## 3. Mandatory multi-subagent doctrine

Read and route through:

```text
D:\Delayed_Self\.codex\config.toml
```

`.codex/config.toml` is the sole permitted pre-existing untracked path. Record its path and SHA-256 without exposing contents. Do not modify, stage, commit, delete, or copy it. Require byte identity at closeout.

Use at least three distinct roles:

1. **source/contract mapper** — inspect the helper's exact sensor descriptor contract, sequence-11 level/control evidence, Task 0024AI failures, and protected boundaries;
2. **validation worker** — implement the Task 0024AJ Gameplay preflight, exact sensor selector, swapped-identity control, thirteen-entry registry, adapter, probes, and retained driver;
3. **independent reviewer/test auditor** — verify stable-ID selection, public activator-derived variants, control equivalence, registry completeness, semantic preservation, and fail-closed final behavior.

A documentation/evidence writer may be a fourth role. Only the main agent may launch Godot, integrate changes, execute retained cases, stage, commit, or push. No subagent may run Godot, perform Git writes, or access Profile/user data. Parallelism is permitted only for disjoint read-only analysis; one writer per file.

If multi-subagent routing or local configuration parsing fails, return an honest blocker rather than silently falling back to one agent.

## 4. Startup gate

Before editing or launching Godot:

1. require root `D:\Delayed_Self`, branch `main`, expected origin, and no Git operation in progress;
2. fetch and fast-forward only;
3. require `HEAD == origin/main`, divergence `0/0`, ancestry of `934ebefd4747db351db11c7628130a2275fd3786`, and no tracked or staged changes;
4. permit only untracked `.codex/config.toml`, record its SHA-256, and require it to remain unchanged;
5. require zero pre-existing Godot processes;
6. require Godot `4.7.1.stable.steam.a13da4feb` and exact wrapper blob `5f14eb0feff903b40311c1c7283b20e2eabbffc2`;
7. read `AGENTS.md`, current pointer docs, this task, Task 0024AI task/report/GPT acceptance, Tasks 0024AF–AH blocker reports/GPT acceptances, Task 0024AB sequence-11 corrected report/summary, Task 0024AE acceptance/report, Task 0024AD matrix, accepted sequence-9–12 reports, schema-v2/Simulation contracts, and the lifecycle policy;
8. record exact hashes for the provisional presentation helper/Gameplay file, every historical Task 0024AF–AI artifact, all twelve levels, production catalog/route/Profile files, staging route, mechanics foundation, and wrapper;
9. stop on drift, process ambiguity, additional untracked paths, historical mismatch, or any need to inspect production Profile/user data.

The accepted Task 0024AE route case may use its exact isolated fixture only through its existing runner and cleanup contract. The focused visual case must access no Profile surface.

## 5. Authorized repository paths

Create or modify only:

```text
scripts/gameplay/schema_v2_presentation.gd
scripts/gameplay/vertical_slice.gd
tests/presentation/task_0024aj_sensor_contract_and_dynamic_signature_validation.gd
tests/presentation/task_0024aj_sensor_contract_and_dynamic_signature_validation.gd.uid
tests/tools/task_0024aj_*.ps1
tests/tools/task_0024aj_*.py
tests/tools/task_0024aj_*.json
docs/presentation/SCHEMA_V2_MINIMUM_OWNER_REVIEW_VISUAL_CONTRACT_CURRENT.md
docs/playtest/SUBMISSION_CORE_1_12_OWNER_CURRICULUM_REVIEW_CURRENT.md
docs/reports/0024AJ_SENSOR_CONTRACT_AND_DYNAMIC_SCHEMA_V2_VISUAL_VALIDATION_REPORT.md
docs/CODEX_ACTIVE_TASK_CURRENT.md
docs/CODEX_STARTUP_SNAPSHOT_CURRENT.md
docs/DEVELOPMENT_PLAN_CURRENT.md
docs/DOC_INDEX_CURRENT.md
docs/HANDOFF_MASTER_CURRENT.md
docs/project_memory.md
docs/production/EXPANDED_CONTENT_SUBMISSION_ROADMAP_CURRENT.md
```

No other path may change. Historical Task 0024AF–AI files are not authorized for modification.

### 5.1 Provisional product implementation policy

Begin with these files byte-identical to result commit `2df29272b4c1696063aae00ed8e796e28f4f5929`:

```text
scripts/gameplay/schema_v2_presentation.gd
scripts/gameplay/vertical_slice.gd
```

First validate them unchanged.

- If all Task 0024AJ assertions pass, do not modify either product file.
- A modification is permitted only after a retained repeatable assertion identifies a specific missing, collapsed, or incorrect presentation state after the harness and selector contracts have passed.
- Any product-file change must cite the exact failed label, receive independent review, and preserve route/input/Simulation/completion semantics.
- Do not weaken tests, delete a required state, or alter mechanics to obtain a pass.

## 6. Frozen and protected surfaces

Do not modify:

```text
scenes/**
project.godot
data/catalog/**
data/levels/**
scripts/app/**
scripts/simulation/**
scripts/solver/**
scripts/gameplay files other than the two authorized presentation files
schemas/**
tests/run_all.gd
tests/simulation/**
tests/levels/**
tests/curriculum/**
tests/playtest/**
tests/presentation/task_0024af_*
tests/presentation/task_0024ag_*
tests/presentation/task_0024ah_*
tests/presentation/task_0024ai_*
tests/tools/run_owned_godot_headless.ps1
all Task 0024T–0024AI historical evidence
localization/**
assets/**
audio/**
build/export/release/submission surfaces
```

Do not change input, collision, transitions, restart, history, completion, routes, catalog, finale, or Profile behavior.

## 7. Task 0024AJ wrapper adapter and parser qualification

Create a new Task 0024AJ adapter instead of editing historical tools, for example:

```text
tests/tools/task_0024aj_invoke_wrapper_case.ps1
```

It must preserve the accepted Task 0024AG/AI parsing doctrine:

1. invoke `tests/tools/run_owned_godot_headless.ps1` unchanged;
2. collect pipeline output as raw values;
3. select exactly one nonempty compact JSON object record;
4. explicitly run `ConvertFrom-Json -Depth 100`;
5. require schema version, status, underlying status, native exit, marker result, stderr, cleanup count, owned/unproven PID arrays, and stdout with correct non-null types;
6. reject missing, duplicate, non-JSON, truncated, array-root, null-field, wrong-version, and wrong-type inputs;
7. retain raw wrapper JSON SHA-256, byte count, and parsed record;
8. never infer missing data.

Create Task 0024AJ negative probes. Statistically compare the new adapter contract with Task 0024AI and record that differences are limited to task identity, paths, and stricter checks.

## 8. Four-candidate Gameplay route preflight

Before any lifecycle registration, repeat the already-proven fail-closed preflight for all four candidates:

1. load the exact path using public `LevelLoader.load_file`;
2. read the exact nonempty `loaded.level.level_id`;
3. configure a fresh Gameplay instance before tree entry with that exact ID and exact path;
4. require `configure_route_payload()` true;
5. add the instance to the tree only after configuration;
6. require `load_error == ""`, `is_runtime_ready() == true`, and no `LEVEL_ID_MISMATCH`;
7. require the returned route payload ID/path to match the public loader result exactly;
8. require the real Gameplay presentation snapshot to equal the pure helper snapshot for the same initial state;
9. require deep-copy mutation resistance;
10. free and verify each instance before constructing the next one.

Internal aliases may index dictionaries but must never enter route payload fields.

All four preflight records must pass before sensor or registry assertions execute.

## 9. Exact public sensor descriptor contract

For `identity_shift_bridge`, identify sensor descriptors by stable ID derived from the public level definitions:

```text
sensor:sensor_you
sensor:sensor_echo
```

Do not select them using invented shortened variants such as `YOU_INACTIVE`, `ECHO_INACTIVE`, `YOU_ACTIVE`, or `ECHO_ACTIVE`.

For each sensor definition, derive the expected descriptor state from its exact public `activator`:

```text
expected inactive variant = <activator> + "_INACTIVE"
expected active variant   = <activator> + "_ACTIVE"
```

Therefore the current accepted level must expose:

```text
YOU_ONLY_INACTIVE
YOU_ONLY_ACTIVE
ECHO_ONLY_INACTIVE
ECHO_ONLY_ACTIVE
```

Also require:

- `sensor_you` glyph token `Y`;
- `sensor_echo` glyph token `E`;
- stable ID and cell match the public level definition;
- both expose the same dependent barrier-group token;
- inactive and active variants have distinct shape and monochrome signatures;
- YOU and ECHO signatures remain distinct even at the same activity state;
- descriptor selection is independent of array order.

A public helper may be added inside the new validator for stable-ID lookup. Do not alter the product helper solely to accommodate a test selector.

## 10. Accepted sequence-11 witness sampling

Use the accepted exact witness:

```text
RIGHT RIGHT UP UP LEFT LEFT UP UP RIGHT RIGHT RIGHT RIGHT RIGHT RIGHT RIGHT RIGHT DOWN DOWN
```

Drive it only through shared `Simulation.transition` and retain every state.

Required states:

- initial: `sensor:sensor_you` is `YOU_ONLY_INACTIVE`, `sensor:sensor_echo` is `ECHO_ONLY_INACTIVE`, and the group is closed;
- action 10: `sensor:sensor_you` is `YOU_ONLY_ACTIVE`, `sensor:sensor_echo` is `ECHO_ONLY_ACTIVE`, and both group cells are open;
- actions 11 and 12: closure is requested while each grouped bridge cell is occupied; every affected cell is `OPEN_DEFERRED`, with common group token and signature;
- action 13: both cells are closed after vacancy;
- restart: exact initial sensor and barrier descriptors return.

Before registering a sensor signature, require the expected stable ID, exact public variant, glyph, cell, and actor occupancy facts.

## 11. Wrong-actor inertness through accepted swapped-identity control

Do not assume every sensor is inactive at one arbitrary fixed witness turn.

Reproduce the accepted Task 0024AB swapped-identity control using the same bounded in-memory control construction and shared Simulation authority:

1. deep-copy the accepted `identity_shift_bridge` level;
2. preserve sensor IDs, positions, barrier group, geometry, actors, delays, and all non-identity fields;
3. swap only the two sensor `activator` values (`YOU_ONLY` ↔ `ECHO_ONLY`) in the exact manner used by the accepted sequence-11 identity control;
4. record a deterministic control-definition digest and prove the original level remains byte/semantic unchanged;
5. run the accepted witness prefix through the last state reachable before the control blocks later bridge progress;
6. at the physical paired-occupancy moment, require each actor to occupy the opposite-required sensor identity;
7. require both sensor descriptors to remain inactive under their swapped public activators;
8. require the grouped barrier not to present the accepted correct-pair open state;
9. require the control to reproduce the accepted identity-necessity conclusion without implementing alternate transitions.

If the exact Task 0024AB control method differs from a literal activator swap, reproduce that accepted method instead and document the difference. Do not invent a looser control.

Wrong-actor evidence must name the sensor stable ID, occupying actor identity, required activator, state variant, signature, and control source. It must not require unrelated correctly occupied sensors to be inactive.

## 12. Exact thirteen-entry lifecycle-signature registry

Create one registry with exactly these labels:

```text
crate
floor_key
held_key
lock
sensor_you_inactive
sensor_you_active
sensor_echo_inactive
sensor_echo_active
barrier_closed
barrier_open
barrier_open_deferred
latch_inactive
latch_activated
```

For every label retain:

- source level ID;
- exact turn/action index or state classification;
- descriptor family, stable ID, cell, and state variant;
- shape token, glyph token, and type/group token;
- exact `monochrome_signature`;
- lifecycle evidence and actor occupancy where applicable.

Require:

1. all thirteen labels present exactly once;
2. every signature a nonempty string;
3. every descriptor satisfies the public contract;
4. every label maps to the intended family/state;
5. all thirteen signature strings pairwise distinct;
6. sensor entries use stable-ID/public-activator selection;
7. the registry record contains the full label-to-signature map and count `13`.

Retain the accepted sequence-9, sequence-10, and sequence-12 witness sampling from Task 0024AI, but implement it under new Task 0024AJ paths. Do not reuse historical output as final evidence.

## 13. Fit, layering, v1, and 1–12 regression

Prove:

- all descriptor bounds fit inside the existing logical gameplay area for sequences 1–12;
- layer ordering matches terrain → underlay/open edges → blockers/interactables → actors → tokens/inventory;
- actor occupancy does not erase sensor or barrier-edge identity;
- sequences 1–8 load and produce no spurious schema-v2 descriptors;
- the accepted Task 0024AD focused validator preserves all twelve `(L*, N*)` values;
- route, input, Simulation, completion, finale acknowledgement, and request-back semantics remain unchanged.

Do not claim human clarity or final art quality from these tests.

## 14. Static preservation qualification

Create a machine-readable Task 0024AJ static qualification that:

1. compares product presentation files against commit `2df29272b4c1696063aae00ed8e796e28f4f5929`;
2. records whether either product file changed;
3. if changed, proves route-payload validation, loading, input mapping, Simulation transition/restart calls, completion/finale acknowledgement, and request-back behavior remain semantically unchanged;
4. proves the helper contains no loader, file, catalog, Profile, user-path, transition, or restart implementation;
5. proves all changed paths are authorized;
6. proves every historical Task 0024AF–AI artifact byte-identical;
7. proves `.codex/config.toml` remains untracked, unstaged, and byte-identical;
8. proves production scene/catalog/route/Profile/level/foundation files unchanged.

## 15. Evidence format and parser

Emit independently parseable prefixed JSON records below 1800 UTF-8 bytes. Include separate records for:

- multi-subagent ledger;
- adapter/parser qualification;
- static preservation;
- four-candidate Gameplay preflight;
- sensor public-contract inventory;
- accepted sensor/barrier lifecycle;
- swapped-identity wrong-actor control;
- thirteen-entry registry;
- crate lifecycle;
- key/lock/inventory lifecycle;
- latch lifecycle;
- fit/layering/v1/1–12 regression;
- summary.

The capture parser must reject missing, duplicate, truncated, invalid-JSON, and over-bound records and retain exact counts and byte lengths.

## 16. Repeatable qualification

Before final retention, repeatable qualification is repairable.

Every Godot execution must use the accepted wrapper through the new adapter, invoke cleanup exactly once, retain no owned/unproven PID, and end with Godot zero. Preserve failed attempts honestly. No subagent may run Godot.

Require at minimum:

1. parser/adapter negative probes pass;
2. static preservation passes;
3. four-candidate Gameplay preflight passes twice;
4. exact public sensor selector contract passes twice;
5. swapped-identity wrong-actor control passes twice;
6. complete thirteen-entry registry passes twice with pairwise uniqueness;
7. comprehensive focused validation passes twice with complete non-null wrapper records;
8. PRE_FINAL frozen audit has zero mismatch.

Do not declare a final manifest until all items above are stable.

## 17. Fresh retained Task 0024AJ cycle

After all qualification is stable, execute exactly once each under one new Task 0024AJ manifest:

1. focused Task 0024AJ sensor-contract/dynamic-signature validator;
   - marker `TASK_0024AJ_SENSOR_CONTRACT_DYNAMIC_SIGNATURE_PASS`;
   - stderr empty;
2. accepted Task 0024AE isolated staging route qualification;
   - marker `TASK_0024AE_ISOLATED_STAGING_OWNER_ROUTE_PASS`;
   - stderr empty;
3. accepted Task 0024AD 1–12 batch validator;
   - marker `TASK_0024AD_SUBMISSION_CORE_1_12_BATCH_PASS`;
   - stderr empty;
4. Aggregate;
   - exact marker `TASK_0003_TESTS_PASS assertions=621 vectors=9`;
   - stderr exactly `LEVEL_ID_MISMATCH`;
5. corrected complete-v2;
   - marker `TASK_0024V_V2_COMPLETE_PASS`;
   - stderr empty.

All wrapper-driven cases must use the new adapter and retain raw wrapper JSON SHA-256, parsed status, native exit, marker, stderr, cleanup, PID arrays, and Godot before/after. The route case may use its accepted runner, but the Task 0024AJ driver must explicitly parse and validate its output.

Fail immediately on null required fields or any case failure. Do not run later cases after failure. Final Godot count must be zero. Once declared consumed, do not rerun the Task 0024AJ cycle.

## 18. Report and status update

Create:

```text
docs/reports/0024AJ_SENSOR_CONTRACT_AND_DYNAMIC_SCHEMA_V2_VISUAL_VALIDATION_REPORT.md
```

The report must include:

- exact product hashes before/after and whether product files changed;
- multi-subagent routing ledger;
- Task 0024AF–AI frozen evidence;
- four-candidate route-preflight evidence;
- exact sensor selector root cause and corrected stable-ID/public-activator contract;
- swapped-identity control construction and equivalence to Task 0024AB;
- full thirteen-entry registry and pairwise uniqueness;
- repeatable qualifications, parser probes, static audits, and retained cycle;
- `.codex/config.toml` identity;
- explicit owner/final-art/catalog boundaries.

On pass, update the owner worksheet only to:

```text
owner_route_status = MACHINE_QUALIFIED_ISOLATED_STAGING_ROUTE
owner_playthrough_status = NOT_YET_REVIEWED
presentation_readiness = MACHINE_QUALIFIED_SCHEMA_V2_MINIMUM_OWNER_REVIEW_REQUIRED
visual_art_direction_status = NOT_FINAL
catalog_admission_status = NOT_AUTHORIZED
```

Do not populate subjective answers.

## 19. Commit and push

On pass or honest blocker:

1. stage only authorized repository paths;
2. never stage `.codex/config.toml`;
3. inspect staged paths and run `git diff --cached --check`;
4. create one single-topic result commit without amend/rebase/squash;
5. fetch before push and stop on remote drift;
6. push only a clean fast-forward to `origin/main`;
7. require `HEAD == origin/main`, divergence `0/0`, no tracked/staged changes, only unchanged `.codex/config.toml` untracked, and Godot zero;
8. report correction commits transparently.

Suggested pass commit:

```text
test: validate sensor contract and schema v2 dynamic presentation
```

## 20. Terminal verdict

Return exactly one:

```text
SCHEMA_V2_SENSOR_CONTRACT_DYNAMIC_PRESENTATION_READY_FOR_GPT_REVIEW
BLOCKED_SCHEMA_V2_SENSOR_CONTRACT_VALIDATION
PROTECTED_DATA_BOUNDARY_BREACH
```

A pass authorizes only GPT review of the minimum schema-v2 presentation evidence. It does not authorize owner subjective acceptance, final visual-art acceptance, production catalog/progression/finale/Profile changes, localization, assets/audio, build/export, release, upload, or submission.