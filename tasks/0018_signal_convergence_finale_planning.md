# Task 0018 — Plan the `signal_convergence` sequence-9 finale

- Status: `READY`
- Gate: `FINALE DESIGN PLANNING / DOCUMENTATION ONLY`
- Repository/root/branch: `zcx369658780/delayed-self-godot` / `D:\Delayed_Self` / `main`
- Latest owner adjudication: `TASK_0017RCV_OWNER_BRIDGE_MODEL_ACCEPTED_WITH_RESIDUAL_LIMITATIONS`
- Accepted bridge: `echo_spacing_bridge`, sequence 7
- Accepted synthesis room: `two_echo_convergence`, sequence 8 and current sole final
- Future target: `signal_convergence`, prospective sequence 9

## 1. Objective

Produce one implementation-ready planning specification for the future finale:

```text
signal_convergence / Signal Convergence / 信号汇流
```

The finale must synthesize accepted mechanics without introducing a new mechanic. This task is planning and documentation only. Do not create a formal level, edit the catalog, run Godot, run the solver, alter Help/Timeline, or implement product code.

## 2. Startup and required reads

Verify exact root, `main`, SSH origin, clean tracked/untracked worktree, synchronized `HEAD == origin/main`, and no residual Godot process.

Read in order:

1. `AGENTS.md`;
2. current startup and active-task documents;
3. this task;
4. `docs/reports/0017RCV_GPT_OWNER_CONTEXTUAL_HELP_RETEST_ADJUDICATION.md`;
5. Task 0017RCV review/report;
6. Task 0017RC acceptance and implementation report;
7. Task 0017C bridge validation/report;
8. Task 0015/0015R Level 8 validation, presentation repair, and owner evidence;
9. Task 0012 Level 6 validation/report;
10. `docs/level_design/LEVEL_8_FINALE_AND_DIFFICULTY_BRIDGE_PLAN_CURRENT.md`;
11. `docs/level_design/FIVE_LEVEL_DIFFICULTY_RECOVERY_AND_LEVELS_6_8_PLAN.md`;
12. simulation, schema, catalog, progression, Gameplay, Help, Timeline, presentation, testing, capture, and safety contracts;
13. all eight formal levels and the eight-entry catalog;
14. current GDD finale, narrative, non-goal, scope, and festival-delivery sections.

Before writing, print a concise planning outline with candidate structures, curriculum chain, proof feasibility, presentation policy, owner-evidence boundary, selected paths, and stop conditions.

## 3. Frozen authorities

Preserve:

- `simulation_v1`, `level_v1`, catalog-v1 semantics, solver behavior, history, simultaneous independent movement, legal overlap, combinational Doors, open-result/next-input entry, player-only EXIT, restart, and canonical key;
- all eight existing formal JSON files, hashes, metrics, sequence, prerequisites, and acceptance boundaries;
- sequence 7 `echo_spacing_bridge` and its contextual causal Help;
- sequence 8 `two_echo_convergence`, byte-identical SHA-256 `772410d3b8bb807e35472ee2a7d07b703de66e1e0e92ffb4d1ff8433a345d5fd`;
- Task 0015R divergence feedback;
- Task 0017P scrollable Level Select;
- `LABEL_SHAPE_PIP_PRIMARY_NO_NEW_LINES`;
- Timeline as optional and never required;
- Help as manual, zero-turn, contextual, and non-route;
- owner-only review constraint and the absence of fresh/independent evidence.

Do not propose Gate, latch, persistent/one-shot Door, switch, timer, key, box, push, hazard, enemy, combat, teleport, interaction, actor collision, new action, actor-specific Plate ownership, new goal semantics, literal WAIT teaching, route walkthrough, or always-on line expansion.

## 4. Finale role

The finale must be a synthesis rather than a longer duplicate of sequence 8.

Prospective frozen entity grammar:

- one YOU;
- exactly two shared-history ECHOS, provisionally delays 2 and 4;
- exactly three distinct Plates;
- exactly two initially closed Doors;
- one Door with a single-Plate dependency;
- one Door with a two-Plate A+B dependency;
- exactly one player-only EXIT beyond the final articulation;
- board no larger than 10×8, preferably 9×8 or smaller;
- no third dependency stage;
- no new mechanic.

The intended two-stage mental model is:

1. establish and use one visible single-Plate Door window;
2. use the opened route or changed phase to align the two ECHOS;
3. form the two-Plate A+B result while YOU remains free;
4. enter the final Door from the following open snapshot and complete with YOU.

The plan must explain how this differs from:

- Level 6's two temporally separated single-Plate Doors;
- the bridge's one spacing-change lesson;
- sequence 8's one simultaneous A+B convergence.

## 5. Candidate comparison

Compare exactly three bounded structural directions:

### A — Single Door first, A+B Door final

The single dependency creates route access or phase change; the later A+B Door is the final synthesis.

### B — A+B Door first, single Door final

The simultaneous convergence opens an intermediate region; a later single-Plate window completes the sequence.

### C — Interleaved visible stages

Both Doors remain visible in one compact loop, but their useful windows occur in a deliberately interleaved order without adding a third stage.

Score each from 1–5 using weighted criteria:

- curriculum synthesis 20%;
- mechanical differentiation 15%;
- observability 15%;
- proof feasibility 15%;
- recovery quality 10%;
- anti-padding 10%;
- 960×540 / grayscale presentation 10%;
- implementation risk 5%.

Select exactly one primary structure and at most one fallback. Scores are planning judgments, not measured evidence.

## 6. Prospective quantitative bands

Freeze implementation targets, not observed claims:

```text
21 <= L* <= 30
1 <= N* <= 12
N* status = EXACT preferred; CAPPED is not acceptance
20000 <= visited_states <= 300000
2000 <= maximum_frontier <= 50000
meaningful decisions = 4–6
recoverable alternatives >= 4
dependency stages = exactly 2
```

The plan may narrow these bands with a documented rationale, but must not broaden them beyond the stated ceiling.

A valid finale must be materially more complex than sequence 8 without relying on empty distance, repeated collision, literal WAIT, hidden rules, or unreadable state.

## 7. Mandatory future proof family

The plan must define an implementation proof package covering:

1. strict schema load, catalog validation, direct launch, normal unlock, completion progression, and sole-final migration;
2. exact solve, witness replay, restart, SHA-256, `L*`, `N*`, visited states, frontier, and declared limits;
3. all-shortest or complete augmented traces for both dependency stages and following-snapshot Door entries;
4. no-ECHO, E2-only, E4-only, equal 2/2, and equal 4/4 complete-unsolved controls where relevant to the selected causal chain;
5. individual ECHO, Plate, and Door necessity;
6. single-dependency and A+B dependency simplifications;
7. Door removal, Plate relocation, blocker relocation, articulation, and bypass controls;
8. YOU-substitution exclusion for the final A+B stage;
9. overlap and player-only EXIT controls;
10. shortcut audit proving early actions cannot accidentally satisfy both stages;
11. 4–6 meaningful decisions and at least four recoverable alternatives;
12. exact preservation of all eight prior hashes and metrics;
13. Level Select nine-entry reachability and sole-final migration;
14. reusable Gameplay, divergence feedback, Help predicate isolation, Timeline optionality, reduced-motion, grayscale, and 960×540 captures;
15. aggregate regression with a new stable Task 0018 implementation marker.

Resource-limited results may not be called exact or complete-unsolved.

## 8. Presentation and Help policy

Planning must preserve:

- board state, labels/shapes/pips, E2/E4 badges/outlines, Door state, and transition feedback as primary;
- no new always-on dotted relationship lines;
- no dependence on Timeline or reverse-counting;
- contextual Help may explain reusable causal relationships and role constraints only;
- Help may not disclose an action sequence, exact role assignment, coordinates, turns, target spacing, next action, branch choice, solver state, remaining distance, or complete route;
- sequence-7 bridge-specific Help must not leak into the finale unless the finale independently satisfies the accepted data predicate; any finale-specific Help requires a separately specified stable data predicate and later implementation authority;
- product-wide localization remains a later coherent gate; do not create an isolated Chinese-only finale or bridge exception in this task.

## 9. Owner evidence and review plan

Carry forward:

```text
OWNER_ONLY_REVIEW
FRESH_INDEPENDENT_REVIEW_UNAVAILABLE_BY_OWNER_CONSTRAINT
```

The owner has prior exposure to sequence 7 and sequence 8. A future finale review may evaluate the new finale route without third-party recruitment, but must disclose all prior project familiarity and any route exposure that occurs during development.

Plan review questions must cover:

- whether the two stages are separately visible;
- whether the first stage causally enables the second;
- whether the finale feels like synthesis rather than repetition;
- whether recovery is understandable without Timeline;
- whether Help supports an experiment without giving a route;
- whether visual density remains acceptable;
- whether the ending communicates completion and narrative payoff.

Do not require or suggest a third-party reviewer.

## 10. Narrative/finale boundary

Include a bounded finale-presentation section covering:

- level title and one-sentence narrative purpose;
- completion message or short ending beat using existing presentation capabilities only;
- no voice acting, cutscene system, branching narrative, account/network feature, or new asset pipeline;
- no claim that narrative/audio/animation is implemented;
- any art/audio/animation request becomes a later separately authorized production task.

## 11. Outputs

Create:

```text
docs/level_design/SIGNAL_CONVERGENCE_FINALE_PLAN_CURRENT.md
docs/reports/0018_SIGNAL_CONVERGENCE_FINALE_PLANNING_REPORT.md
```

Update only the minimum necessary CURRENT Markdown pointers, roadmap, finale plan, GDD planning references, document index, handoff, development plan, presentation direction, and project memory.

Do not modify product data, catalog, formal levels, source, tests, schema, scenes, project settings, prior reports/reviews/acceptances, assets, persistence, build, export, or release surfaces.

## 12. Git discipline

Use one documentation-only commit:

```text
docs: plan Delayed Self signal convergence finale
```

Stage explicit authorized Markdown paths only. Require `git diff --check`, fetch before push, stop on remote drift, push normally, and finish with a clean worktree and `HEAD == origin/main`.

## 13. Stop conditions

Stop with a specific blocker if:

- no candidate remains materially differentiated from sequence 8;
- the design needs a new mechanic, schema, Simulation, solver, or Timeline dependency;
- two stages cannot remain observable at 960×540;
- the concept only adds route length or repeated replay;
- the final stage admits unavoidable YOU substitution or bypass that defeats the intended synthesis;
- no unique primary structure can be selected;
- any product path changes.

## 14. Terminal verdict

On success return:

```text
SIGNAL_CONVERGENCE_FINALE_PLAN_READY_FOR_GPT_REVIEW
```

Include commit SHA, selected structure, fallback if any, frozen grammar, prospective bands, proof package, presentation/Help policy, owner-review boundary, output paths, and final Git state.

This task does not authorize finale implementation, production art/audio, localization implementation, owner acceptance, candidate readiness, export, release, or submission.
