# Task 0023ZH — Execute the repaired AppRoot Profile-session full matrix twice

- Status: `READY`
- Gate: `PROTECTED PLAYER DATA / REPAIRED FIXED-FIXTURE APPROOT FULL-MATRIX EXECUTION`
- Repository/root/branch: `zcx369658780/delayed-self-godot` / `D:\Delayed_Self` / `main`
- GPT prerequisite acceptance: `docs/reports/0023ZG_GPT_APP_ROOT_PROFILE_SESSION_HARNESS_ORDER_REPAIR_ACCEPTANCE.md`
- Required checkpoint: `15e063e7cb45342e14d60149c0b03b34ddfb9533`
- Accepted harness implementation checkpoint: `cfbcbf6c8c7990db232a2fa6fbb22d937e0b623a`
- Fixed task-owned fixture ID: `task_0023zh_app_root_profile_session`
- Canonical target: `user://delayed_self_test_profiles/task_0023zh_app_root_profile_session/delayed_self_profile.json`
- Execution guard: `--task-0023zh-fixture-execution`

## 1. Objective

Execute the repaired and retargeted canonical AppRoot Profile-session harness against the exact Task 0023ZH fixture and determine whether the complete configured-session matrix passes twice from separately clean fixture states.

The accepted path is frozen as:

```text
fresh exact 0023ZH fixture
→ guarded 18-stage / 41-case harness
→ configured AppRoot load and hydration
→ real Tutorial-0 persistence and fresh reload
→ NO_CHANGE
→ transactional failure rollback
→ malformed / unknown-version recovery and recovery failure
→ exact public snapshots and router contract
→ exact owned cleanup
→ protected-state verification
```

This is an execution/evidence gate only. No product, harness, runner, manifest, parser, validator, diagnostic, scene, or formal-data source repair is authorized.

## 2. Closed historical scopes

Do not execute, resume, edit, or reuse fixtures from:

```text
tasks/0023Y_app_root_test_profile_session_gate.md
tasks/0023Y_profile_test_app_root_session_orchestration_gate.md
tasks/0023Z_app_root_test_profile_session_retry_after_parser_gate.md
tasks/0023ZA_app_root_test_profile_session_instrumented_diagnostic_retry.md
tasks/0023ZB_app_root_test_profile_session_evidence_complete_rebuild.md
tasks/0023ZE_app_root_profile_session_full_matrix_execution_gate.md
tasks/0023ZF_tutorial_zero_persist_subcondition_diagnostic_gate.md
```

Do not reuse:

```text
task_0023ze_app_root_profile_session
task_0023zf_tutorial_persist_diagnostic
```

Task 0023ZG is accepted and frozen. Do not alter its repaired harness or future contract.

## 3. Startup gate

Before any Godot, PowerShell, fixture check, or documentation edit:

1. require absolute root `D:\Delayed_Self`, branch `main`, expected origin, and clean tracked/untracked worktree;
2. run all startup Git facts required by `AGENTS.md`;
3. fetch and fast-forward only;
4. require `HEAD == origin/main` and ancestry of `15e063e7cb45342e14d60149c0b03b34ddfb9533`;
5. require ancestry of harness implementation checkpoint `cfbcbf6c8c7990db232a2fa6fbb22d937e0b623a`;
6. read the complete 0023ZC, 0023ZD, 0023ZF, and 0023ZG GPT acceptances/adjudications, the 0023ZE blocker report, and the 0023ZG implementation report;
7. verify pinned Godot `4.7.stable.steam.5b4e0cb0f` or the exact documented equivalent;
8. require no residual Godot process.

Record the current Git blob or SHA-256 of every frozen product, accepted prerequisite, diagnostic, and five canonical harness artifact listed in Sections 6–7.

Stop on divergence, conflict, unexpected files, missing checkpoint, remote mismatch, or process residue.

## 4. Protected production and test-root boundary

Before and after every Godot or PowerShell group, record only production Profile non-content metadata:

```text
exists = true
Length = 473
LastWriteTimeUtc = 2026-07-19T08:15:02.2089459Z
```

Do not open, read, parse, hash, copy, rename, back up, delete, reset, repair, compare, or write production Profile content.

For `user://delayed_self_test_profiles/`, record only whether the root exists. Do not enumerate, list, inspect, clean, or modify unrelated fixtures.

For Task 0023ZH, only the exact fixture directory, canonical target, and these 24 known owned sibling paths may be checked or operated:

```text
.delayed_self_0023w_temp_0.json ... _7.json
.delayed_self_0023w_transaction_0.json ... _7.json
.delayed_self_0023w_recovery_0.json ... _7.json
```

Never enumerate the fixture directory. Exact path checks are allowed; directory listing is not.

Any production metadata change, production-content access, test-root enumeration, unrelated-fixture operation, or unexpected non-owned file is an immediate safety stop.

## 5. Exact accepted execution artifacts

Use only these accepted canonical artifacts as committed at `cfbcbf6c8c7990db232a2fa6fbb22d937e0b623a`:

```text
tests/app/task_0023zd_app_root_profile_session_full_matrix.gd
tests/app/task_0023zd_full_matrix_parser_smoke.gd
tests/app/task_0023zd_full_matrix_harness_validator.gd
tests/app/task_0023zd_full_matrix_manifest.json
tests/app/task_0023zd_capture_profile_session.ps1
```

The fixed contract is:

```text
future_task = 0023ZH
fixture_id = task_0023zh_app_root_profile_session
canonical_profile_path = user://delayed_self_test_profiles/task_0023zh_app_root_profile_session/delayed_self_profile.json
execution_guard = --task-0023zh-fixture-execution
```

Do not copy or rename these files. Do not create a replacement runner or behavior script.

## 6. Frozen product and prerequisite authorities

Do not modify:

```text
scripts/app/app_root.gd
scripts/app/profile_codec.gd
scripts/app/local_profile.gd
scripts/app/profile_progress_adapter.gd
scripts/app/memory_progress.gd
all existing 0023R–0023ZC tests
all 0023ZF diagnostic/parser/capture files
formal catalog/level data
Simulation/solver/schema/canonical state
Gameplay/Help/Timeline/Level Select/final acknowledgment
scenes/project.godot
```

The following accepted contracts remain frozen:

- canonical test-path isolation;
- Profile v1 codec;
- configured-instance filesystem store, recovery, rollback, reset, and cleanup;
- Profile↔MemoryProgress adapter and atomic runtime hydration;
- AppRoot pre-tree test configuration and configured boot ordering;
- prepare → persist → commit ordering;
- exact nested-runtime public progress shape;
- deterministic sorted unlocked-ID snapshots;
- revised sorted exact harness expectation;
- existing router and finale behavior.

No source correction is permitted after any failure.

## 7. Mandatory pre-execution regressions

Before fixture checks or runner Execute, run from fresh processes:

```text
0023R = 123 assertions / 4 positive / 39 negative
0023T = compile marker + 34 assertions
0023V = compile marker + 28 assertions
0023W = compile marker + 68 assertions
0023X = compile marker + 31 assertions
0023ZC = parser marker + compile contract 17 assertions
0023ZF = parser marker only
0023ZG parser = TASK_0023ZG_FULL_MATRIX_PARSER_SMOKE_PASS
0023ZG validator = TASK_0023ZG_VALIDATOR_ASSERTIONS=279 + TASK_0023ZG_FULL_MATRIX_HARNESS_VALIDATOR_PASS
```

Every process must:

- exit 0;
- have empty stderr;
- print each required marker exactly once;
- preserve production metadata and test-root existence;
- leave the 0023W fixture absent;
- leave no Godot process.

Do not run aggregate or `tests/run_all.gd`.

## 8. Mandatory no-guard proof

Before the first fixture check, run the exact full behavior script once **without** the 0023ZH guard in a separately captured process.

Require:

```text
Godot exit = 2
TASK_0023ZH_EXECUTION_GUARD_BLOCKED count = 1
TASK_0023ZH_BEHAVIOR_PROCESS_STARTED count = 0
TASK_0023ZH_STAGE_BEGIN count = 0
TASK_0023ZH_ASSERTIONS count = 0
TASK_0023ZH_APP_ROOT_PROFILE_SESSION_FULL_MATRIX_PASS count = 0
stderr empty
production metadata unchanged
test-root existence unchanged
worktree unchanged
Godot processes before/after = 0 / 0
```

The guard-proof command must not include `--task-0023zh-fixture-execution`.

Retain its independent command/stdout/stderr/exit/process/protected-state/worktree evidence.

## 9. Mandatory capture Qualification

Run the accepted PowerShell runner in `Qualify` mode once before Attempt 1 into:

```text
D:\Delayed_Self_Evidence\0023ZH_app_root_profile_session_full_matrix_execution\qualification_pre\
```

Require all 11 Qualification files, parser marker once, exit 0, empty stderr, `ExecutionGuardPassed=False`, unchanged production metadata/test-root existence, and zero Godot processes.

If Qualification fails, stop before fixture operation and create a docs-only evidence-incomplete blocker report.

## 10. Exact fixture freshness gate

After pre-Qualification, build the canonical virtual path only through the accepted path policy and require exact equality to the frozen canonical target.

Before **each** Execute attempt, check only:

```text
exact fixture directory absent
canonical target absent
all 24 known owned siblings absent
```

Do not enumerate the test root or fixture directory. If any exact owned path already exists, stop without opening or deleting it.

Attempt 2 may begin only after Attempt 1 itself proves exact cleanup and the runner's post-attempt exact state reports every owned path absent.

## 11. Attempt 1 execution

Use only the accepted runner:

```powershell
-Mode Execute
-AttemptNumber 1
-ScriptPath res://tests/app/task_0023zd_app_root_profile_session_full_matrix.gd
```

Use evidence root:

```text
D:\Delayed_Self_Evidence\0023ZH_app_root_profile_session_full_matrix_execution\attempts\
```

The runner must append the exact 0023ZH execution guard. Do not invoke the behavior script directly with the guard.

Immediately after the process and before any cleanup, edit, or report drafting, require all 17 attempt files:

```text
attempt_1_command.txt
attempt_1_stdout.txt
attempt_1_stderr.txt
attempt_1_exit_code.txt
attempt_1_process_before.txt
attempt_1_process_after.txt
attempt_1_production_metadata_before.txt
attempt_1_production_metadata_after.txt
attempt_1_test_root_exists_before.txt
attempt_1_test_root_exists_after.txt
attempt_1_fixture_exact_state_before.txt
attempt_1_fixture_exact_state_after.txt
attempt_1_worktree.patch
attempt_1_worktree_patch_sha256.txt
attempt_1_stage_summary.txt
attempt_1_classification.txt
attempt_1_inventory.txt
```

Missing evidence is `DIAGNOSTIC_CAPTURE_INCOMPLETE` and an immediate stop.

## 12. Attempt acceptance audit

A runner `PASS_CANDIDATE` becomes task-level `PASS_COMPLETE_MATRIX` only if an external acceptance audit proves all of the following:

```text
exit code = 0
stderr length = 0
runner classification = PASS_CANDIDATE
behavior-process-start marker count = 1
18 TASK_0023ZH_STAGE_BEGIN markers exactly once in manifest order
18 TASK_0023ZH_STAGE_PASS markers exactly once in manifest order
assert-fail marker count = 0
abort marker count = 0
TASK_0023ZH_ASSERTIONS=41 exactly once
TASK_0023ZH_APP_ROOT_PROFILE_SESSION_FULL_MATRIX_PASS exactly once
17/17 inventory complete
worktree patch empty and SHA-256 valid
production metadata unchanged
test-root existence unchanged
process before/after zero
fixture directory absent after process
canonical target absent after process
all 24 owned siblings absent after process
```

The acceptance audit must parse marker order from retained stdout/stage summary, not infer success from exit code alone.

If any condition fails, classify from evidence as exactly one:

```text
ASSERTION_FAILURE
CONTROLLED_ABORT
PROCESS_OR_RUNTIME_FAILURE
DIAGNOSTIC_CAPTURE_INCOMPLETE
PLAYER_DATA_SAFETY_FAULT
FROZEN_SOURCE_DRIFT
FIXTURE_RESIDUE
UNCLASSIFIED_NONPASS
```

Stop immediately. Do not repair or rerun.

## 13. Attempt 2 execution

Attempt 2 is authorized only if Attempt 1 is externally audited as `PASS_COMPLETE_MATRIX`, the worktree is unchanged, no source/document edit has occurred, production metadata/test-root existence remain unchanged, no Godot process remains, and the exact 0023ZH fixture plus all owned paths are absent.

Run the same accepted runner with:

```powershell
-Mode Execute
-AttemptNumber 2
-ScriptPath res://tests/app/task_0023zd_app_root_profile_session_full_matrix.gd
```

Apply the identical 17-file inventory and task-level acceptance audit.

Acceptance requires:

```text
Attempt 1 = PASS_COMPLETE_MATRIX
Attempt 2 = PASS_COMPLETE_MATRIX
```

There must be no intervening edit, commit, source regeneration, assertion adjustment, fixture manipulation, or harness change between the two attempts.

## 14. Failure evidence and emergency cleanup

On any nonpass:

1. preserve and audit the complete runner evidence before cleanup;
2. record the last passed stage, first failing stage, assertion/abort label if present, runner classification, process and protected-state facts;
3. retain the worktree patch and hash;
4. do not modify source, harness, validator, runner, manifest, parser, or assertions;
5. inspect only the exact fixture directory, target, and 24 owned siblings;
6. after evidence preservation, delete only existing exact owned files;
7. remove the exact fixture directory non-recursively only after every known owned path is absent;
8. do not enumerate the fixture directory or test root;
9. verify production metadata/test-root existence unchanged and zero Godot processes.

If an unexpected non-owned file prevents non-recursive fixture-directory removal, stop and report `PLAYER_DATA_SAFETY_FAULT`; do not enumerate or delete it.

Attempt 2 must not run after any Attempt 1 nonpass.

## 15. Post-execution Qualification and regressions

After two consecutive `PASS_COMPLETE_MATRIX` attempts, run a fresh accepted runner `Qualify` into:

```text
D:\Delayed_Self_Evidence\0023ZH_app_root_profile_session_full_matrix_execution\qualification_post\
```

Then rerun the complete regression matrix from Section 7.

Require:

- all pre/post regression outputs match their accepted counts and markers;
- revised validator remains 279 assertions;
- all frozen source and harness blobs/hashes match their pre-execution values;
- no unauthorized changed path;
- 0023W, 0023ZE, 0023ZF, and 0023ZH fixtures absent;
- production metadata/test-root existence unchanged;
- no Godot process;
- no `project.godot`, `.gd.uid`, or `.godot/` pollution.

Do not run aggregate or export.

## 16. Repository outputs

### Passing output

Create:

```text
docs/reports/0023ZH_APP_ROOT_PROFILE_SESSION_FULL_MATRIX_EXECUTION_REPORT.md
```

The report must include:

- both attempt classifications and assertion counts;
- exact ordered stage audit for each attempt;
- complete evidence inventories;
- guard proof and pre/post Qualifications;
- pre/post regressions;
- exact cleanup and protected-state facts;
- frozen hash/diff audit;
- explicit non-claims.

Commit only authorized report/current documentation with message:

```text
docs: record Delayed Self repaired full-matrix execution
```

### Blocked output

Create only:

```text
docs/reports/0023ZH_APP_ROOT_PROFILE_SESSION_FULL_MATRIX_BLOCKER_REPORT.md
```

Commit message:

```text
docs: classify Delayed Self repaired full-matrix blocker
```

No failed source, harness, runner, parser, validator, manifest, fixture, or external evidence may be committed.

## 17. Authorized repository paths

```text
docs/reports/0023ZH_APP_ROOT_PROFILE_SESSION_FULL_MATRIX_EXECUTION_REPORT.md
docs/reports/0023ZH_APP_ROOT_PROFILE_SESSION_FULL_MATRIX_BLOCKER_REPORT.md
docs/CODEX_ACTIVE_TASK_CURRENT.md
docs/CODEX_STARTUP_SNAPSHOT_CURRENT.md
docs/DEVELOPMENT_PLAN_CURRENT.md
docs/DOC_INDEX_CURRENT.md
docs/HANDOFF_MASTER_CURRENT.md
docs/project_memory.md
docs/production/COMPLETE_FLOW_PRODUCTION_CLOSURE_PLAN_CURRENT.md
```

Do not modify any other repository path.

## 18. Evidence root

Use only:

```text
D:\Delayed_Self_Evidence\0023ZH_app_root_profile_session_full_matrix_execution\
```

Retain startup facts, frozen hashes, guard proof, pre/post Qualifications, both 17-file attempts, external acceptance audits, exact cleanup audits, regressions, protected-state snapshots, command audit, and final Git/process state.

## 19. Git closeout

Stage authorized documentation paths explicitly. Run `git diff --cached --check`. Fetch and stop on remote drift. Push normally.

Finish with:

```text
clean tracked/untracked worktree
HEAD == origin/main
divergence 0/0
no Godot process
production metadata unchanged
test-root existence unchanged
all owned fixtures absent
all frozen source/harness paths unchanged
```

## 20. Terminal verdict and non-claims

Return exactly one:

```text
APP_ROOT_PROFILE_SESSION_FULL_MATRIX_EXECUTION_READY_FOR_GPT_REVIEW
BLOCKED_APP_ROOT_PROFILE_SESSION_EXECUTION_CLASSIFIED
BLOCKED_APP_ROOT_PROFILE_SESSION_EXECUTION_EVIDENCE_INCOMPLETE
```

A passing verdict establishes only that the frozen repaired test-only AppRoot/Profile matrix passed twice with complete evidence. It does not activate production Profile persistence, enable default product persistence, register aggregate coverage, implement menus/settings/localization, prove build/export/RC readiness, or authorize release, upload, or festival submission.
