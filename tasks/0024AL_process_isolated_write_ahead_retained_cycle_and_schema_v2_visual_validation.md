# Task 0024AL — Build a process-isolated write-ahead retained cycle and complete schema-v2 visual validation

- Status: `READY`
- Gate: `PROCESS-ISOLATED CASE WORKER / WRITE-AHEAD DURABILITY / FULL REHEARSAL / FRESH FIVE-CASE RETENTION / MULTI-SUBAGENT REQUIRED`
- Repository/root/branch: `zcx369658780/delayed-self-godot` / `D:\Delayed_Self` / `main`
- Required accepted checkpoint: `0388be4b7b97ff45d960eab32f3ab88879f78bdb`
- Historical blocked Task 0024AK result: `e6b57d4ed2f703287abab88809a551c5da50114d`
- Historical consumed AK manifest: `2de60fc6-9065-4ecf-a5d9-a09b739f1461`
- Latest GPT verdict: `TASK_0024AK_HONEST_MODULE_SESSION_RELOAD_DURABILITY_BLOCKER_ACCEPTED_FRESH_PROCESS_ISOLATED_GATE_REQUIRED`
- Frozen qualified focused validator: `res://tests/presentation/task_0024aj_sensor_contract_and_dynamic_signature_validation.gd`
- Accepted wrapper blob: `5f14eb0feff903b40311c1c7283b20e2eabbffc2`
- Production baseline: exactly eight cataloged levels; sequence 8 remains the sole formal finale
- Accepted uncatalogued candidates: `crate_shadow_timing`, `key_route_commitment`, `identity_shift_bridge`, `lasting_echo_latch`
- Recommended main-agent model: `GPT-5.6 Sol Max`; constrained fallback: `GPT-5.6 Terra Max`

## 1. Objective

Under a new task identity:

1. preserve every Task 0024AF–0024AK artifact byte-for-byte;
2. preserve the frozen AJ focused validator and its already-qualified visual/sensor contract;
3. eliminate shared PowerShell module-table lifetime from the parent retained orchestrator;
4. execute every case in a fresh PowerShell process;
5. require each case worker to atomically persist a complete case record before reporting success;
6. independently validate every persisted case record in a Python parent process;
7. atomically journal retained progress after every completed case;
8. prove completed case evidence survives parent-process failure;
9. pass two focused qualifications plus one complete five-case non-retained rehearsal through the exact final architecture;
10. execute one fresh retained five-case Task 0024AL cycle;
11. determine whether the minimum schema-v2 presentation is machine-ready for owner review.

This task does not conduct owner subjective playtesting and does not authorize production catalog, progression, finale or Profile changes.

## 2. Historical immutability

All committed artifacts with these identities are historical and immutable:

```text
task_0024af_*
task_0024ag_*
task_0024ah_*
task_0024ai_*
task_0024aj_*
task_0024ak_*
```

This includes all validators, adapters, modules, drivers, manifests, sidecars, blocker records, qualification records, audits and reports.

Do not edit, delete, repair, rename, relabel, reconstruct or rerun either historical retained identity:

```text
Task 0024AJ manifest d481c57c-1999-42e8-b000-84068b1c103d
Task 0024AK manifest 2de60fc6-9065-4ecf-a5d9-a09b739f1461
```

The frozen AJ focused validator may be executed under the new 0024AL worker/orchestrator, but its file must remain byte-identical.

All new tools, evidence, reports and retained artifacts must use the `task_0024al_*` identity.

## 3. Mandatory multi-subagent execution

Read and route through:

```text
D:\Delayed_Self\.codex\config.toml
```

`.codex/config.toml` is the sole permitted pre-existing untracked path. Record its exact path and SHA-256 without exposing contents. Do not modify, stage, commit, delete or copy it. Require byte identity at closeout.

Use at least three distinct roles:

1. **process/durability contract mapper** — read-only analysis of the AK module-session failure, PowerShell process boundaries, accepted wrapper/route runner contracts and historical boundaries;
2. **worker/orchestrator implementation worker** — create only new AL case-worker, parser, probes, Python orchestrator and evidence artifacts;
3. **independent reviewer/test auditor** — verify process isolation, atomic durability, crash recovery, manifest consumption, exact five-case behavior and changed-path boundaries.

A documentation/evidence writer may be a fourth role. Only the main agent may launch Godot, run the full rehearsal, declare or execute a retained manifest, stage, commit or push. No subagent may run Godot, perform Git writes or access Profile/user data. Parallelism is allowed only for disjoint read-only analysis; one writer per file.

If multi-subagent routing or local configuration parsing fails, return an honest blocker. Do not silently fall back to single-agent execution.

## 4. Continuous-execution doctrine

Use this state machine:

```text
AUTHORING
→ INDEPENDENT_REVIEW
→ PROCESS_AND_PARSER_PROBES
→ FOCUSED_QUALIFICATION_1
→ FOCUSED_QUALIFICATION_2
→ FULL_FIVE_CASE_REHEARSAL
→ STATIC_AND_PRE_FINAL_AUDIT
→ RETAINED_MANIFEST_DECLARED
→ RETAINED_CYCLE
→ POST_FINAL_AUDIT
→ COMMIT_AND_PUSH
```

Before `RETAINED_MANIFEST_DECLARED`, defects in new `task_0024al_*` tools, workers, parsers, probes, rehearsal drivers, evidence capture or documentation are repairable authoring failures. They block progression but do not terminate the task. Continue repairing, independently reviewing and requalifying.

Do not return a terminal response while an authorized subagent is still running or while a repairable AL-only defect remains unresolved.

A terminal blocker before retention is permitted only for:

- protected/historical artifact drift;
- remote drift that cannot be safely fast-forwarded;
- unexpected tracked/staged/untracked paths;
- inability to preserve `.codex/config.toml` identity;
- unowned/unproven Godot process residue;
- requirement to modify a frozen or unauthorized surface;
- multi-subagent capability failure.

After the AL retained manifest is declared, any retained failure is terminal for Task 0024AL and must be reported honestly without repair or rerun.

## 5. Startup gate

Before editing or launching Godot:

1. require root `D:\Delayed_Self`, branch `main`, expected origin and no Git operation in progress;
2. fetch and fast-forward only;
3. require `HEAD == origin/main`, divergence `0/0`, ancestry of `0388be4b7b97ff45d960eab32f3ab88879f78bdb`, and no tracked/staged changes;
4. permit only untracked `.codex/config.toml`, record its SHA-256 and require it unchanged;
5. require zero pre-existing Godot processes;
6. require Godot `4.7.1.stable.steam.a13da4feb`, Python 3 and PowerShell 7;
7. require exact accepted wrapper blob `5f14eb0feff903b40311c1c7283b20e2eabbffc2`;
8. read `AGENTS.md`, current pointer docs, this task, AK task/report/GPT acceptance, AJ task/report/GPT acceptance, AF–AI blocker reports/GPT acceptances, AJ qualifications/audits, AK qualifications/audits/manifest/blocker, Task 0024AE acceptance/report, Task 0024AD matrix and the headless lifecycle policy;
9. record exact hashes for all Task 0024AF–AK artifacts, the frozen AJ validator, presentation helper/Gameplay file, all twelve levels, production catalog/route/Profile files, staging route, mechanics foundation and wrapper;
10. stop on historical drift, process ambiguity, additional untracked paths or any need to inspect production Profile/user data.

The accepted Task 0024AE route case may use its exact isolated fixture only through its existing accepted runner and cleanup contract. The frozen focused AJ validator accesses no Profile surface.

## 6. Authorized repository paths

Create or modify only:

```text
tests/tools/task_0024al_case_worker.ps1
tests/tools/task_0024al_worker_parser.psm1
tests/tools/task_0024al_orchestrator.py
tests/tools/task_0024al_process_isolation_probes.py
tests/tools/task_0024al_worker_parser_probes.ps1
tests/tools/task_0024al_orchestrator_probes.py
tests/tools/task_0024al_static_preservation.py
tests/tools/task_0024al_*.json
docs/reports/0024AL_PROCESS_ISOLATED_WRITE_AHEAD_RETAINED_CYCLE_AND_SCHEMA_V2_VISUAL_VALIDATION_REPORT.md
docs/presentation/SCHEMA_V2_MINIMUM_OWNER_REVIEW_VISUAL_CONTRACT_CURRENT.md
docs/playtest/SUBMISSION_CORE_1_12_OWNER_CURRICULUM_REVIEW_CURRENT.md
docs/CODEX_ACTIVE_TASK_CURRENT.md
docs/CODEX_STARTUP_SNAPSHOT_CURRENT.md
docs/DEVELOPMENT_PLAN_CURRENT.md
docs/DOC_INDEX_CURRENT.md
docs/HANDOFF_MASTER_CURRENT.md
docs/project_memory.md
docs/production/EXPANDED_CONTENT_SUBMISSION_ROADMAP_CURRENT.md
```

No other path may change. Do not create a replacement presentation validator. Execute the frozen AJ validator unchanged.

## 7. Frozen and protected surfaces

Do not modify:

```text
scenes/**
project.godot
data/catalog/**
data/levels/**
scripts/**
schemas/**
tests/run_all.gd
tests/simulation/**
tests/levels/**
tests/curriculum/**
tests/playtest/**
tests/presentation/**
tests/tools/run_owned_godot_headless.ps1
all Task 0024T–0024AK historical evidence
localization/**
assets/**
audio/**
build/export/release/submission surfaces
```

Do not change input, collision, transitions, restart, history, completion, routes, catalog, finale, presentation implementation or Profile behavior.

## 8. Required process-isolated architecture

### 8.1 Python parent orchestrator

Create:

```text
tests/tools/task_0024al_orchestrator.py
```

The parent orchestrator must:

- use Python standard library only;
- never import a PowerShell module;
- never invoke a PowerShell function by name;
- never use PowerShell dot-source or `-Command` for case execution;
- invoke every case worker as a fresh process using the equivalent of:

```text
pwsh -NoProfile -NonInteractive -File tests/tools/task_0024al_case_worker.ps1 ...
```

- validate manifest schema, task identity, exact case order, canonical paths, unique manifest ID and exact expected markers;
- support `qualification` and `retained` modes with distinct output roots and identities;
- in retained mode atomically create a consumption sidecar before the first case and reject any pre-existing sidecar;
- require Godot count zero before and after every worker process and at final closeout;
- stop on the first failed worker/case and never invoke later cases;
- independently parse and validate each durable case JSON using Python `json` and `hashlib`;
- never depend on worker stdout as the sole case evidence;
- atomically write a progress journal after every accepted case;
- atomically write the final result only after all five case records validate;
- emit exactly one compact JSON object on success.

The Python parent must not duplicate Gameplay or Simulation logic. It validates evidence and sequencing only.

### 8.2 Fresh PowerShell case worker

Create:

```text
tests/tools/task_0024al_case_worker.ps1
```

Every invocation runs in a new PowerShell process. The worker must:

- have uniquely prefixed parameters such as `AlWorkerGodotExecutable`, `AlWorkerProjectRoot`, `AlWorkerCaseName`, `AlWorkerCaseKind`, `AlWorkerScriptPath`, `AlWorkerRequiredMarker`, `AlWorkerStderrPolicy`, `AlWorkerManifestId`, `AlWorkerInvocationId`, `AlWorkerOrdinal`, `AlWorkerOutputPath`;
- import only `task_0024al_worker_parser.psm1` inside its own fresh process;
- invoke the accepted wrapper unchanged for ordinary wrapper cases;
- invoke the exact accepted Task 0024AE route runner for the isolated route case, without modifying that runner or fixture;
- collect and parse the complete wrapper/route result;
- enforce the exact case contract before writing success;
- for the frozen AJ focused case, parse and retain the exact thirteen unique bounded AJ evidence records;
- build one unified case-record schema;
- write the case record to a temporary file in the same directory;
- flush the temporary file to disk, close it and atomically rename/replace it to the canonical output path;
- only after the canonical output exists and re-parses successfully may the worker emit a compact success record and exit `0`;
- on failure, exit nonzero and never create a false PASS file.

A successful worker process must leave a durable canonical case file even if the parent orchestrator crashes immediately after worker exit.

### 8.3 Worker parser module

Create:

```text
tests/tools/task_0024al_worker_parser.psm1
```

It must:

- have no top-level `param()`;
- export only uniquely prefixed functions;
- export no variables;
- perform no process launch, Git access or file write;
- parse wrapper values, route-runner output and AJ evidence lines fail-closed;
- use no generic global variable names;
- be imported only inside the fresh case-worker process.

The parent Python orchestrator must never import or rely on this module.

### 8.4 Unified durable case-record schema

Every successful case file must include at least:

```text
schema_version
task_identity = 0024AL
run_mode
manifest_id or qualification_run_id
invocation_id
ordinal
case_name
case_kind
script_path
required_marker
required_marker_occurrence_count
stderr_policy
raw_wrapper_or_runner_sha256
raw_utf8_byte_count
parsed_status
underlying_status
native_exit_code
marker_pass
stderr
cleanup_invocation_count
cleanup_order
remaining_owned_pids
remaining_unproven_pids
worker_godot_count_before
worker_godot_count_after
canonical_case_record_sha256
created_utc
```

The focused first case must additionally retain the complete parsed AJ evidence, including exactly thirteen unique bounded records and the exact thirteen-entry lifecycle-signature map.

The route case must retain exact isolated Profile path/cleanup facts returned by the accepted route runner without inspecting other Profile/user data.

## 9. Write-ahead durability and crash-recovery probes

Before any real Godot qualification, create deterministic probes proving:

1. the Python parent source contains no `Import-Module`, dot-source or direct PowerShell function invocation;
2. every real worker command uses `pwsh -NoProfile -NonInteractive -File`;
3. the worker parser module is imported only inside the worker process;
4. parent sentinel environment values remain unchanged after a child worker imports/reloads its module multiple times;
5. malformed worker JSON, missing output file, duplicate output, wrong task identity, wrong ordinal, wrong marker, wrong hash and noncanonical output path are rejected;
6. the worker refuses to overwrite an existing canonical case record;
7. a synthetic successful worker atomically creates a case file before exit;
8. an injected parent failure immediately after synthetic worker success leaves the canonical case file byte-identical and parseable;
9. the progress journal records exactly the completed case count and survives injected parent failure;
10. resume/reuse of a consumed retained manifest is rejected; no retained resume is authorized;
11. qualification mode never creates a retained consumption sidecar;
12. retained mode creates the sidecar exactly once before the first case.

The crash-recovery probe must not run Godot and must use synthetic fixtures only.

## 10. Worker/parser qualification

Create negative probes proving rejection of:

- missing/duplicate/extra/non-string wrapper records;
- invalid/truncated/array-root/null-field/wrong-version/wrong-type wrapper JSON;
- marker zero, marker twice and regex-lookalike-only output;
- missing/duplicate/invalid/over-bound AJ evidence records;
- malformed route-runner output;
- null cleanup/PID/Godot fields;
- nonempty stderr where empty is required;
- aggregate stderr other than exact `LEVEL_ID_MISMATCH`;
- wrong case identity, ordinal, manifest/run ID or canonical output path.

All probes must run under the actual Python 3 and PowerShell 7 environment.

## 11. Repeatable focused qualification

Before full rehearsal, execute the frozen AJ focused validator twice through the exact AL case worker and Python parent qualification path:

```text
res://tests/presentation/task_0024aj_sensor_contract_and_dynamic_signature_validation.gd
```

Required marker:

```text
TASK_0024AJ_SENSOR_CONTRACT_DYNAMIC_SIGNATURE_PASS
```

Each qualification must independently retain:

- wrapper schema version 2;
- `PASS/PASS`;
- native exit 0;
- literal marker occurrence exactly one;
- empty stderr;
- cleanup exactly once;
- empty remaining owned/unproven PID arrays;
- worker and parent Godot counts `0/0`;
- a durable canonical case file written before worker exit;
- complete AJ evidence parse with exactly thirteen unique bounded records;
- exact thirteen nonempty, pairwise-distinct lifecycle signatures;
- four candidate Gameplay preflights;
- swapped-identity wrong-actor control;
- action 10 open/unoccupied, actions 11/12 per-cell deferred and action 13 vacancy-close.

Preserve failed AL authoring attempts honestly. Failures before retained declaration remain repairable within authorized AL tools.

## 12. Full five-case non-retained rehearsal

Before PRE_FINAL audit and retained manifest declaration, execute one complete non-retained rehearsal through the exact same Python orchestrator and case worker that will be used for retention.

Use a distinct `qualification_run_id`. Do not create a retained sidecar or retained manifest. Execute exactly once each, in this order:

1. frozen AJ focused validator;
2. accepted Task 0024AE isolated staging route qualification;
3. accepted Task 0024AD 1–12 batch validator;
4. Aggregate;
5. corrected complete-v2.

The rehearsal must produce five durable case files, a five-case progress journal and a complete qualification result. All five must pass the same case contracts required for retention.

This rehearsal is repairable authoring qualification. If it fails, preserve the failed evidence, repair only authorized AL tools, independently review and run a new qualification rehearsal with a new qualification ID. Do not declare a retained manifest until one complete rehearsal passes.

## 13. Static and frozen audits

Create machine-readable PRE_FINAL and POST_FINAL audits proving:

1. all Task 0024AF–AK artifacts are byte-identical;
2. AJ and AK manifests/sidecars/blockers remain exact and consumed;
3. frozen AJ validator is byte-identical;
4. wrapper blob is exact;
5. no protected product/test surface changed;
6. changed paths are within the exact AL allowlist;
7. `.codex/config.toml` remains untracked, unstaged and byte-identical;
8. Python parent contains no PowerShell module import or dot-source architecture;
9. every case uses a fresh `pwsh -File` worker process;
10. worker output is atomically durable before success;
11. the complete five-case rehearsal passed through the exact final architecture;
12. production catalog remains exactly eight levels and sequence 8 remains the formal finale;
13. all twelve accepted `(L*, N*)` values remain exact in the accepted Task 0024AD matrix.

PRE_FINAL must pass with zero mismatch before retained manifest declaration.

## 14. Fresh retained Task 0024AL cycle

Only after all process/durability probes, worker/parser probes, two focused qualifications, one complete five-case rehearsal, independent review and PRE_FINAL audit pass may the main agent create one canonical AL manifest and atomically declare it.

Execute exactly once each, in this order:

1. frozen AJ focused sensor-contract/dynamic-signature validator;
   - marker `TASK_0024AJ_SENSOR_CONTRACT_DYNAMIC_SIGNATURE_PASS`;
   - stderr empty;
2. accepted Task 0024AE isolated staging route qualification;
   - marker `TASK_0024AE_ISOLATED_STAGING_OWNER_ROUTE_PASS`;
   - stderr empty;
3. accepted Task 0024AD 1–12 batch validator;
   - marker `TASK_0024AD_SUBMISSION_CORE_1_12_BATCH_PASS`;
   - stderr empty;
4. Aggregate;
   - exact marker `TASK_0003_TESTS_PASS assertions=621 vectors=9`;
   - stderr exactly `LEVEL_ID_MISMATCH`;
5. corrected complete-v2;
   - marker `TASK_0024V_V2_COMPLETE_PASS`;
   - stderr empty.

For every case:

- launch a fresh PowerShell worker process;
- require a durable canonical case file before accepting worker success;
- independently validate the file in Python;
- atomically update the progress journal before moving to the next case;
- stop immediately on failure and never invoke later cases;
- preserve all completed case files and journal entries even on terminal failure.

Any failure after retained manifest declaration is terminal for AL. Do not repair or rerun. Final Godot count must be zero.

## 15. Report and status update

Create:

```text
docs/reports/0024AL_PROCESS_ISOLATED_WRITE_AHEAD_RETAINED_CYCLE_AND_SCHEMA_V2_VISUAL_VALIDATION_REPORT.md
```

The report must include:

- exact AK blocker root cause and historical preservation;
- Python-parent/fresh-worker architecture;
- module-session isolation evidence;
- atomic case-file and progress-journal durability evidence;
- crash-after-worker-success probe;
- worker/parser negative probes;
- two focused qualifications;
- complete five-case rehearsal;
- retained-cycle records and hashes;
- PRE_FINAL/POST_FINAL audits;
- multi-subagent routing ledger;
- `.codex/config.toml` identity;
- explicit owner/final-art/catalog boundaries.

On pass update the owner worksheet only to:

```text
owner_route_status = MACHINE_QUALIFIED_ISOLATED_STAGING_ROUTE
owner_playthrough_status = NOT_YET_REVIEWED
presentation_readiness = MACHINE_QUALIFIED_SCHEMA_V2_MINIMUM_OWNER_REVIEW_REQUIRED
visual_art_direction_status = NOT_FINAL
catalog_admission_status = NOT_AUTHORIZED
```

Do not populate subjective answers.

## 16. Commit and push

On pass or honest blocker:

1. stage only authorized AL paths;
2. never stage `.codex/config.toml`;
3. inspect staged names and run `git diff --cached --check`;
4. create one single-topic result commit without amend/rebase/squash;
5. fetch before push and stop on remote drift;
6. push only a clean fast-forward to `origin/main`;
7. require `HEAD == origin/main`, divergence `0/0`, no tracked/staged changes, only unchanged `.codex/config.toml` untracked and Godot zero;
8. report any separate correction commit transparently.

Suggested pass commit:

```text
test: persist process-isolated retained evidence and validate schema v2 presentation
```

## 17. Terminal verdict

Return exactly one:

```text
SCHEMA_V2_MINIMUM_OWNER_REVIEW_PRESENTATION_WRITE_AHEAD_READY_FOR_GPT_REVIEW
BLOCKED_SCHEMA_V2_PROCESS_ISOLATED_RETAINED_DURABILITY
PROTECTED_DATA_BOUNDARY_BREACH
```

A pass authorizes only GPT review of the machine-qualified minimum schema-v2 presentation. It does not authorize owner subjective acceptance, final visual-art acceptance, production catalog/progression/finale/Profile changes, localization, assets/audio, build/export, release, upload or submission.
