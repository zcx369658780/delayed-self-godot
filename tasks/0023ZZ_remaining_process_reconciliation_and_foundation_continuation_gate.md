# Task 0023ZZ — Reconcile the remaining Godot process residue and continue the core-mechanics foundation

- Status: `READY`
- Gate: `OWNER-AUTHORIZED CONTINUATION / PRESERVED INPUT VERIFICATION + ONE REMAINING-PROCESS RECONCILIATION + UID/REGRESSION/FOUNDATION CONTINUATION`
- Repository/root/branch: `zcx369658780/delayed-self-godot` / `D:\Delayed_Self` / `main`
- Required GPT adjudication checkpoint: `a0a04581bccbb3f043817d7d146262300e827bb4`
- Closed predecessors: Tasks `0023ZX` and `0023ZY`
- Existing implemented baseline: eight formal levels, unchanged
- Production Profile authority: none
- Catalog/formal-level/final-flag authority: none
- Presentation/asset/font/audio/localization/build authority: none

## 1. Owner authorization and objective

The owner authorizes this new bounded continuation to:

1. verify the current local set of 77 preserved Task 0023ZX continuation inputs against the already verified Task 0023ZY external preservation record;
2. inspect the remaining Godot process set once using only PID, executable identity/path and command line;
3. close only exact processes whose identity and command line independently prove they are Delayed Self Task 0023ZX headless-test processes;
4. leave every ambiguous, unrelated, editor, project-manager or possibly manual process untouched and stop;
5. if and only if the final Godot process count is zero, continue the reviewed `.uid` baseline, stale Task 0012 correction and preserved schema-v2/Simulation/solver foundation work;
6. run exactly one complete final Task 0023ZX validation after implementation is ready.

No process-name-wide action, wildcard action, second process-reconciliation phase, broad reset/clean/stash, second complete validation, formal-level change, Profile operation or protected-surface expansion is authorized.

## 2. Verify the existing preservation record

Before fetch, repository reconciliation, edits or tests:

1. require root `D:\Delayed_Self`, branch `main`, expected origin and no Git operation in progress;
2. inventory all staged, modified, deleted and untracked paths;
3. require zero staged paths and exactly the authorized continuation-input set retained from Task 0023ZX/0023ZY, with no unrelated dirty path;
4. read the existing Task 0023ZY external preservation inventory without overwriting or deleting it;
5. compare every one of the 77 current local inputs against the preserved path, tracked state, SHA-256, byte length and line count;
6. stop if any path is missing, additional, changed or cannot be matched exactly;
7. do not recopy over the existing verified preservation set.

Broad `git reset`, `git clean` and unreviewed stash operations remain forbidden. Fetch and fast-forward only after exact preservation verification.

## 3. One remaining-process reconciliation phase

After Section 2 passes, inspect the complete current Godot process set once.

A process is eligible for owner-authorized closure only when executable identity/path and command line together prove all of:

- headless Godot execution;
- the exact Delayed Self repository root;
- a Task 0023ZX or directly required zero-I/O regression/test script;
- no editor, project-manager, interactive main-scene, export/build or other-project indicator.

Classify the complete set before any action. If any process is ambiguous or unrelated, stop without closing any process and without beginning UID/test/foundation work.

If every residual process is clearly owned, revalidate each exact identity immediately before closing only those exact PIDs in one bounded phase. No wildcard or process-name-wide action is allowed. Require the final Godot process count to become zero. If it does not, stop; no second process-reconciliation phase is authorized.

If the initial count is already zero, record that fact and continue.

## 4. UID baseline and historical regression

Only after Sections 2–3 pass and zero Godot processes are proven:

- continue the exact per-file `.uid` review contract from Task 0023ZY;
- adopt only valid unique source-paired `.uid` sidecars;
- remove only individually proven invalid/orphan/duplicate sidecars after verified external preservation;
- do not use wildcard or directory-level deletion;
- create/update `docs/engineering/GODOT_UID_SIDECAR_BASELINE_CURRENT.md`;
- modify only the catalog/progression/final-level expectations in `tests/levels/task_0012_staggered_doors_validation.gd` to the accepted eight-level order;
- retain all Level 6 solver, replay, door-window, necessity, Help, presentation and restart assertions unchanged.

Accepted catalog facts remain:

```text
1 tutorial_reach_exit
2 tutorial_echo_bridge
3 vertical_slice_delay_3
4 door_one_turn_late
5 two_keys_one_door
6 staggered_doors (non-final)
7 echo_spacing_bridge
8 two_echo_convergence (sole final)
```

## 5. Continue the core-mechanics foundation

After all prior gates pass, continue from the preserved Task 0023ZX drafts within the original pure rules-layer scope:

- backward-compatible schema v2;
- schema v1 and all eight formal levels behavior-identical;
- YOU-only crates;
- YOU-only consumable keys/locks;
- grouped barriers/bridges;
- typed sensors;
- one-shot room-local latches;
- one shared Simulation transition for replay and BFS;
- deterministic canonical state key;
- exact restart behavior;
- bounded non-catalog fixtures and solver budgets.

Focused development tests may run only in fresh headless processes and must finish with zero Godot processes. They may not access ordinary user data, production Profile, test-profile root, formal catalog/levels, main scene, export or build.

## 6. Exactly one complete final validation

When implementation is ready, run the complete Task 0023ZX validation matrix exactly once.

It must prove:

1. every schema-v1 reference vector remains exact;
2. all eight formal levels retain validation, restart, solver and replay facts;
3. catalog and all eight formal-level SHA-256 values remain unchanged;
4. all Task 0023ZX mechanic, canonical-key, replay, solver and malformed-input tests pass;
5. the corrected Task 0012 regression passes against the accepted eight-level baseline;
6. every adopted `.uid` has a valid unique eligible source pair;
7. only authorized paths changed;
8. zero Godot processes remain;
9. Git and remote-drift audits pass.

If the complete validation exposes any new substantive rules, solver, replay, state-space, formal-level regression, UID-policy or protected-boundary failure, stop immediately. Do not repair and rerun the complete validation in this task.

## 7. Authorized repository paths

All original Task 0023ZX paths remain authorized:

```text
docs/game_design/GAMEPLAY_SIMULATION_CONTRACT_V2_CURRENT.md
docs/level_design/LEVEL_SCHEMA_V2_CURRENT.md
docs/level_design/reference/turn_transition_vectors_v2.json
schemas/level_v2.schema.json
scripts/simulation/level_loader.gd
scripts/simulation/simulation.gd
scripts/solver/bfs_solver.gd
tests/simulation/task_0023zx_core_mechanics_*.gd
tests/simulation/fixtures/task_0023zx_*.json
```

Additional authorized paths:

```text
tests/levels/task_0012_staggered_doors_validation.gd
docs/engineering/GODOT_UID_SIDECAR_BASELINE_CURRENT.md
docs/reports/0023ZZ_REMAINING_PROCESS_RECONCILIATION_AND_FOUNDATION_CONTINUATION_REPORT.md
docs/CODEX_ACTIVE_TASK_CURRENT.md
docs/CODEX_STARTUP_SNAPSHOT_CURRENT.md
docs/DEVELOPMENT_PLAN_CURRENT.md
docs/DOC_INDEX_CURRENT.md
docs/HANDOFF_MASTER_CURRENT.md
docs/project_memory.md
docs/production/EXPANDED_CONTENT_SUBMISSION_ROADMAP_CURRENT.md
```

Also authorized are only exact reviewed valid `.uid` sidecars paired with eligible repository `.gd`/`.gdshader` sources.

Freeze without exception:

```text
data/catalog/**
data/levels/**
scenes/**
scripts/gameplay/**
scripts/app/**
project.godot
schemas/level_v1.schema.json
docs/level_design/reference/turn_transition_vectors_v1.json
tests/run_all.gd
assets/**
```

## 8. Repository output

On complete pass, commit only authorized paths with:

```text
feat: complete Delayed Self core mechanics foundation after process reconciliation
```

Push normally only after changed-path, frozen-hash, UID-pair, diff-check, process and remote-drift audits.

The report must include bounded preservation-verification facts, process classification/remediation counts, UID reviewed/adopted/rejected counts, exact Task 0012 correction scope, final validation results, frozen hashes and non-claims.

## 9. Terminal verdict

Return exactly one:

```text
CORE_MECHANICS_FOUNDATION_CONTINUATION_READY_FOR_GPT_REVIEW
BLOCKED_REMAINING_PROCESS_RECONCILIATION_OR_FOUNDATION_CONTINUATION
PROTECTED_DATA_BOUNDARY_BREACH
```

A pass establishes only the schema/Simulation/solver foundation, reviewed UID baseline and corrected historical regression. It does not authorize formal rooms 9–12, catalog/finale/Profile migration, Gameplay/UI, presentation, localization, asset/font/audio import, build/export, release, upload or submission.
