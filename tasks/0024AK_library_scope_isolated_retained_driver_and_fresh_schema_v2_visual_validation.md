# Task 0024AK — Build a Library-scope-isolated retained driver and complete fresh schema-v2 visual validation

- Status: `READY`
- Gate: `RETAINED DRIVER SCOPE ISOLATION / FRESH FIVE-CASE VALIDATION / MULTI-SUBAGENT REQUIRED`
- Repository/root/branch: `zcx369658780/delayed-self-godot` / `D:\Delayed_Self` / `main`
- Required accepted checkpoint: `75b6cfa6f918c2d4ff52ebbe2c0ec014fec6f3c5`
- Historical blocked Task 0024AJ result: `d92e315e5667d38fec59b508dd6eb16d6c0ed816`
- Historical consumed AJ manifest: `d481c57c-1999-42e8-b000-84068b1c103d`
- Latest GPT verdict: `TASK_0024AJ_HONEST_LIBRARY_SCOPE_RETAINED_CYCLE_BLOCKER_ACCEPTED_FRESH_DRIVER_GATE_REQUIRED`
- Frozen qualified focused validator: `res://tests/presentation/task_0024aj_sensor_contract_and_dynamic_signature_validation.gd`
- Accepted wrapper blob: `5f14eb0feff903b40311c1c7283b20e2eabbffc2`
- Production baseline: exactly eight cataloged levels; sequence 8 remains the sole formal finale
- Accepted uncatalogued candidates: `crate_shadow_timing`, `key_route_commitment`, `identity_shift_bridge`, `lasting_echo_latch`
- Recommended main-agent model: `GPT-5.6 Sol Max`; constrained fallback `GPT-5.6 Terra Max`

## 1. Objective

Under a new task identity:

1. preserve every Task 0024AF–0024AJ artifact byte-for-byte;
2. preserve the frozen AJ focused validator and its qualified sensor/lifecycle contract;
3. replace the failed dot-sourced Library architecture with a scope-isolated adapter/module/final-driver design;
4. prove caller parameter variables cannot be overwritten by imports or helper loading;
5. qualify the new adapter and driver before retention;
6. execute the frozen AJ focused validator twice through the new adapter as repeatable qualification;
7. execute one fresh retained five-case Task 0024AK cycle;
8. determine whether the minimum schema-v2 presentation is machine-ready for owner review.

This task does not conduct owner subjective playtesting and does not authorize production catalog, progression, finale or Profile changes.

## 2. Historical immutability

All committed artifacts with these identities are historical and immutable:

```text
task_0024af_*
task_0024ag_*
task_0024ah_*
task_0024ai_*
task_0024aj_*
```

This includes the AJ validator, adapter, parser, final driver, manifest, consumption sidecar, blocker record, qualifications, audits and report.

Do not edit, delete, repair, rename, relabel, reconstruct or rerun the AJ retained manifest/driver. Do not remove or alter its `CONSUMED_BEFORE_FIRST_CASE` evidence. The frozen AJ focused validator may be executed as a test case under the new AK adapter and AK manifest, but its file must remain byte-identical.

All new tools, evidence, reports and retained artifacts must use the `task_0024ak_*` identity.

## 3. Mandatory multi-subagent execution

Read and route through:

```text
D:\Delayed_Self\.codex\config.toml
```

`.codex/config.toml` is the sole permitted pre-existing untracked path. Record its path and SHA-256 without exposing contents. Do not modify, stage, commit, delete or copy it. Require byte identity at closeout.

Use at least three distinct roles:

1. **PowerShell scope/contract mapper** — read-only analysis of AJ root cause, PowerShell dot-source/module scope, wrapper contract and historical boundaries;
2. **driver implementation worker** — create only new AK adapter/module/probes/preflight/final-driver artifacts;
3. **independent reviewer/test auditor** — verify no dot-sourced parameter script, sentinel preservation, parser fail-closed behavior, consumption semantics and changed-path boundaries.

A documentation/evidence writer may be a fourth role. Only the main agent may launch Godot, integrate changes, declare or execute a retained manifest, stage, commit or push. No subagent may run Godot, perform Git writes or access Profile/user data. Parallelism is allowed only for disjoint read-only work; one writer per file.

If multi-subagent routing or local configuration parsing fails, return an honest blocker. Do not silently fall back to single-agent execution.

## 4. Continuous-execution doctrine

Use this explicit state machine:

```text
AUTHORING
→ INDEPENDENT_REVIEW
→ PARSER_AND_SCOPE_PROBES
→ REPEATABLE_FOCUSED_1
→ REPEATABLE_FOCUSED_2
→ STATIC_AND_PRE_FINAL_AUDIT
→ RETAINED_MANIFEST_DECLARED
→ RETAINED_CYCLE
→ POST_FINAL_AUDIT
→ COMMIT_AND_PUSH
```

Before `RETAINED_MANIFEST_DECLARED`, defects in new `task_0024ak_*` scripts, modules, probes, parsers, validators, evidence capture or documentation are repairable authoring failures. They block progression to the next state but do not terminate the task. Continue repairing, independently reviewing and requalifying.

Do not return a terminal response while an authorized subagent is still running or while a repairable AK-only defect remains unresolved.

A terminal blocker before retention is permitted only for:

- protected/historical artifact drift;
- remote drift that cannot be fast-forwarded safely;
- unexpected tracked/staged/untracked paths;
- inability to preserve `.codex/config.toml` identity;
- unowned/unproven Godot process residue;
- requirement to modify a frozen or unauthorized surface;
- multi-subagent capability failure.

After the AK retained manifest is declared, any retained failure is terminal for Task 0024AK and must be reported honestly without repair or rerun.

## 5. Startup gate

Before editing or launching Godot:

1. require root `D:\Delayed_Self`, branch `main`, expected origin and no Git operation in progress;
2. fetch and fast-forward only;
3. require `HEAD == origin/main`, divergence `0/0`, ancestry of `75b6cfa6f918c2d4ff52ebbe2c0ec014fec6f3c5`, and no tracked/staged changes;
4. permit only untracked `.codex/config.toml`, record its SHA-256 and require it unchanged;
5. require zero pre-existing Godot processes;
6. require Godot `4.7.1.stable.steam.a13da4feb` and exact wrapper blob `5f14eb0feff903b40311c1c7283b20e2eabbffc2`;
7. read `AGENTS.md`, current pointer docs, this task, AJ task/report/GPT acceptance, AF–AI blocker reports/GPT acceptances, AJ qualifications/audits/manifest/blocker, Task 0024AE acceptance/report, Task 0024AD matrix and the headless lifecycle policy;
8. record exact hashes for all Task 0024AF–AJ artifacts, the frozen AJ validator, presentation helper/Gameplay file, all twelve levels, production catalog/route/Profile files, staging route, mechanics foundation and wrapper;
9. stop on historical drift, process ambiguity, additional untracked paths or any need to inspect production Profile/user data.

The accepted Task 0024AE route case may use its exact isolated fixture only through its existing accepted runner and cleanup contract. The focused AJ validator accesses no Profile surface.

## 6. Authorized repository paths

Create or modify only:

```text
tests/tools/task_0024ak_wrapper_capture.ps1
tests/tools/task_0024ak_evidence_parser.psm1
tests/tools/task_0024ak_scope_isolation_probes.ps1
tests/tools/task_0024ak_wrapper_parser_probes.ps1
tests/tools/task_0024ak_driver_preflight.ps1
tests/tools/task_0024ak_static_preservation.ps1
tests/tools/task_0024ak_final_cycle.ps1
tests/tools/task_0024ak_*.json
docs/reports/0024AK_LIBRARY_SCOPE_ISOLATED_RETAINED_DRIVER_AND_SCHEMA_V2_VISUAL_VALIDATION_REPORT.md
docs/presentation/SCHEMA_V2_MINIMUM_OWNER_REVIEW_VISUAL_CONTRACT_CURRENT.md
docs/playtest/SUBMISSION_CORE_1_12_OWNER_CURRICULUM_REVIEW_CURRENT.md
docs/CODEX_ACTIVE_TASK_CURRENT.md
docs/CODEX_STARTUP_SNAPSHOT_CURRENT.md
docs/DEVELOPMENT_PLAN_CURRENT.md
docs/DOC_INDEX_CURRENT.md
docs/HANDOFF_MASTER_CURRENT.md
docs/project_memory.md
docs/production/EXPANDED_CONTENT_SUBMISSION_ROADMAP_CURRENT.md
```

No other path may change. Do not create a replacement presentation validator. Execute the frozen AJ validator unchanged.

## 7. Frozen and protected surfaces

Do not modify:

```text
scenes/**
project.godot
data/catalog/**
data/levels/**
scripts/**
schemas/**
tests/run_all.gd
tests/simulation/**
tests/levels/**
tests/curriculum/**
tests/playtest/**
tests/presentation/**
tests/tools/run_owned_godot_headless.ps1
all Task 0024T–0024AJ historical evidence
localization/**
assets/**
audio/**
build/export/release/submission surfaces
```

Do not change input, collision, transitions, restart, history, completion, routes, catalog, finale, presentation implementation or Profile behavior.

## 8. Required scope-isolated architecture

### 8.1 Standalone wrapper adapter

Create:

```text
tests/tools/task_0024ak_wrapper_capture.ps1
```

It must be an executable standalone script only. It must not support or expose a `-Library` parameter and must never be dot-sourced.

It must:

1. accept uniquely named invocation parameters, preferably `AkGodotExecutable`, `AkProjectRoot`, `AkScriptPath`, `AkRequiredMarker`, `AkStderrPolicy`;
2. invoke the frozen accepted wrapper unchanged in child scope;
3. collect all pipeline output as raw values;
4. select exactly one nonempty compact JSON object string;
5. explicitly parse with `ConvertFrom-Json` using supported depth behavior;
6. require schema version/status/underlying status/native exit/marker/stderr/cleanup/PID/stdout fields with correct non-null types;
7. retain raw JSON SHA-256 and byte count;
8. retain adapter Godot count before/after;
9. fail closed on missing, duplicate, invalid, truncated, array-root, null-field, wrong-version, wrong-type or extra output;
10. emit exactly one compact JSON object on success.

Do not infer missing values from process state or previous evidence.

### 8.2 Evidence-parser module

Create:

```text
tests/tools/task_0024ak_evidence_parser.psm1
```

The module must contain only uniquely prefixed function definitions, constants scoped inside functions if possible, and explicit `Export-ModuleMember`. It must have no top-level `param()` block, no exported variables, no process launch, no Git access and no assignment to generic names such as `GodotPath`, `ProjectPath`, `ManifestPath`, `ScriptPath` or `RequiredMarker`.

Import it with `Import-Module`, never dot-source it.

### 8.3 Final driver

Create:

```text
tests/tools/task_0024ak_final_cycle.ps1
```

Use uniquely prefixed driver parameters and immutable local copies, for example:

```text
AkDriverGodotExecutable
AkDriverProjectRoot
AkDriverManifestPath
```

Binding requirements:

- no dot-source operator may load any `.ps1` file;
- the evidence parser must be loaded only via `Import-Module`;
- the wrapper adapter must be invoked only via call operator in child scope;
- helper loading must not introduce top-level parameters into driver scope;
- after every module import and before every case, require the immutable driver copies to remain exact, nonempty and equal to startup values;
- never use generic mutable variables named `GodotPath`, `ProjectPath` or `ManifestPath` in the final driver.

The driver must validate manifest schema/order, atomically consume once, fail on pre-existing sidecar, execute cases in exact order, stop on first failure, retain complete parsed evidence, require Godot 0 before/after each case and in `finally`, and emit exactly one compact final JSON object on pass.

## 9. Scope-isolation and parser qualification

Create deterministic probes proving:

1. importing the AK module leaves sentinel variables named `GodotPath`, `ProjectPath`, `ManifestPath`, `ScriptPath`, `RequiredMarker` and all `AkDriver*` values byte-for-byte unchanged;
2. importing the module twice with `-Force` does not alter sentinels or export variables;
3. the final driver AST contains no dot-source invocation of `.ps1` files;
4. the adapter AST exposes no `Library` parameter;
5. module exports only the expected prefixed functions;
6. standalone adapter parsing rejects missing, duplicate, non-JSON, truncated, array-root, null-field, wrong-version, wrong-type and extra-output cases;
7. driver preflight rejects malformed case order, duplicate manifest ID, existing consumption sidecar, empty executable/root/manifest values and noncanonical manifest location;
8. all probes work under the actual PowerShell 7 execution environment; parser-only compatibility may also be checked under Windows PowerShell 5.1 but does not replace PowerShell 7 qualification.

The preflight must not declare or consume a retained manifest and must not run Godot.

## 10. Repeatable real qualification

Before retention, execute the frozen AJ focused validator twice through the new AK standalone adapter:

```text
res://tests/presentation/task_0024aj_sensor_contract_and_dynamic_signature_validation.gd
```

Required marker:

```text
TASK_0024AJ_SENSOR_CONTRACT_DYNAMIC_SIGNATURE_PASS
```

Each qualification must independently retain:

- wrapper schema version 2;
- status and underlying status `PASS`;
- native exit 0;
- marker exact once;
- stderr empty;
- cleanup exactly once;
- empty remaining owned/unproven PID arrays;
- adapter Godot counts 0/0;
- complete AJ evidence parse with exactly thirteen unique bounded records;
- exact 13-entry lifecycle signature registry;
- all thirteen signatures nonempty and pairwise distinct;
- four candidate route preflights;
- swapped-identity control;
- action 10 open/unoccupied, actions 11/12 per-cell deferred and action 13 vacancy-close.

Preserve all failed AK authoring attempts honestly. Repeatable qualification failures before manifest declaration remain repairable within authorized AK tools.

## 11. Static and frozen audits

Create machine-readable PRE_FINAL and POST_FINAL audits proving:

1. all Task 0024AF–AJ artifacts are byte-identical;
2. AJ manifest/sidecar/blocker remain exact and consumed;
3. frozen AJ validator is byte-identical;
4. wrapper blob is exact;
5. no protected product/test surface changed;
6. changed paths are within the exact AK allowlist;
7. `.codex/config.toml` remains untracked, unstaged and byte-identical;
8. new driver has no `.ps1` dot-source operation;
9. module exports no variables and no generic parameter names;
10. production catalog remains exactly eight levels and sequence 8 remains the formal finale;
11. all twelve accepted `(L*, N*)` values remain available and exact in the accepted Task 0024AD matrix.

PRE_FINAL must pass with zero mismatch before manifest declaration.

## 12. Fresh retained Task 0024AK cycle

Only after all probes, two repeatable qualifications, independent review and PRE_FINAL audit pass may the main agent create one new canonical AK manifest and atomically declare it.

Execute exactly once each, in this order:

1. frozen AJ focused sensor-contract/dynamic-signature validator;
   - marker `TASK_0024AJ_SENSOR_CONTRACT_DYNAMIC_SIGNATURE_PASS`;
   - stderr empty;
2. accepted Task 0024AE isolated staging route qualification;
   - marker `TASK_0024AE_ISOLATED_STAGING_OWNER_ROUTE_PASS`;
   - stderr empty;
3. accepted Task 0024AD 1–12 batch validator;
   - marker `TASK_0024AD_SUBMISSION_CORE_1_12_BATCH_PASS`;
   - stderr empty;
4. Aggregate;
   - exact marker `TASK_0003_TESTS_PASS assertions=621 vectors=9`;
   - stderr exactly `LEVEL_ID_MISMATCH`;
5. corrected complete-v2;
   - marker `TASK_0024V_V2_COMPLETE_PASS`;
   - stderr empty.

For every case retain:

- raw wrapper JSON SHA-256 and byte count;
- parsed schema/status/underlying status/native exit;
- exact marker count;
- stderr;
- cleanup count/order;
- owned/unproven PID arrays;
- wrapper and adapter Godot before/after;
- bounded output;
- AJ evidence parse for the first case.

Any failure after manifest declaration is terminal for AK. Do not repair or rerun. Final Godot count must be zero.

## 13. Report and status update

Create:

```text
docs/reports/0024AK_LIBRARY_SCOPE_ISOLATED_RETAINED_DRIVER_AND_SCHEMA_V2_VISUAL_VALIDATION_REPORT.md
```

The report must include:

- exact AJ blocker root cause and historical preservation;
- new adapter/module/final-driver architecture;
- sentinel and AST scope-isolation probes;
- parser/driver negative probes;
- two repeatable real qualifications;
- full retained-cycle records and hashes;
- PRE_FINAL/POST_FINAL frozen audits;
- multi-subagent routing ledger;
- `.codex/config.toml` identity;
- explicit owner/final-art/catalog boundaries.

On pass update the owner worksheet only to:

```text
owner_route_status = MACHINE_QUALIFIED_ISOLATED_STAGING_ROUTE
owner_playthrough_status = NOT_YET_REVIEWED
presentation_readiness = MACHINE_QUALIFIED_SCHEMA_V2_MINIMUM_OWNER_REVIEW_REQUIRED
visual_art_direction_status = NOT_FINAL
catalog_admission_status = NOT_AUTHORIZED
```

Do not populate subjective answers.

## 14. Commit and push

On pass or honest blocker:

1. stage only authorized AK paths;
2. never stage `.codex/config.toml`;
3. inspect staged names and run `git diff --cached --check`;
4. create one single-topic result commit without amend/rebase/squash;
5. fetch before push and stop on remote drift;
6. push only a clean fast-forward to `origin/main`;
7. require `HEAD == origin/main`, divergence `0/0`, no tracked/staged changes, only unchanged `.codex/config.toml` untracked and Godot zero;
8. report any separate correction commit transparently.

Suggested pass commit:

```text
test: isolate retained driver scope and validate schema v2 presentation
```

## 15. Terminal verdict

Return exactly one:

```text
SCHEMA_V2_MINIMUM_OWNER_REVIEW_PRESENTATION_DRIVER_ISOLATED_READY_FOR_GPT_REVIEW
BLOCKED_SCHEMA_V2_RETAINED_DRIVER_SCOPE_ISOLATION
PROTECTED_DATA_BOUNDARY_BREACH
```

A pass authorizes only GPT review of the machine-qualified minimum schema-v2 presentation. It does not authorize owner subjective acceptance, final visual-art acceptance, production catalog/progression/finale/Profile changes, localization, assets/audio, build/export, release, upload or submission.
