# Task 0023ZL — Continue cleanup/capture repair after the already-completed fetch

- Status: `READY`
- Gate: `PROTECTED PLAYER DATA / POST-FETCH DIRTY-DRAFT CONTINUATION + CAPTURE FINALIZATION`
- Repository/root/branch: `zcx369658780/delayed-self-godot` / `D:\Delayed_Self` / `main`
- GPT adjudication: `docs/reports/0023ZK_GPT_CIRCULAR_PREFETCH_EVIDENCE_ADJUDICATION.md`
- Required remote checkpoint: `789c79cdb42ba308f0bc87eefeaaebe2deabe700`
- Required local pre-fast-forward HEAD: `fb8f2acc7d021bb7f01febb358e548f3b26b07bf`
- Future execution task: `0023ZJ`
- Future fixture ID: `task_0023zj_app_root_profile_session`

## 1. Objective

Continue the exact five uncommitted canonical harness drafts already present after the one reported `git fetch`, without pretending to possess pre-fetch evidence.

Finish only:

```text
canonical 17-file runner finalization on every post-process path
+ complete static validator reinforcement
+ final parser / no-guard / two zero-I/O Qualifications
+ pre/post frozen regressions and protected-state audits
```

Do not execute the full matrix or touch any fixture.

## 2. Accepted startup exception

The first fetch has already happened. Do not attempt to reconstruct, rename, or fabricate pre-fetch evidence.

Before any further fetch, fast-forward, edit, Godot, or PowerShell validation, require:

```text
absolute root = D:\Delayed_Self
branch = main
local HEAD = fb8f2acc7d021bb7f01febb358e548f3b26b07bf
origin/main contains 789c79cdb42ba308f0bc87eefeaaebe2deabe700
no merge/rebase/cherry-pick/bisect in progress
no staged changes
only the five canonical harness artifacts are dirty
```

Permitted dirty paths:

```text
tests/app/task_0023zd_app_root_profile_session_full_matrix.gd
tests/app/task_0023zd_full_matrix_parser_smoke.gd
tests/app/task_0023zd_full_matrix_harness_validator.gd
tests/app/task_0023zd_full_matrix_manifest.json
tests/app/task_0023zd_capture_profile_session.ps1
```

Stop without reset, restore, stash, clean, checkout, or overwrite if any condition fails.

## 3. Current post-fetch inherited-draft evidence

Before fast-forward, retain under:

```text
D:\Delayed_Self_Evidence\0023ZL_cleanup_capture_repair_post_fetch_continuation\draft_intake\
```

at minimum:

```text
post_fetch_local_head.txt
post_fetch_origin_main.txt
post_fetch_status_porcelain_v2.txt
post_fetch_dirty_paths.txt
post_fetch_worktree_binary.patch
post_fetch_worktree_patch_sha256.txt
post_fetch_diff_stat.txt
post_fetch_staged_diff.txt
post_fetch_git_operation_state.txt
remote_commits_changed_paths.txt
```

Label every file as post-fetch evidence. Do not call it pre-fetch evidence.

Use direct .NET SHA-256 or another already available deterministic implementation. Do not use `Get-FileHash`.

Require the remote changes from local HEAD through `origin/main` to modify none of the five canonical harness paths.

## 4. Safe fast-forward

After current-state capture:

1. fast-forward only to `origin/main`;
2. no merge or rebase;
3. require the five drafts remain present;
4. recapture their binary patch and SHA-256;
5. require the before/after draft patch bytes are identical;
6. require no sixth dirty path appears.

If the draft cannot be preserved byte-identically, stop without modifying it further.

## 5. Protected boundary

Production Profile content remains forbidden. Record only:

```text
exists = true
Length = 473
LastWriteTimeUtc = 2026-07-19T08:15:02.2089459Z
```

For `user://delayed_self_test_profiles/`, record only whether the root exists. Do not enumerate it.

The 0023ZJ contract may exist only as inert constants:

```text
future_task = 0023ZJ
fixture_id = task_0023zj_app_root_profile_session
canonical_profile_path = user://delayed_self_test_profiles/task_0023zj_app_root_profile_session/delayed_self_profile.json
execution_guard = --task-0023zj-fixture-execution
```

Forbidden in this task:

```text
runner Execute
0023ZJ guard-passing
0023ZJ fixture check or operation
configured AppRoot tree entry
full behavior execution
production Profile content access
aggregate / export / release / submission
```

## 6. Frozen sources

Do not modify AppRoot, codec, store, adapter, MemoryProgress, accepted 0023R–0023ZC tests, 0023ZF diagnostic artifacts, scenes, project settings, catalog/formal data, Simulation, solver, schema, Gameplay, router, finale, Help, Timeline, or Level Select.

Only the five canonical harness files plus authorized report/current docs may change.

## 7. Required final source state

Preserve working draft repairs already present. Complete and prove all of the following.

### 7.1 Cleanup invariant

- no runtime self-source read;
- no enumeration APIs in behavior source;
- `cleanup_no_test_root_enumeration` remains a stable case;
- it calls a bounded exact-owned-path helper;
- exact inventory remains 25 unique paths;
- canonical target is first;
- the other 24 paths are bounded to the exact fixture directory;
- deletion is exact-file only;
- fixture-directory removal is non-recursive;
- final exact absence checks remain.

### 7.2 Portable SHA-256

- no executable `Get-FileHash` use;
- direct .NET SHA-256 implementation;
- empty-file known vector:
  `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`;
- all streams and hash objects disposed deterministically.

### 7.3 Fail-closed canonical 17-file finalization

After a behavior process starts, every runner path must finalize all 17 canonical attempt files even when:

```text
Godot exits nonzero
stderr is nonempty
assertion or abort occurs
patch capture fails
hash generation fails
stage-summary parsing fails
classification is unresolved
```

Write post-state files before patch/hash work. Inventory is written last. Missing or failed evidence must produce bounded sentinel content and final classification `DIAGNOSTIC_CAPTURE_INCOMPLETE`; it must not truncate the inventory.

### 7.4 Validator authority

The validator must prove:

- exact 0023ZJ task/fixture/path/guard contract;
- old 0023ZH executable tokens absent;
- cleanup helper shape and call site;
- no behavior-source enumeration/self-read tokens;
- direct .NET SHA-256 and no `Get-FileHash` dependency;
- known-vector Qualification support;
- post-state before hash ordering;
- all 17 canonical files represented;
- inventory written last;
- hash/classification failures still finalize 17 files;
- all original 18 stages, 41 cases, evidence inventories, failure stages, disclosure checks, AppRoot ordering checks, sorted-unlock repair, and frozen hashes remain.

Do not weaken an existing assertion merely to pass.

## 8. Mandatory final validation

Preliminary 0023ZI evidence does not count.

From the completed final source state run:

1. fresh parser smoke;
2. strengthened validator, deterministic assertion count and marker;
3. no-guard behavior proof: exit 2, guard marker once, behavior start zero;
4. final zero-I/O Qualification 1 with 11/11 files and known-vector pass;
5. final zero-I/O Qualification 2 with 11/11 files and known-vector pass;
6. frozen regressions:
   - 0023R 123 assertions / 4 positive / 39 negative;
   - 0023T compile + 34;
   - 0023V compile + 28;
   - 0023W compile + 68;
   - 0023X compile + 31;
   - 0023ZC parser + 17;
   - 0023ZF parser only.

Every process must exit 0, have empty stderr, print its marker once, preserve protected metadata/test-root existence, and leave no Godot process.

Command audit must show:

```text
Execute commands = 0
guard-passing commands = 0
fixture commands = 0
full behavior executions = 0
```

## 9. Repository output

Create:

```text
docs/reports/0023ZL_APP_ROOT_PROFILE_SESSION_CLEANUP_AND_CAPTURE_REPAIR_REPORT.md
```

Commit only after every final gate passes:

```text
test: finalize Delayed Self cleanup and capture repair
```

Authorized paths are the five canonical harness files, the report, and current project documentation.

Finish with clean worktree, `HEAD == origin/main`, divergence `0/0`, no Godot process, unchanged production metadata/test-root existence, and no fixture operation.

## 10. Terminal verdict

Return exactly one:

```text
APP_ROOT_PROFILE_SESSION_CLEANUP_AND_CAPTURE_REPAIR_READY_FOR_GPT_REVIEW
BLOCKED_APP_ROOT_PROFILE_SESSION_CLEANUP_AND_CAPTURE_REPAIR
```

A passing verdict establishes only harness/capture repair and zero-I/O requalification. It does not establish a passing full matrix or production persistence.
