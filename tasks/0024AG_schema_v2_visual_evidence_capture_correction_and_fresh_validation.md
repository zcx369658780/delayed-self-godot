# Task 0024AG — Correct schema-v2 visual evidence capture and complete fresh machine validation

- Status: `READY`
- Gate: `SCHEMA-V2 PRESENTATION EVIDENCE-CAPTURE CORRECTION / COMPREHENSIVE VALIDATION / MULTI-SUBAGENT REQUIRED`
- Repository/root/branch: `zcx369658780/delayed-self-godot` / `D:\Delayed_Self` / `main`
- Required accepted checkpoint: `f7f0e08edeaebcc796d6908d8e822f0d68c33a17`
- Historical blocked result commit: `25d7fda3937fc269cdf730c14698e47bfb209c29`
- Latest GPT verdict: `TASK_0024AF_HONEST_RETAINED_CYCLE_DRIVER_BLOCKER_ACCEPTED_FRESH_EVIDENCE_GATE_REQUIRED`
- Provisional implementation candidate:
  - `scripts/gameplay/schema_v2_presentation.gd`
  - `scripts/gameplay/vertical_slice.gd`
- Historical focused validator: `tests/presentation/task_0024af_schema_v2_minimum_visual_validation.gd`
- Formal product baseline: exactly eight production-catalog levels; sequence 8 remains the sole formal finale
- Accepted uncatalogued candidates: `crate_shadow_timing`, `key_route_commitment`, `identity_shift_bridge`, `lasting_echo_latch`
- Recommended main-agent model: `GPT-5.6 Sol Max`; fallback `GPT-5.6 Sol High`

## 1. Objective

Under a new task identity:

1. preserve the honest Task 0024AF blocker and all consumed artifacts;
2. correct wrapper-result capture by explicitly parsing the accepted wrapper's compact JSON text;
3. comprehensively validate the provisional schema-v2 presentation implementation against the original visual contract;
4. fix only evidence-proven presentation defects if the unchanged implementation fails the new validation;
5. run one fresh retained five-case cycle with complete non-null evidence;
6. establish whether the minimum schema-v2 presentation is machine-ready for owner review.

This task does not conduct owner subjective playtesting and does not authorize production catalog, finale or Profile changes.

## 2. Historical Task 0024AF immutability

The following historical facts and paths are terminal and immutable:

- retained invocation `621b1e96-adeb-4a7d-8398-68fef9c2b0a4`;
- `tests/tools/task_0024af_final_cycle.ps1`;
- `tests/tools/task_0024af_final_manifest.json`;
- `tests/tools/task_0024af_final_case_focused.json`;
- `docs/reports/0024AF_SCHEMA_V2_MINIMUM_OWNER_REVIEW_VISUAL_PRESENTATION_REPORT.md`;
- every other committed `task_0024af_*` evidence artifact;
- the GPT blocker acceptance report.

Do not edit, delete, repair, reconstruct or rerun the Task 0024AF retained cycle. The new task may execute the historical focused validator only during repeatable qualification if useful, but all final evidence must use Task 0024AG identity and paths.

## 3. Mandatory multi-subagent doctrine

Read and route through:

```text
D:\Delayed_Self\.codex\config.toml
```

`.codex/config.toml` is the sole permitted pre-existing untracked path. Record its path and SHA-256 without exposing contents. Do not modify, stage, commit, delete or copy it. Require byte identity at closeout.

Use at least three distinct roles:

1. **source/contract mapper** — read-only inspection of the provisional helper, Gameplay integration, Simulation state and Task 0024AF evidence;
2. **driver and validation worker** — implement the new wrapper-output parser, comprehensive Task 0024AG validator and bounded evidence tools;
3. **independent reviewer/test auditor** — review semantic preservation, state coverage, parser failure behavior and changed-path boundaries.

A documentation/evidence writer may be used as a fourth role. Only the main agent may launch Godot, integrate final changes, run retained cases, stage, commit or push. No subagent may access Profile/user data, perform Git writes or launch Godot. Parallel work is allowed only for disjoint read-only analysis; one writer per file.

If multi-subagent routing or configuration parsing fails, return an honest blocker. Do not silently fall back to single-agent execution.

## 4. Startup gate

Before editing or launching Godot:

1. require root `D:\Delayed_Self`, branch `main`, expected origin and no Git operation in progress;
2. fetch and fast-forward only;
3. require `HEAD == origin/main`, divergence `0/0`, ancestry of `f7f0e08edeaebcc796d6908d8e822f0d68c33a17` and no tracked/staged changes;
4. allow only untracked `.codex/config.toml` and require its pre-task hash;
5. require zero pre-existing Godot processes;
6. require Godot `4.7.1.stable.steam.a13da4feb` and accepted wrapper blob `5f14eb0feff903b40311c1c7283b20e2eabbffc2`;
7. read `AGENTS.md`, current pointer docs, this task, Task 0024AF v2, its blocker report/GPT adjudication, Task 0024AE acceptance/report, Task 0024AD matrix, accepted sequence-9–12 evidence, schema-v2/Simulation contracts and lifecycle policy;
8. record exact hashes for provisional presentation files, historical Task 0024AF artifacts, all twelve levels, production catalog/route/Profile files, staging route files, foundation and accepted evidence;
9. stop on drift, process ambiguity, additional untracked paths, historical-evidence mismatch or any need to inspect production Profile/user data.

The exact isolated Task 0024AE test Profile may be used only by the accepted route case through its existing runner and exact cleanup policy. The focused visual case must access no Profile surface.

## 5. Authorized repository paths

Create or modify only:

```text
scripts/gameplay/schema_v2_presentation.gd
scripts/gameplay/vertical_slice.gd
tests/presentation/task_0024ag_schema_v2_visual_correction_validation.gd
tests/presentation/task_0024ag_schema_v2_visual_correction_validation.gd.uid
tests/tools/task_0024ag_*.ps1
tests/tools/task_0024ag_*.py
tests/tools/task_0024ag_*.json
docs/presentation/SCHEMA_V2_MINIMUM_OWNER_REVIEW_VISUAL_CONTRACT_CURRENT.md
docs/playtest/SUBMISSION_CORE_1_12_OWNER_CURRICULUM_REVIEW_CURRENT.md
docs/reports/0024AG_SCHEMA_V2_VISUAL_EVIDENCE_CORRECTION_AND_VALIDATION_REPORT.md
docs/CODEX_ACTIVE_TASK_CURRENT.md
docs/CODEX_STARTUP_SNAPSHOT_CURRENT.md
docs/DEVELOPMENT_PLAN_CURRENT.md
docs/DOC_INDEX_CURRENT.md
docs/HANDOFF_MASTER_CURRENT.md
docs/project_memory.md
docs/production/EXPANDED_CONTENT_SUBMISSION_ROADMAP_CURRENT.md
```

No other path may change. The historical Task 0024AF focused validator is frozen; the new comprehensive validator must use a new Task 0024AG path.

### 5.1 Provisional implementation change policy

Begin by validating the committed Task 0024AF implementation byte-for-byte unchanged.

- If it passes the complete Task 0024AG contract, do not modify the helper or `vertical_slice.gd`.
- If the new validator exposes a specific presentation defect, a bounded fix to those two authorized product files is permitted.
- Every such change must be directly tied to a retained failed assertion, independently reviewed and requalified.
- Do not weaken an assertion, remove a required state or change mechanics to obtain a pass.

## 6. Frozen and protected surfaces

Do not modify:

```text
scenes/**
project.godot
data/catalog/**
data/levels/**
scripts/app/**
scripts/simulation/**
scripts/solver/**
scripts/gameplay files other than the two authorized presentation files
schemas/**
tests/run_all.gd
tests/simulation/**
tests/levels/**
tests/curriculum/**
tests/playtest/**
tests/presentation/task_0024af_schema_v2_minimum_visual_validation.gd
tests/tools/run_owned_godot_headless.ps1
all Task 0024T–0024AF historical evidence
localization/**
assets/**
audio/**
build/export/release/submission surfaces
```

Do not change input, collision, transition, restart, history, completion, route, catalog, finale or Profile behavior.

## 7. Wrapper-result capture correction

The accepted wrapper emits one compact JSON string to stdout; it does not return a hydrated PowerShell object.

Create a new bounded adapter, for example:

```text
tests/tools/task_0024ag_invoke_wrapper_case.ps1
```

It must:

1. invoke `tests/tools/run_owned_godot_headless.ps1` without modifying it;
2. collect all PowerShell pipeline output as raw values;
3. select exactly one nonempty string that parses as the wrapper schema-v2 JSON record;
4. run `ConvertFrom-Json -Depth 100` explicitly;
5. require all fields below to exist and have non-null values:
   - `schema_version`
   - `status`
   - `underlying_status`
   - `native_exit_code`
   - `marker_pass`
   - `stderr`
   - `cleanup_invocation_count`
   - `remaining_owned_pids`
   - `remaining_unproven_pids`
   - `stdout`;
6. require `schema_version=2`;
7. fail closed on zero records, multiple records, invalid JSON, JSON arrays, null required fields, wrong types or extra unparsed output that could be mistaken for a result;
8. retain the raw wrapper JSON line SHA-256 and parsed record;
9. never infer missing values from process state or previous runs.

### 7.1 Parser qualification

Before any final cycle, create deterministic negative probes proving rejection of:

- missing record;
- duplicate records;
- non-JSON text;
- truncated JSON;
- JSON array instead of object;
- object with null required fields;
- wrong schema version;
- wrong field types.

Run at least two repeatable real focused qualifications through the adapter and require complete non-null records. These runs are authoring qualification, not final retained evidence.

## 8. Static semantic-preservation qualification

Create a machine-readable static qualification that:

1. compares the provisional/final presentation helper and `vertical_slice.gd` against commit `25d7fda3937fc269cdf730c14698e47bfb209c29`;
2. records whether either implementation file changed during Task 0024AG;
3. if changed, proves route-payload validation, level loading, input mapping, Simulation transition/restart calls, completion/finale acknowledgement and request-back behavior remain semantically unchanged;
4. proves the helper contains no loader, file, catalog, Profile, user path, transition or restart implementation;
5. proves all changed paths are authorized;
6. proves historical Task 0024AF artifacts are byte-identical;
7. proves `.codex/config.toml` remains untracked, unstaged and byte-identical;
8. proves production scene/catalog/route/Profile/level/foundation files remain unchanged.

## 9. Comprehensive focused Task 0024AG validator

Create:

```text
res://tests/presentation/task_0024ag_schema_v2_visual_correction_validation.gd
```

Pass marker:

```text
TASK_0024AG_SCHEMA_V2_VISUAL_CORRECTION_PASS
```

Failure prefix:

```text
TASK_0024AG_SCHEMA_V2_VISUAL_CORRECTION_FAIL
```

Use public LevelLoader, shared Simulation, accepted witness actions and the real presentation helper/Gameplay snapshot. Do not duplicate transition logic.

### 9.1 Descriptor contract and purity

For sequences 9–12 prove:

- every currently visible v2 entity/state has exactly the expected descriptor count;
- every descriptor has stable family, ID, cell/position, layer, variant, shape, glyph, type/group token, bounds and monochrome signature;
- ordering is deterministic;
- duplicate snapshot calls are identical;
- returned snapshots are deep-copy mutation resistant;
- helper generation does not mutate level, Simulation state, canonical key, history, completion or turn index;
- normal Gameplay snapshot exactly equals the helper output for the same state.

### 9.2 Sequence 9 crate lifecycle

Prove initial crate presence/cell, exact movement after the accepted YOU push, no ECHO push affordance, structural distinction from wall/YOU/ECHO and exact restart restoration.

### 9.3 Sequence 10 key/lock/inventory lifecycle

Retain exact states proving:

- initial floor key and matching lock share one stable key-type token;
- ECHO contact changes no floor-key, held-key or lock presentation;
- YOU collection removes floor key and adds held inventory;
- same-turn matching lock consumption/entry removes both lock and held key;
- no stale inventory remains;
- restart restores key/lock and clears inventory.

### 9.4 Sequence 11 sensor/barrier lifecycle

Retain exact states proving:

- inactive YOU_ONLY and ECHO_ONLY sensors remain visible and have distinct non-color signatures;
- wrong-actor contact does not present the correct active identity state;
- correct paired occupancy presents both active sensors;
- the two cells share one stable group token;
- closed, open-next-snapshot, occupied-closure-deferred on each group cell, and closed-after-vacancy are structurally distinct;
- sensor underlay and barrier edge identity remain visible under actor occupancy;
- restart restores inactive sensors and closed group.

### 9.5 Sequence 12 latch synthesis lifecycle

Retain exact states proving:

- inactive latch and target token;
- first activation changes to a persistent committed signature;
- later turns preserve activated presentation and do not imply a second activation;
- `barrier_phase` and `barrier_typed` have distinct stable group tokens;
- latch token targets only `barrier_phase`;
- persistent latch-open state differs from temporary sensor-open and deferred states;
- restart restores inactive latch and initial group states.

### 9.6 Monochrome uniqueness

Ignoring all color fields, require distinct structural signatures for at least:

```text
crate
floor_key
held_key
lock
sensor_you_inactive
sensor_you_active
sensor_echo_inactive
sensor_echo_active
barrier_closed
barrier_open
barrier_open_deferred
latch_inactive
latch_activated
```

No required pair may collapse to the same signature.

### 9.7 Fit, layering and v1 regression

Prove:

- all descriptor bounds remain inside the existing logical gameplay area for sequences 1–12;
- layer ordering matches terrain → underlay/open edges → blockers/interactables → actors → tokens/inventory;
- actor occupancy does not erase sensor/barrier identity;
- sequences 1–8 produce no spurious v2 descriptors;
- all twelve levels load and their accepted `(L*, N*)` values remain unchanged through the accepted Task 0024AD focused validator;
- no route, input, mechanics or completion regression.

Do not claim human clarity or final art quality from these tests.

## 10. Evidence format

Emit independently parseable prefixed JSON records below 1800 UTF-8 bytes. Include separate records for:

- multi-subagent ledger;
- static preservation;
- wrapper-parser qualification;
- per-level inventory;
- crate lifecycle;
- key/lock/inventory lifecycle;
- sensor/barrier lifecycle;
- latch lifecycle;
- monochrome uniqueness;
- fit/layering/v1 regression;
- summary.

The capture parser must reject missing, duplicate, truncated, invalid-JSON and over-bound records and retain exact byte counts.

## 11. Repeatable qualification

Before final retention, repeatable qualification is repairable.

Every Godot execution must use the accepted wrapper through the new explicit parser adapter, invoke cleanup exactly once, retain no owned/unproven PID and end with Godot zero. Preserve failed attempts honestly. No subagent may run Godot.

Require at minimum:

1. two passing real focused adapter qualifications with complete non-null wrapper records;
2. passing comprehensive focused validation;
3. passing static preservation qualification;
4. passing evidence parser negative probes;
5. PRE_FINAL frozen audit with zero mismatches.

## 12. Fresh retained Task 0024AG cycle

After all qualification is stable, execute exactly once each under one new Task 0024AG manifest:

1. focused Task 0024AG visual correction validator;
   - marker `TASK_0024AG_SCHEMA_V2_VISUAL_CORRECTION_PASS`;
   - stderr empty;
2. accepted Task 0024AE isolated staging route qualification;
   - marker `TASK_0024AE_ISOLATED_STAGING_OWNER_ROUTE_PASS`;
   - stderr empty;
3. accepted Task 0024AD 1–12 batch validator;
   - marker `TASK_0024AD_SUBMISSION_CORE_1_12_BATCH_PASS`;
   - stderr empty;
4. Aggregate;
   - exact marker `TASK_0003_TESTS_PASS assertions=621 vectors=9`;
   - stderr exactly `LEVEL_ID_MISMATCH`;
5. corrected complete-v2;
   - marker `TASK_0024V_V2_COMPLETE_PASS`;
   - stderr empty.

All wrapper-driven cases must use the new adapter and retain:

- raw wrapper JSON SHA-256;
- parsed schema version/status/underlying status;
- native exit;
- exact marker result;
- stderr;
- cleanup count/order;
- owned/unproven PID arrays;
- Godot before/after.

The route case may continue using its accepted route runner, but its returned file must be parsed and validated explicitly under the Task 0024AG driver.

Fail immediately on any null required field. Do not proceed to later cases after a failed case. Final Godot count must be zero. Once declared consumed, do not rerun the Task 0024AG cycle.

## 13. Report and status update

Create:

```text
docs/reports/0024AG_SCHEMA_V2_VISUAL_EVIDENCE_CORRECTION_AND_VALIDATION_REPORT.md
```

The report must include:

- exact implementation hashes before/after;
- whether provisional product files changed and why;
- multi-subagent routing ledger;
- Task 0024AF historical preservation evidence;
- exact wrapper-capture root cause and corrected parser architecture;
- parser negative probes and repeatable real-case qualifications;
- full visual state evidence;
- retained cycle records and raw JSON hashes;
- frozen audits and `.codex/config.toml` identity;
- explicit human/final-art/catalog boundaries.

On pass, update the owner worksheet only to:

```text
owner_route_status = MACHINE_QUALIFIED_ISOLATED_STAGING_ROUTE
owner_playthrough_status = NOT_YET_REVIEWED
presentation_readiness = MACHINE_QUALIFIED_SCHEMA_V2_MINIMUM_OWNER_REVIEW_REQUIRED
visual_art_direction_status = NOT_FINAL
catalog_admission_status = NOT_AUTHORIZED
```

Do not populate subjective answers.

## 14. Commit and push

On pass or honest blocker:

1. stage only authorized repository paths;
2. never stage `.codex/config.toml`;
3. inspect staged names and run `git diff --cached --check`;
4. create one single-topic result commit without amend/rebase/squash;
5. fetch before push and stop on drift;
6. push only a clean fast-forward to `origin/main`;
7. require `HEAD == origin/main`, divergence `0/0`, no tracked/staged changes, only unchanged `.codex/config.toml` untracked and Godot zero;
8. report any correction commit transparently.

Suggested pass commit:

```text
test: correct schema v2 visual evidence capture and validate presentation
```

## 15. Terminal verdict

Return exactly one:

```text
SCHEMA_V2_MINIMUM_OWNER_REVIEW_PRESENTATION_CORRECTED_READY_FOR_GPT_REVIEW
BLOCKED_SCHEMA_V2_VISUAL_EVIDENCE_CORRECTION
PROTECTED_DATA_BOUNDARY_BREACH
```

A pass authorizes only GPT review of corrected minimum schema-v2 presentation evidence. It does not authorize owner subjective acceptance, final visual-art acceptance, production catalog/progression/finale/Profile changes, localization, assets/audio, build/export, release, upload or submission.