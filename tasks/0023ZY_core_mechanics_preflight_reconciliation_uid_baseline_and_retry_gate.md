# Task 0023ZY — Reconcile the blocked core-mechanics preflight, establish the Godot UID baseline, and complete one canonical retry

- Status: `READY`
- Gate: `OWNER-AUTHORIZED CONTINUATION / DRAFT PRESERVATION + PROCESS RECONCILIATION + UID BASELINE + STALE REGRESSION CORRECTION + ONE COMPLETE RETRY`
- Repository/root/branch: `zcx369658780/delayed-self-godot` / `D:\Delayed_Self` / `main`
- Required GPT adjudication checkpoint: `64f30c41132deeefc9cc440b54f8673ae0b51eaf`
- Original foundation task: `tasks/0023ZX_core_mechanics_schema_simulation_solver_foundation_gate.md`
- Existing implemented baseline: eight formal levels, unchanged
- Production Profile authority: none
- Catalog/formal-level/final-flag authority: none
- Presentation/asset/font/audio/localization/build authority: none

## 1. Owner authorization and objective

The owner authorizes this continuation to:

1. preserve every current uncommitted Task 0023ZX draft, fixture and generated `.uid` sidecar byte-identically in external evidence before repository synchronization;
2. perform one bounded reconciliation of the current Godot process set using only PID, executable identity/path and command line;
3. close only processes whose identity and command line unambiguously prove they are Delayed Self Task 0023ZX headless test processes;
4. leave every ambiguous, unrelated, editor, project-manager or possibly manual process untouched and stop;
5. establish a reviewed Godot `.uid` sidecar baseline, adopting only valid unique sidecars paired with an eligible `.gd` or `.gdshader` source;
6. delete only individually proven invalid/orphan/duplicate sidecars after external byte-identical preservation;
7. correct only the stale six-level catalog/progression/final assertions in `tests/levels/task_0012_staggered_doors_validation.gd` to the accepted eight-level baseline;
8. continue the preserved Task 0023ZX implementation and focused fixture work;
9. run exactly one final complete Task 0023ZX validation after implementation is ready.

No broad reset, clean, unreviewed stash, wildcard file deletion, process-name-wide action, formal-level change, Profile operation or second complete validation is authorized.

## 2. Startup continuation preservation

Before fetch, merge, edit, process reconciliation or test execution:

1. require root `D:\Delayed_Self`, branch `main`, expected origin and no Git operation in progress;
2. inventory every staged, modified, deleted and untracked path;
3. require zero staged paths and require every dirty path to be one of:
   - the original Task 0023ZX contract/product paths;
   - `tests/simulation/task_0023zx_core_mechanics_*.gd`;
   - `tests/simulation/fixtures/task_0023zx_*.json`;
   - a `.uid` sidecar generated beside an existing tracked `.gd`/`.gdshader` source or beside an authorized new Task 0023ZX `.gd` source;
4. stop if any unrelated dirty path exists;
5. record path, tracked state, SHA-256, byte length and line count for every continuation input;
6. copy every continuation input byte-identically to:

```text
D:\Delayed_Self_Evidence\0023ZY_core_mechanics_preflight_reconciliation\continuation_inputs\
```

7. verify every copied identity before proceeding.

Do not discard or overwrite the preserved drafts. Fetch and fast-forward only after preservation; require synchronization to succeed without broad workspace cleanup.

## 3. Bounded process reconciliation

After continuation preservation, inspect the complete current Godot process set once.

A process is eligible for owner-authorized closure only when executable identity/path and command line together prove:

- a headless Godot process;
- the exact Delayed Self repository root;
- a Task 0023ZX or directly required zero-I/O regression/test script;
- no editor, project-manager, interactive main-scene, export/build or other-project indicator.

Classify the complete set before any action. If any process is ambiguous or unrelated, stop without closing any process and without running tests.

If every residual process is clearly owned, revalidate the exact identities and close only those exact PIDs in one bounded phase. No wildcard or process-name-wide action is allowed. Require the final Godot process count to become zero. If it does not, stop; no second remediation phase is authorized.

If the initial process count is already zero, record that and continue.

## 4. Godot UID baseline

Review the exact preserved/generated `.uid` set individually.

A sidecar may be adopted only when all are true:

1. its exact source is an existing tracked `.gd`/`.gdshader` file or an authorized new Task 0023ZX `.gd` source that will be committed in this task;
2. the sidecar is adjacent to that source and follows the expected source-name-plus-`.uid` relationship;
3. content is one bounded valid UID declaration with no extra payload;
4. the UID is unique across the reviewed repository set;
5. the source file exists and is not an external/generated cache path.

Adopt every reviewed valid paired sidecar. Do not add a blanket ignore rule for valid script/shader UID sidecars.

A sidecar may be removed only if it is individually proven orphaned, duplicate, incorrectly paired or malformed, and only after the external preserved copy is verified. Record the exact reason per removed path. No wildcard or directory-level deletion is authorized.

Create/update:

```text
docs/engineering/GODOT_UID_SIDECAR_BASELINE_CURRENT.md
```

The document must state the reviewed counts, valid-pair rule, uniqueness rule, invalid-path disposition and future source/sidecar commit rule without exposing local absolute paths.

## 5. Exact stale Task 0012 correction

Authorize modification of exactly:

```text
tests/levels/task_0012_staggered_doors_validation.gd
```

Only catalog/progression/final-level expectations may change.

The accepted catalog facts are exactly:

```text
1 tutorial_reach_exit
2 tutorial_echo_bridge
3 vertical_slice_delay_3
4 door_one_turn_late
5 two_keys_one_door
6 staggered_doors
7 echo_spacing_bridge
8 two_echo_convergence
```

Required correction outcomes:

- catalog cardinality is exactly 8;
- IDs/order match the list above;
- sequence 8 `two_echo_convergence` is the sole final entry;
- `staggered_doors` remains sequence 6, non-final, unlocked by `two_keys_one_door`;
- completion of sequence 6 unlocks `echo_spacing_bridge`;
- completion of sequence 7 unlocks `two_echo_convergence`;
- reset still unlocks only `tutorial_reach_exit`;
- the direct-level parser still accepts `staggered_doors`;
- the Level 6 runtime route payload uses `final_level=false`.

Do not weaken or alter Level 6 loading, solver bands, replay, necessity controls, door-window evidence, meaningful-decision evidence, Help behavior, board presentation or restart assertions.

## 6. Continue Task 0023ZX foundation

After Sections 2–5 pass and the Godot process count is zero, continue from the preserved Task 0023ZX drafts.

All original Task 0023ZX deterministic contracts remain binding:

- explicit backward-compatible schema v2;
- schema v1 and all eight formal levels behavior-identical;
- YOU-only crates;
- YOU-only consumable keys/locks;
- grouped barriers/bridges;
- typed sensors;
- one-shot room-local latches;
- one shared Simulation transition for replay and BFS;
- deterministic canonical state key;
- exact restart behavior;
- bounded non-catalog fixtures and solver budgets.

Focused development tests may run in fresh headless processes while implementation is being completed. Each focused run must finish with zero Godot processes and may not touch ordinary user data, the production Profile, the test-profile root, formal catalog/levels, main scene, export or build.

## 7. Exactly one complete final validation

When implementation is ready, run the complete Task 0023ZX validation matrix exactly once.

It must prove:

1. every schema-v1 reference vector remains exact;
2. all eight formal levels retain validation, restart, solver and replay facts;
3. catalog and all eight formal-level SHA-256 values remain unchanged;
4. all Task 0023ZX mechanic, canonical-key, replay, solver and malformed-input tests pass;
5. the corrected Task 0012 regression passes against the accepted eight-level baseline;
6. every adopted `.uid` has a valid unique eligible source pair;
7. only authorized paths changed;
8. zero Godot processes remain;
9. Git and remote-drift audits pass.

If this complete validation exposes any new substantive rules, solver, replay, state-space, formal-level regression, UID-policy or protected-boundary failure, stop immediately. Do not repair and rerun the complete validation in this task.

## 8. Authorized repository paths

Original Task 0023ZX paths remain authorized:

```text
docs/game_design/GAMEPLAY_SIMULATION_CONTRACT_V2_CURRENT.md
docs/level_design/LEVEL_SCHEMA_V2_CURRENT.md
docs/level_design/reference/turn_transition_vectors_v2.json
schemas/level_v2.schema.json
scripts/simulation/level_loader.gd
scripts/simulation/simulation.gd
scripts/solver/bfs_solver.gd
tests/simulation/task_0023zx_core_mechanics_*.gd
tests/simulation/fixtures/task_0023zx_*.json
```

Additional authorized paths:

```text
tests/levels/task_0012_staggered_doors_validation.gd
docs/engineering/GODOT_UID_SIDECAR_BASELINE_CURRENT.md
docs/reports/0023ZY_CORE_MECHANICS_PREFLIGHT_RECONCILIATION_AND_RETRY_REPORT.md
docs/CODEX_ACTIVE_TASK_CURRENT.md
docs/CODEX_STARTUP_SNAPSHOT_CURRENT.md
docs/DEVELOPMENT_PLAN_CURRENT.md
docs/DOC_INDEX_CURRENT.md
docs/HANDOFF_MASTER_CURRENT.md
docs/project_memory.md
docs/production/EXPANDED_CONTENT_SUBMISSION_ROADMAP_CURRENT.md
```

Also authorized are only the exact reviewed valid `.uid` sidecars paired with eligible repository `.gd`/`.gdshader` sources under Section 4.

Freeze without exception:

```text
data/catalog/**
data/levels/**
scenes/**
scripts/gameplay/**
scripts/app/**
project.godot
schemas/level_v1.schema.json
docs/level_design/reference/turn_transition_vectors_v1.json
tests/run_all.gd
assets/**
```

## 9. Repository output

On a complete pass, commit only authorized paths with:

```text
feat: add Delayed Self core mechanics foundation after preflight reconciliation
```

Push normally only after changed-path, frozen-hash, UID-pair, diff-check, process and remote-drift audits.

The report must include bounded startup dirty-state identities, external preservation proof, process classification/remediation counts, UID reviewed/adopted/rejected counts, exact Task 0012 correction scope, final validation results, frozen hashes and non-claims.

## 10. Terminal verdict

Return exactly one:

```text
CORE_MECHANICS_PREFLIGHT_RECONCILIATION_AND_RETRY_READY_FOR_GPT_REVIEW
BLOCKED_CORE_MECHANICS_PREFLIGHT_RECONCILIATION_OR_RETRY
PROTECTED_DATA_BOUNDARY_BREACH
```

A pass establishes only the schema/Simulation/solver foundation, reviewed UID baseline and corrected historical regression. It does not authorize formal rooms 9–12, catalog/finale/Profile migration, Gameplay/UI, presentation, localization, asset/font/audio import, build/export, release, upload or submission.