# Task 0024AH — Build the dynamic lifecycle-signature registry and complete fresh schema-v2 visual validation

- Status: `READY`
- Gate: `DYNAMIC VISUAL SIGNATURE COVERAGE / FRESH SCHEMA-V2 PRESENTATION VALIDATION / MULTI-SUBAGENT REQUIRED`
- Repository/root/branch: `zcx369658780/delayed-self-godot` / `D:\Delayed_Self` / `main`
- Required accepted checkpoint: `a7b404a63e7a546501be215aaf300cabfa74441c`
- Historical blocked Task 0024AG result: `2813d08bf65eae2ae0c528a4825bfc133d5cdb43`
- Latest GPT verdict: `TASK_0024AG_HONEST_LIFECYCLE_SIGNATURE_SAMPLING_BLOCKER_ACCEPTED_FRESH_VALIDATION_GATE_REQUIRED`
- Historical Task 0024AF consumed invocation: `621b1e96-adeb-4a7d-8398-68fef9c2b0a4`
- Provisional presentation implementation baseline: commit `25d7fda3937fc269cdf730c14698e47bfb209c29`
- Accepted wrapper blob: `5f14eb0feff903b40311c1c7283b20e2eabbffc2`
- Production baseline: exactly eight cataloged levels; sequence 8 remains the sole formal finale
- Accepted uncatalogued candidates: `crate_shadow_timing`, `key_route_commitment`, `identity_shift_bridge`, `lasting_echo_latch`
- Recommended main-agent model: `GPT-5.6 Sol Max`; fallback `GPT-5.6 Sol High`

## 1. Objective

Under a new task identity:

1. preserve every historical Task 0024AF and Task 0024AG artifact byte-for-byte;
2. retain the corrected wrapper-JSON parsing doctrine without modifying the accepted wrapper;
3. replace the blocked initial-only uniqueness sampling with an explicit thirteen-entry dynamic lifecycle-signature registry;
4. close the lifecycle-coverage gaps left by Task 0024AG;
5. begin with the provisional product implementation unchanged and modify it only if a named retained assertion proves a real presentation defect;
6. perform two complete repeatable focused qualifications;
7. execute one fresh retained five-case Task 0024AH cycle;
8. determine whether the minimum schema-v2 presentation is machine-ready for owner review.

This task does not conduct owner subjective playtesting and does not authorize production catalog, finale, progression or Profile changes.

## 2. Historical immutability

All committed `task_0024af_*` and `task_0024ag_*` artifacts are historical and immutable, including:

```text
tests/presentation/task_0024af_schema_v2_minimum_visual_validation.gd
tests/presentation/task_0024ag_schema_v2_visual_correction_validation.gd
tests/tools/task_0024af_*
tests/tools/task_0024ag_*
docs/reports/0024AF_SCHEMA_V2_MINIMUM_OWNER_REVIEW_VISUAL_PRESENTATION_REPORT.md
docs/reports/0024AG_SCHEMA_V2_VISUAL_EVIDENCE_CORRECTION_AND_VALIDATION_REPORT.md
docs/reports/0024AF_GPT_HONEST_RETAINED_CYCLE_DRIVER_BLOCKER_ACCEPTANCE.md
docs/reports/0024AG_GPT_LIFECYCLE_SIGNATURE_SAMPLING_BLOCKER_ACCEPTANCE.md
```

Do not edit, delete, repair, relabel or rerun either historical final-cycle identity. Task 0024AG did not start a final cycle, but its validator and qualification records remain historical blocker evidence and must not be edited.

All new validation, adapter, probe and retained-cycle artifacts must use the `task_0024ah_*` identity.

## 3. Mandatory multi-subagent doctrine

Read and route through:

```text
D:\Delayed_Self\.codex\config.toml
```

`.codex/config.toml` is the sole permitted pre-existing untracked path. Record its exact path and SHA-256 without exposing contents. Do not modify, stage, commit, delete or copy it. Require byte identity at closeout.

Use at least three distinct roles:

1. **source/contract mapper** — inspect the provisional helper, Gameplay integration, accepted sequence-9–12 witnesses, Task 0024AG sampling defect and protected boundaries;
2. **validation worker** — implement the new Task 0024AH adapter, lifecycle registry validator, probes and retained driver;
3. **independent reviewer/test auditor** — verify exact state sampling, thirteen-label coverage, pairwise uniqueness, semantic preservation and final-cycle fail-closed behavior.

A documentation/evidence writer may be a fourth role. Only the main agent may launch Godot, integrate changes, execute final cases, stage, commit or push. No subagent may run Godot, perform Git writes or access Profile/user data. Parallelism is allowed only for disjoint read-only analysis; one writer per file.

If multi-subagent routing or local configuration parsing fails, return an honest blocker rather than silently falling back to one agent.

## 4. Startup gate

Before editing or launching Godot:

1. require root `D:\Delayed_Self`, branch `main`, expected origin and no Git operation in progress;
2. fetch and fast-forward only;
3. require `HEAD == origin/main`, divergence `0/0`, ancestry of `a7b404a63e7a546501be215aaf300cabfa74441c`, and no tracked or staged changes;
4. permit only untracked `.codex/config.toml`, record its SHA-256 and require it to remain unchanged;
5. require zero pre-existing Godot processes;
6. require Godot `4.7.1.stable.steam.a13da4feb` and exact wrapper blob `5f14eb0feff903b40311c1c7283b20e2eabbffc2`;
7. read `AGENTS.md`, current pointer docs, this task, Task 0024AF v2, Task 0024AG, both blocker reports/GPT acceptances, Task 0024AE acceptance/report, Task 0024AD matrix, accepted sequence-9–12 reports and the lifecycle policy;
8. record exact hashes for the provisional presentation helper/Gameplay file, all historical Task 0024AF/AG artifacts, twelve levels, production catalog/route/Profile files, staging route, mechanics foundation and wrapper;
9. stop on drift, process ambiguity, any additional untracked path, historical mismatch or any need to inspect production Profile/user data.

The accepted Task 0024AE route case may use its exact isolated fixture only through its existing runner and cleanup contract. The focused visual case must access no Profile surface.

## 5. Authorized repository paths

Create or modify only:

```text
scripts/gameplay/schema_v2_presentation.gd
scripts/gameplay/vertical_slice.gd
tests/presentation/task_0024ah_dynamic_lifecycle_signature_validation.gd
tests/presentation/task_0024ah_dynamic_lifecycle_signature_validation.gd.uid
tests/tools/task_0024ah_*.ps1
tests/tools/task_0024ah_*.py
tests/tools/task_0024ah_*.json
docs/presentation/SCHEMA_V2_MINIMUM_OWNER_REVIEW_VISUAL_CONTRACT_CURRENT.md
docs/playtest/SUBMISSION_CORE_1_12_OWNER_CURRICULUM_REVIEW_CURRENT.md
docs/reports/0024AH_DYNAMIC_LIFECYCLE_SIGNATURE_AND_SCHEMA_V2_VISUAL_VALIDATION_REPORT.md
docs/CODEX_ACTIVE_TASK_CURRENT.md
docs/CODEX_STARTUP_SNAPSHOT_CURRENT.md
docs/DEVELOPMENT_PLAN_CURRENT.md
docs/DOC_INDEX_CURRENT.md
docs/HANDOFF_MASTER_CURRENT.md
docs/project_memory.md
docs/production/EXPANDED_CONTENT_SUBMISSION_ROADMAP_CURRENT.md
```

No other path may change. The historical Task 0024AF and Task 0024AG validators/tools/reports are not authorized for modification.

### 5.1 Provisional product implementation policy

Begin with these files byte-identical to result commit `2813d08bf65eae2ae0c528a4825bfc133d5cdb43`:

```text
scripts/gameplay/schema_v2_presentation.gd
scripts/gameplay/vertical_slice.gd
```

First validate them unchanged.

- If all Task 0024AH assertions pass, do not modify either product file.
- A modification is allowed only after a retained repeatable assertion identifies a specific missing, collapsed or incorrect presentation state.
- Any product-file change must cite the exact failed label, receive independent review and preserve route/input/Simulation/completion semantics.
- Do not weaken tests, delete a required state or alter mechanics to obtain a pass.

## 6. Frozen and protected surfaces

Do not modify:

```text
scenes/**
project.godot
data/catalog/**
data/levels/**
scripts/app/**
scripts/simulation/**
scripts/solver/**
scripts/gameplay files other than the two authorized presentation files
schemas/**
tests/run_all.gd
tests/simulation/**
tests/levels/**
tests/curriculum/**
tests/playtest/**
tests/presentation/task_0024af_schema_v2_minimum_visual_validation.gd
tests/presentation/task_0024ag_schema_v2_visual_correction_validation.gd
tests/tools/run_owned_godot_headless.ps1
all Task 0024T–0024AG historical evidence
localization/**
assets/**
audio/**
build/export/release/submission surfaces
```

Do not change input, collision, transitions, restart, history, completion, routes, catalog, finale or Profile behavior.

## 7. Task 0024AH wrapper adapter and parser qualification

Create a new Task 0024AH adapter rather than editing Task 0024AG tools, for example:

```text
tests/tools/task_0024ah_invoke_wrapper_case.ps1
```

It must preserve the accepted Task 0024AG parsing doctrine:

1. invoke `tests/tools/run_owned_godot_headless.ps1` unchanged;
2. collect pipeline output as raw values;
3. select exactly one nonempty compact JSON object record;
4. explicitly run `ConvertFrom-Json -Depth 100`;
5. require schema version, status, underlying status, native exit, marker result, stderr, cleanup count, owned/unproven PID arrays and stdout to exist with correct non-null types;
6. reject missing, duplicate, non-JSON, truncated, array-root, null-field, wrong-version and wrong-type inputs;
7. retain raw wrapper JSON SHA-256, byte count and parsed record;
8. never infer missing data.

Create machine-readable Task 0024AH negative probes. Statistically compare the new adapter contract with the historical Task 0024AG adapter and record that differences are limited to task identity, output paths and any stricter fail-closed checks.

## 8. New comprehensive validator

Create:

```text
res://tests/presentation/task_0024ah_dynamic_lifecycle_signature_validation.gd
```

Pass marker:

```text
TASK_0024AH_DYNAMIC_LIFECYCLE_SIGNATURE_PASS
```

Failure prefix:

```text
TASK_0024AH_DYNAMIC_LIFECYCLE_SIGNATURE_FAIL
```

Use only public LevelLoader, shared Simulation, accepted witness actions and the real presentation helper/Gameplay snapshot. Do not duplicate transition logic.

## 9. Exact thirteen-entry lifecycle-signature registry

The validator must create one explicit dictionary with exactly these labels:

```text
crate
floor_key
held_key
lock
sensor_you_inactive
sensor_you_active
sensor_echo_inactive
sensor_echo_active
barrier_closed
barrier_open
barrier_open_deferred
latch_inactive
latch_activated
```

For every label retain:

- source level ID;
- exact turn/action index or state classification;
- descriptor family, stable ID and state variant;
- shape token, glyph token and type/group token;
- exact `monochrome_signature`;
- evidence that the descriptor came from the intended lifecycle state.

Require:

1. all thirteen labels are present exactly once;
2. every signature is a nonempty string;
3. every descriptor satisfies the public descriptor contract;
4. every label maps to the intended family/state;
5. all thirteen signature strings are pairwise distinct;
6. no uniqueness result may be derived solely from four initial-state snapshots;
7. the emitted machine record contains the full label→signature map and exact count `13`.

### 9.1 Required sampling sources

Use exact accepted witnesses wherever specified.

#### Sequence 9 — crate

Witness:

```text
DOWN RIGHT DOWN DOWN DOWN RIGHT RIGHT UP RIGHT UP RIGHT UP RIGHT UP
```

- `crate`: initial state and after action 2; both must use the same crate structural signature while the descriptor cell changes `[2,2] → [3,2]`.
- action 3 must prove ECHO contact does not move the crate or create a different push affordance.
- restart restores initial descriptor exactly.

#### Sequence 10 — floor key, held key and lock

Witness:

```text
DOWN DOWN RIGHT RIGHT LEFT LEFT UP UP RIGHT RIGHT UP RIGHT RIGHT RIGHT RIGHT UP UP LEFT LEFT
```

- `floor_key` and `lock`: initial state; same stable `K*` type token but distinct structural signatures.
- after action 4: floor key absent; exactly one `held_key` descriptor present.
- shared-Simulation ECHO-item probe: ECHO contact leaves floor key, held inventory and lock presentation unchanged.
- after action 12: lock and held key both absent on the same accepted turn.
- restart restores key/lock and clears held inventory.

#### Sequence 11 — sensors and grouped barrier

Witness:

```text
RIGHT RIGHT UP UP LEFT LEFT UP UP RIGHT RIGHT RIGHT RIGHT RIGHT RIGHT RIGHT RIGHT DOWN DOWN
```

- `sensor_you_inactive`, `sensor_echo_inactive`, `barrier_closed`: initial state.
- after action 10: both correctly typed sensors active and the group is open; sample `sensor_you_active`, `sensor_echo_active`, `barrier_open`.
- after action 11 and action 12: closure requested while YOU occupies each grouped bridge cell; every occupied group-cell descriptor must use `barrier_open_deferred`, and both cells must share the same group token/signature.
- after action 13: group closed after vacancy.
- retain at least one reachable wrong-actor contact state, or a schema-valid shared-Simulation identity control derived from the accepted sequence-11 control contract, proving wrong identity does not use the correct active signature.
- restart restores inactive sensors and closed barrier.

#### Sequence 12 — latch

Witness:

```text
RIGHT UP RIGHT UP UP UP UP LEFT LEFT UP UP RIGHT RIGHT RIGHT RIGHT RIGHT RIGHT RIGHT RIGHT DOWN DOWN
```

- `latch_inactive`: initial state, including target group token.
- after action 3: sample `latch_activated` and persistent-open phase group.
- after action 13: typed group open from sensors while latch remains activated.
- after action 16: typed group closed after vacancy while latch remains activated and phase group remains persistently open.
- repeated/later turns must retain the same activated signature and must not expose a second activation affordance.
- `barrier_phase` and `barrier_typed` must use distinct stable group tokens.
- restart restores inactive latch and initial group states.

## 10. Complete lifecycle and regression coverage

In addition to the registry, prove:

1. descriptor counts exactly match every visible v2 entity in each sampled state;
2. snapshots are deterministic, pure and deep-copy mutation resistant;
3. normal Gameplay snapshot equals helper output for identical state;
4. actor occupancy does not erase sensor underlay or barrier edge identity;
5. all bounds fit inside the existing logical gameplay area;
6. layer order remains terrain → underlay/open edge → blockers/interactables → actors → tokens/inventory;
7. all sequences 1–8 load and expose zero schema-v2 descriptors;
8. accepted Task 0024AD focused batch still reports all twelve exact `(L*, N*)` values;
9. route, input, restart, completion and finale acknowledgement semantics remain unchanged;
10. the tests do not claim human clarity or final art quality.

## 11. Static semantic-preservation and frozen audit

Create machine-readable PRE_FINAL and POST_FINAL audits proving:

- every Task 0024AF/AG historical artifact is byte-identical;
- the wrapper blob is exact;
- all twelve levels and production catalog are unchanged;
- App/Profile/route/Simulation/solver/foundation files are unchanged;
- the Task 0024AE staging route is unchanged;
- `.codex/config.toml` remains untracked, unstaged and byte-identical;
- all changed repository paths are authorized;
- if product presentation files changed, only evidence-linked presentation code changed and protected Gameplay functions remain semantically identical.

## 12. Evidence format and pre-final qualification

Emit independently parseable JSON records below 1800 UTF-8 bytes for:

- multi-subagent ledger;
- adapter equivalence and negative probes;
- static preservation;
- per-level descriptor inventory;
- crate lifecycle;
- key/lock/inventory lifecycle;
- sensor/barrier lifecycle;
- latch lifecycle;
- exact thirteen-entry signature registry;
- fit/layering/v1 regression;
- summary.

The capture parser must reject missing, duplicate, truncated, invalid-JSON and over-bound records and retain exact byte counts.

Before declaring a final cycle, require all of the following:

1. new adapter negative probes pass;
2. static preservation passes;
3. exactly thirteen named signatures are sampled and pairwise unique;
4. every required lifecycle state above is retained;
5. two independent real focused qualifications pass through the new adapter with complete non-null wrapper records;
6. evidence parser qualification passes;
7. PRE_FINAL frozen audit has zero mismatches;
8. each authoring run cleaned exactly once, left no owned/unproven PID and ended at Godot zero.

The final-cycle declaration must be a separate machine-readable manifest created only after all eight gates pass.

## 13. Fresh retained Task 0024AH cycle

Execute exactly once each under one new Task 0024AH manifest:

1. focused Task 0024AH dynamic lifecycle-signature validator;
   - marker `TASK_0024AH_DYNAMIC_LIFECYCLE_SIGNATURE_PASS`;
   - stderr empty;
2. accepted Task 0024AE isolated staging route qualification;
   - marker `TASK_0024AE_ISOLATED_STAGING_OWNER_ROUTE_PASS`;
   - stderr empty;
3. accepted Task 0024AD 1–12 batch validator;
   - marker `TASK_0024AD_SUBMISSION_CORE_1_12_BATCH_PASS`;
   - stderr empty;
4. Aggregate;
   - exact marker `TASK_0003_TESTS_PASS assertions=621 vectors=9`;
   - stderr exactly `LEVEL_ID_MISMATCH`;
5. corrected complete-v2;
   - marker `TASK_0024V_V2_COMPLETE_PASS`;
   - stderr empty.

All wrapper-driven cases must use the new Task 0024AH adapter. The route runner result must be explicitly parsed and type-checked by the Task 0024AH driver.

For every case retain raw result hash, parsed status, native exit, marker count/result, stderr, cleanup count/order, PID arrays and Godot before/after. Fail immediately on any null or wrong-type required field. Do not execute later cases after a failure.

Final Godot count must be zero. Once declared consumed, never rerun the Task 0024AH cycle.

## 14. Required report and status

Create:

```text
docs/reports/0024AH_DYNAMIC_LIFECYCLE_SIGNATURE_AND_SCHEMA_V2_VISUAL_VALIDATION_REPORT.md
```

The report must include:

- implementation hashes before/after and whether product files changed;
- multi-subagent ledger;
- Task 0024AF/AG historical preservation;
- exact explanation of the initial-only sampling defect;
- complete thirteen-entry signature registry with state sources;
- lifecycle, purity, fit, layering and v1 evidence;
- adapter/probe evidence;
- retained five-case cycle evidence;
- frozen audits and `.codex/config.toml` identity;
- explicit owner/final-art/catalog boundaries.

On pass, update the owner worksheet only to:

```text
owner_route_status = MACHINE_QUALIFIED_ISOLATED_STAGING_ROUTE
owner_playthrough_status = NOT_YET_REVIEWED
presentation_readiness = MACHINE_QUALIFIED_SCHEMA_V2_MINIMUM_OWNER_REVIEW_REQUIRED
visual_art_direction_status = NOT_FINAL
catalog_admission_status = NOT_AUTHORIZED
```

Do not populate subjective answers.

## 15. Commit and push

On pass or honest blocker:

1. stage only authorized repository paths;
2. never stage `.codex/config.toml`;
3. inspect staged paths and run `git diff --cached --check`;
4. make one single-topic commit without amend/rebase/squash;
5. fetch before push and stop on drift;
6. push only a clean fast-forward to `origin/main`;
7. require post-push `HEAD == origin/main`, divergence `0/0`, no tracked/staged changes, only unchanged `.codex/config.toml` untracked, and Godot zero;
8. report any separate correction commit transparently.

Suggested pass commit:

```text
test: validate dynamic schema v2 visual signatures
```

## 16. Terminal verdict

Return exactly one:

```text
SCHEMA_V2_DYNAMIC_LIFECYCLE_PRESENTATION_READY_FOR_GPT_REVIEW
BLOCKED_SCHEMA_V2_DYNAMIC_LIFECYCLE_VALIDATION
PROTECTED_DATA_BOUNDARY_BREACH
```

A pass authorizes only GPT review of the machine-qualified minimum schema-v2 presentation. It does not authorize owner subjective acceptance, final visual-art acceptance, production catalog/progression/finale/Profile changes, localization, assets/audio, build/export, release, upload or submission.
