# Task 0024AB — Correct sequence-11 evidence capture and execute one fresh retained validation cycle

- Status: `READY`
- Gate: `SEQUENCE 11 EVIDENCE-CAPTURE CORRECTION / NEW VALIDATOR IDENTITY / FRESH RETAINED CYCLE`
- Repository/root/branch: `zcx369658780/delayed-self-godot` / `D:\Delayed_Self` / `main`
- Required accepted checkpoint: `b426148b95775e8acf8a7cc92796d4678415da3a`
- Closed predecessor: Task `0024AA`
- Latest GPT adjudication: `TASK_0024AA_HONEST_EVIDENCE_RETENTION_BLOCKER_ACCEPTED_NEW_CAPTURE_GATE_REQUIRED`
- Blocked candidate commit: `e2938b61a6207e970302675db8bb454c08c40cd3`
- Frozen candidate SHA-256: `8856d3c64978cef7cff7f1680a7ae2f412c9b0cab058d598cbe3aee0225052e0`
- Historical Task 0024AA focused-validator SHA-256: `bff20626fb4830f4a8fed2e3b7f4b485a172d443db358978d1d1eeafeebc5a82`
- Accepted wrapper implementation/blob: `7c0712d6a602dc23200ccb147d0096acc6333305` / `5f14eb0feff903b40311c1c7283b20e2eabbffc2`
- Recommended Codex model: `GPT-5.6 Sol Max`; fallback `GPT-5.6 Sol High`

## 1. Objective

Preserve the technically passing but evidentially blocked Task 0024AA candidate and historical records, create a new semantically equivalent Task 0024AB focused validator whose required machine evidence is emitted as multiple independently parseable bounded records, qualify that capture format before final use, and execute one fresh retained five-case validation cycle.

A passing result may establish only an uncatalogued machine-validated sequence-11 candidate ready for GPT review. It does not authorize sequence 12, catalog/progression/finale/Profile, owner acceptance, Gameplay/UI, localization, assets/audio, build/export, release, upload or submission.

## 2. Binding Task 0024AA facts

Task 0024AA is terminally closed. Its retained cycle and cases must never be rerun under that task identity.

Preserve as historical facts, not final acceptance:

- candidate technical result: `L*=18`, exact `N*=1`, visited states `1002`, maximum frontier `130`;
- all-shortest full ordered event `1/1`, omissions `0`;
- no-ECHO, swapped-identity and no-deferral complete-unsolved;
- one-cell group result `14/1`;
- both ANY controls eliminate exact typed-identity necessity;
- all five historical final cases technically passed with correct cleanup/process isolation;
- focused result/control compact JSON lines were truncated at exactly 2048 characters;
- exact `solver.elapsed_ms` was not retained and must not be reconstructed or inferred.

The old blocker summary/report remain honest historical evidence. Task 0024AB must create new identities and new retained records.

## 3. Startup gate

Before writing or launching Godot:

1. require root `D:\Delayed_Self`, branch `main`, expected origin and no Git operation in progress;
2. fetch and fast-forward only;
3. require `HEAD == origin/main`, divergence `0/0`, and ancestry of `b426148b95775e8acf8a7cc92796d4678415da3a`;
4. require clean tracked worktree, zero staged paths and no unexpected untracked files;
5. require zero pre-existing Godot processes; never close unknown/pre-existing processes;
6. require Godot `4.7.1.stable.steam.a13da4feb` and accepted wrapper blob `5f14eb0feff903b40311c1c7283b20e2eabbffc2`;
7. read `AGENTS.md`, current startup/active docs, this full task, Task 0024AA task/report/summary/GPT adjudication, Task 0024Z and 0024Y acceptances, Task 0024X scope, schema-v2/Simulation-v2 contracts and the headless lifecycle policy;
8. verify exact hashes for the frozen sequence-11 candidate, historical 0024AA focused validator, all Task 0024AA retained case records/report/summary, accepted sequence-9/10 artifacts, foundation files, wrapper and Task 0024W sealed/historical identities;
9. stop on remote drift, unexpected dirty state, process ambiguity, candidate drift, historical evidence drift or any need to access Profile/user-data surfaces.

Production Profile content/metadata/siblings, ordinary user data and the test-profile root are forbidden.

## 4. Frozen and protected surfaces

Do not modify:

```text
data/levels/identity_shift_bridge.json
tests/levels/task_0024aa_identity_shift_bridge_validation.gd
docs/level_design/validation/sequence_11_identity_shift_bridge_validation.json
docs/reports/0024AA_SEQUENCE_11_IDENTITY_SHIFT_BRIDGE_MACHINE_VALIDATION_REPORT.md
tests/tools/task_0024aa_*
scripts/simulation/level_loader.gd
scripts/simulation/simulation.gd
scripts/solver/bfs_solver.gd
docs/game_design/GAMEPLAY_SIMULATION_CONTRACT_V2_CURRENT.md
docs/level_design/LEVEL_SCHEMA_V2_CURRENT.md
schemas/**
docs/level_design/reference/**
tests/run_all.gd
tests/simulation/**
tests/levels/task_0024y_crate_shadow_timing_validation.gd
tests/levels/task_0024z_key_route_commitment_validation.gd
tests/tools/run_owned_godot_headless.ps1
data/catalog/**
data/levels/tutorial_reach_exit.json
data/levels/tutorial_echo_bridge.json
data/levels/vertical_slice_delay_3.json
data/levels/door_one_turn_late.json
data/levels/two_keys_one_door.json
data/levels/staggered_doors.json
data/levels/echo_spacing_bridge.json
data/levels/two_echo_convergence.json
data/levels/crate_shadow_timing.json
data/levels/key_route_commitment.json
scenes/**
scripts/gameplay/**
scripts/app/**
project.godot
assets/**
audio/**
localization/**
build/export/release/submission surfaces
all Task 0024T/0024U/0024V/0024W sealed or historical evidence
```

Do not modify the candidate to make evidence capture easier. This task corrects only the validator/output/capture layer.

## 5. Authorized repository paths

Create only:

```text
tests/levels/task_0024ab_identity_shift_bridge_validation.gd
tests/levels/task_0024ab_identity_shift_bridge_validation.gd.uid
tests/tools/task_0024ab_*.ps1
tests/tools/task_0024ab_*.py
tests/tools/task_0024ab_*.json
docs/level_design/validation/sequence_11_identity_shift_bridge_validation_0024ab.json
docs/reports/0024AB_SEQUENCE_11_EVIDENCE_CAPTURE_CORRECTION_REPORT.md
```

After terminal result, update only as needed:

```text
docs/CODEX_ACTIVE_TASK_CURRENT.md
docs/CODEX_STARTUP_SNAPSHOT_CURRENT.md
docs/DEVELOPMENT_PLAN_CURRENT.md
docs/DOC_INDEX_CURRENT.md
docs/HANDOFF_MASTER_CURRENT.md
docs/project_memory.md
docs/production/EXPANDED_CONTENT_SUBMISSION_ROADMAP_CURRENT.md
```

No other path may change. A generated UID is allowed only for the new Task 0024AB test script and must be explicitly reviewed and staged.

## 6. Semantically equivalent replacement validator

Create:

```text
res://tests/levels/task_0024ab_identity_shift_bridge_validation.gd
```

It must load the unchanged candidate through public `LevelLoader.load_file` and use only shared `Simulation` and accepted BFS solver.

Required pass marker:

```text
TASK_0024AB_IDENTITY_SHIFT_BRIDGE_PASS
```

Required failure prefix:

```text
TASK_0024AB_IDENTITY_SHIFT_BRIDGE_FAIL
```

The validator must preserve the complete Task 0024AA technical contract:

- exact identity/fallback-envelope assertions;
- limits: depth 64, states 750000, time 35000 ms, count cap 1000000;
- `L*=18–23`, exact `N*=1–8`;
- deterministic replay, exact restart and canonical distinctions;
- ordered typed-pair/open-result/next-turn-entry/deferral-on-both-cells/closure-after-vacancy/YOU-completion trace;
- all-shortest full ordered event proof;
- no-ECHO, swapped-identity, YOU-ANY, ECHO-ANY, one-cell and no-deferral controls;
- wrong-actor, no-crate, no-same-turn-entry, next-turn-entry, closure-deferral, closure-after-vacancy, articulation, bypass, ECHO-exit, blocked-macro and zero-residue probes.

It must not weaken, delete, invert or bypass any Task 0024AA assertion or control. It must not implement a parallel transition system.

## 7. Static equivalence qualification

Before any retained final cycle, create a deterministic static-equivalence tool that compares the historical Task 0024AA validator and the new Task 0024AB validator.

The tool must prove at minimum:

1. same candidate path;
2. same action set and solver limits;
3. same required assertion/control/probe labels and counts;
4. same event-mask stages and ordered-event conditions;
5. same control mutations and restricted-search rules;
6. same expected metric bands;
7. no removal of a historical failure path;
8. differences are limited to task marker names, explicit type annotations if needed, and output/capture helpers;
9. no dynamic `HEAD` historical baseline;
10. historical Task 0024AA files remain byte-identical.

The equivalence result must be machine-readable and committed.

## 8. Bounded output contract

The new validator must not emit one monolithic result/control JSON line.

Emit separate prefixed JSON records, each independently parseable and each strictly shorter than 1800 UTF-8 bytes, including at least:

```text
TASK_0024AB_METRICS_JSON
TASK_0024AB_WITNESS_JSON
TASK_0024AB_EVENT_JSON
TASK_0024AB_ALL_SHORTEST_JSON
TASK_0024AB_CONTROL_IDENTITY_JSON
TASK_0024AB_CONTROL_GEOMETRY_JSON
TASK_0024AB_SHORTCUTS_JSON
TASK_0024AB_REPLAY_RESTART_JSON
```

Requirements:

- `TASK_0024AB_METRICS_JSON` must contain the exact fresh-run integer `solver.elapsed_ms`, status, `L*`, exact `N*`, visited states, frontier and configured limits;
- every required event may be one separate `TASK_0024AB_EVENT_JSON` line;
- control records may be split further when needed;
- no required field may depend on parsing a line at or above the wrapper's 2048-character bound;
- the capture parser must reject missing, duplicate, truncated, non-JSON or over-bound records;
- retained records must preserve raw line byte counts;
- exact `solver.elapsed_ms` must come from the fresh retained focused execution, not from an authoring probe or external wall-clock estimate.

## 9. Pre-final capture qualification

Before declaring the final cycle, the output/capture system is repeatable and repairable.

Run bounded focused qualification until all of the following pass:

- static equivalence passes;
- every required record prefix occurs at the expected count;
- all JSON payloads parse independently;
- every required line is below 1800 UTF-8 bytes;
- metrics include exact integer `elapsed_ms` within `0..35000`;
- event, all-shortest, controls, shortcuts and replay/restart records are complete;
- wrapper cleanup count is one;
- no owned/unproven PID remains;
- final Godot count is zero.

Authoring/qualification repetitions must be recorded honestly. Do not relabel failed capture attempts as passes.

## 10. Fresh retained final cycle

After the candidate-independent capture system is stable, declare and run one fresh Task 0024AB retained cycle containing exactly these five cases, each once:

1. `res://tests/levels/task_0024ab_identity_shift_bridge_validation.gd`
   - marker: `TASK_0024AB_IDENTITY_SHIFT_BRIDGE_PASS`;
   - stderr: empty;
   - all required bounded records parse and include exact fresh `solver.elapsed_ms`.
2. `res://tests/levels/task_0024z_key_route_commitment_validation.gd`
   - marker: `TASK_0024Z_KEY_ROUTE_COMMITMENT_PASS`;
   - stderr: empty.
3. `res://tests/levels/task_0024y_crate_shadow_timing_validation.gd`
   - marker: `TASK_0024Y_CRATE_SHADOW_TIMING_PASS`;
   - stderr: empty.
4. `res://tests/run_all.gd`
   - exact marker: `TASK_0003_TESTS_PASS assertions=621 vectors=9`;
   - stderr exactly `LEVEL_ID_MISMATCH`.
5. `res://tests/simulation/task_0024v_v2_solver_replay_canonical_malformed.gd`
   - marker: `TASK_0024V_V2_COMPLETE_PASS`;
   - stderr: empty.

For each case retain wrapper/native exit, exact marker count, stderr, cleanup count/order, ownership, remaining owned/unproven PIDs, Godot before/after and bounded stdout/stderr.

Final Godot count must be zero. Once this cycle is declared retained and consumed, do not rerun it.

## 11. Acceptance facts required from fresh focused execution

The fresh Task 0024AB focused record must prove:

- `SOLVED`;
- `L*` remains 18–23 inclusive;
- exact `N*` remains 1–8 inclusive;
- exact integer `solver.elapsed_ms` is retained and does not exceed 35000;
- all-shortest count equals exact `N*`, full ordered event count equals exact `N*`, omissions zero;
- typed identities, next-turn entry, two-cell deferral and closure-after-vacancy occur in every shortest solution;
- no-ECHO and no-deferral controls complete finite-state unsolved;
- swapped/ANY/one-cell controls remain materially distinguishing;
- replay/restart/canonical and shortcut probes pass;
- candidate SHA-256 remains `8856d3c64978cef7cff7f1680a7ae2f412c9b0cab058d598cbe3aee0225052e0`.

A different fresh elapsed time is expected and acceptable; it must simply be exactly retained from the fresh run. Any substantive metric/control/event change is a blocker requiring GPT review, not an opportunity to edit the candidate.

## 12. Machine-readable summary

Create:

```text
docs/level_design/validation/sequence_11_identity_shift_bridge_validation_0024ab.json
```

Include:

- task/repository/Godot/wrapper identities;
- frozen candidate and historical 0024AA identities;
- static-equivalence result;
- bounded-record prefixes, counts and byte lengths;
- exact fresh solver metrics including `elapsed_ms`;
- witness and ordered event records;
- all-shortest facts;
- all six controls and shortcuts;
- replay/restart/canonical facts;
- final five-case evidence;
- historical 0024AA cycle explicitly marked consumed and not reused as final evidence;
- `owner_playthrough_status = "PENDING_LATER_CURRICULUM_GATE"`;
- `catalog_admission_status = "NOT_AUTHORIZED"`;
- `profile_accessed = false`.

Do not hand-edit claims that disagree with retained runtime records.

## 13. Report

Create:

```text
docs/reports/0024AB_SEQUENCE_11_EVIDENCE_CAPTURE_CORRECTION_REPORT.md
```

The report must state:

- why Task 0024AA remained blocked;
- how the new output format prevents 2048-character truncation;
- static-equivalence evidence;
- pre-final qualification history;
- exact fresh solver metrics and `elapsed_ms`;
- event/all-shortest/control/shortcut results;
- final five-case wrapper/process evidence;
- frozen candidate, accepted sequence-9/10 and historical hash audits;
- unresolved owner/catalog status and protected boundaries.

## 14. Commit and push policy

On pass or honest blocker:

1. explicitly stage only authorized paths;
2. inspect staged names and run `git diff --cached --check`;
3. create one single-topic result commit; do not amend/rebase/squash;
4. fetch before push and stop on remote drift;
5. push only a clean fast-forward to `origin/main`;
6. require post-push `HEAD == origin/main`, divergence `0/0`, clean worktree and zero Godot processes;
7. report any separate correction commit transparently.

Suggested pass commit:

```text
test: correct sequence 11 evidence capture and validate candidate
```

## 15. Terminal verdict

Return exactly one:

```text
SEQUENCE_11_IDENTITY_SHIFT_BRIDGE_CORRECTED_MACHINE_VALIDATED_FOR_GPT_REVIEW
BLOCKED_SEQUENCE_11_IDENTITY_SHIFT_BRIDGE_EVIDENCE_CORRECTION
PROTECTED_DATA_BOUNDARY_BREACH
```

A pass establishes only an uncatalogued machine-validated sequence-11 fallback candidate ready for GPT review. It does not authorize sequence 12, catalog/progression/finale/Profile, owner acceptance, Gameplay/UI, localization, assets/audio, build/export, release, upload or submission.
