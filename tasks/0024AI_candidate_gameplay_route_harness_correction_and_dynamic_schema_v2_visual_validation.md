# Task 0024AI — Correct candidate Gameplay route harness and complete dynamic schema-v2 visual validation

- Status: `READY`
- Gate: `CANDIDATE GAMEPLAY ROUTE-PAYLOAD CORRECTION / DYNAMIC VISUAL REGISTRY / FRESH RETAINED CYCLE / MULTI-SUBAGENT REQUIRED`
- Repository/root/branch: `zcx369658780/delayed-self-godot` / `D:\Delayed_Self` / `main`
- Required accepted checkpoint: `5b6b54b3d71b4f1117388e3b36f86c4c0b6a8609`
- Historical blocked Task 0024AH result: `1bef7ec79898179079265a132678753da9e1c8f7`
- Latest GPT verdict: `TASK_0024AH_HONEST_GAMEPLAY_ROUTE_PAYLOAD_ID_BLOCKER_ACCEPTED_FRESH_HARNESS_GATE_REQUIRED`
- Historical Task 0024AF consumed invocation: `621b1e96-adeb-4a7d-8398-68fef9c2b0a4`
- Task 0024AG and 0024AH fresh retained cycles: not declared and not consumed
- Provisional presentation implementation baseline: commit `25d7fda3937fc269cdf730c14698e47bfb209c29`
- Accepted wrapper blob: `5f14eb0feff903b40311c1c7283b20e2eabbffc2`
- Production baseline: exactly eight cataloged levels; sequence 8 remains the sole formal finale
- Accepted uncatalogued candidates: `crate_shadow_timing`, `key_route_commitment`, `identity_shift_bridge`, `lasting_echo_latch`
- Recommended main-agent model: `GPT-5.6 Sol Max`; fallback `GPT-5.6 Sol High`

## 1. Objective

Under a new task identity:

1. preserve every Task 0024AF–0024AH historical artifact byte-for-byte;
2. correct the candidate real-Gameplay harness so route payload IDs exactly match the loaded level definitions;
3. add a dedicated fail-closed preflight for all four candidate Gameplay routes;
4. rebuild and validate the exact thirteen-entry dynamic lifecycle-signature registry;
5. begin with the provisional presentation implementation unchanged and modify it only if a named post-harness assertion proves a real presentation defect;
6. complete two passing repeatable focused qualifications;
7. execute one fresh five-case Task 0024AI retained cycle;
8. determine whether minimum schema-v2 presentation is machine-ready for owner review.

This task does not conduct owner subjective playtesting and does not authorize production catalog, progression, finale or Profile changes.

## 2. Accepted blocker root cause

Task 0024AH used aliases as test dictionary keys:

```text
crate
key
sensor
latch
```

and passed those aliases to `Gameplay.configure_route_payload().level_id`.

The exact loaded level IDs are:

```text
crate_shadow_timing
key_route_commitment
identity_shift_bridge
lasting_echo_latch
```

Gameplay correctly compares the route payload ID with `loaded.level.level_id` and emitted `LEVEL_ID_MISMATCH`. The resulting Gameplay snapshots were empty. This was a validator harness defect, not a product defect.

Task 0024AI must never derive a Gameplay route ID from a test alias. It must use the exact nonempty `level_id` returned by public `LevelLoader.load_file()` and the exact path used for that load.

## 3. Historical immutability

All committed `task_0024af_*`, `task_0024ag_*` and `task_0024ah_*` artifacts are historical and immutable, including their validators, adapters, qualification records, reports and GPT adjudications.

Do not edit, delete, repair, relabel, reconstruct or rerun any historical final-cycle identity. All new validation, adapter, preflight, probe and final-cycle paths must use the `task_0024ai_*` identity.

## 4. Mandatory multi-subagent doctrine

Read and route through:

```text
D:\Delayed_Self\.codex\config.toml
```

`.codex/config.toml` is the sole permitted pre-existing untracked path. Record its exact path and SHA-256 without exposing contents. Do not modify, stage, commit, delete or copy it. Require byte identity at closeout.

Use at least three distinct roles:

1. **source/route-contract mapper** — inspect Gameplay route-payload validation, public LevelLoader results, Task 0024AH harness defect, provisional presentation files and protected boundaries;
2. **validation worker** — implement the new Task 0024AI route preflight, wrapper adapter, dynamic registry validator, probes and retained driver;
3. **independent reviewer/test auditor** — verify exact ID/path binding, no alias leakage, thirteen-label lifecycle coverage, semantic preservation and final-cycle fail-closed behavior.

A documentation/evidence writer may be a fourth role. Only the main agent may launch Godot, integrate changes, execute final cases, stage, commit or push. No subagent may run Godot, perform Git writes or access Profile/user data. Parallelism is allowed only for disjoint read-only analysis; one writer per file.

If multi-subagent routing or local configuration parsing fails, return an honest blocker rather than silently falling back to one agent.

## 5. Startup gate

Before editing or launching Godot:

1. require root `D:\Delayed_Self`, branch `main`, expected origin and no Git operation in progress;
2. fetch and fast-forward only;
3. require `HEAD == origin/main`, divergence `0/0`, ancestry of `5b6b54b3d71b4f1117388e3b36f86c4c0b6a8609`, and no tracked or staged changes;
4. permit only untracked `.codex/config.toml`, record its SHA-256 and require it to remain unchanged;
5. require zero pre-existing Godot processes;
6. require Godot `4.7.1.stable.steam.a13da4feb` and exact wrapper blob `5f14eb0feff903b40311c1c7283b20e2eabbffc2`;
7. read `AGENTS.md`, current pointer docs, this task, Tasks 0024AF v2/AG/AH, all three blocker reports/GPT acceptances, Task 0024AE acceptance/report, Task 0024AD matrix, accepted sequence-9–12 reports and the lifecycle policy;
8. record exact hashes for the provisional presentation helper/Gameplay file, all historical Task 0024AF–AH artifacts, twelve levels, production catalog/route/Profile files, staging route, mechanics foundation and wrapper;
9. stop on drift, process ambiguity, any additional untracked path, historical mismatch or any need to inspect production Profile/user data.

The accepted Task 0024AE route case may use its exact isolated fixture only through its existing runner and cleanup contract. The focused visual case and candidate route preflight must access no Profile surface.

## 6. Authorized repository paths

Create or modify only:

```text
scripts/gameplay/schema_v2_presentation.gd
scripts/gameplay/vertical_slice.gd
tests/presentation/task_0024ai_candidate_route_and_dynamic_signature_validation.gd
tests/presentation/task_0024ai_candidate_route_and_dynamic_signature_validation.gd.uid
tests/tools/task_0024ai_*.ps1
tests/tools/task_0024ai_*.py
tests/tools/task_0024ai_*.json
docs/presentation/SCHEMA_V2_MINIMUM_OWNER_REVIEW_VISUAL_CONTRACT_CURRENT.md
docs/playtest/SUBMISSION_CORE_1_12_OWNER_CURRICULUM_REVIEW_CURRENT.md
docs/reports/0024AI_CANDIDATE_GAMEPLAY_ROUTE_AND_DYNAMIC_SCHEMA_V2_VISUAL_VALIDATION_REPORT.md
docs/CODEX_ACTIVE_TASK_CURRENT.md
docs/CODEX_STARTUP_SNAPSHOT_CURRENT.md
docs/DEVELOPMENT_PLAN_CURRENT.md
docs/DOC_INDEX_CURRENT.md
docs/HANDOFF_MASTER_CURRENT.md
docs/project_memory.md
docs/production/EXPANDED_CONTENT_SUBMISSION_ROADMAP_CURRENT.md
```

No other path may change. Historical Task 0024AF–AH validators, adapters, tools, evidence and reports are not authorized for modification.

### 6.1 Provisional product implementation policy

Begin with these files byte-identical to result commit `1bef7ec79898179079265a132678753da9e1c8f7`:

```text
scripts/gameplay/schema_v2_presentation.gd
scripts/gameplay/vertical_slice.gd
```

First validate them unchanged.

- If all Task 0024AI assertions pass, do not modify either product file.
- A modification is allowed only after the corrected candidate route preflight passes and a named retained repeatable assertion identifies a specific missing, collapsed or incorrect presentation state.
- Any product-file change must cite the exact failed label, receive independent review and preserve route/input/Simulation/completion semantics.
- Do not weaken tests, delete a required state or alter mechanics to obtain a pass.

## 7. Frozen and protected surfaces

Do not modify:

```text
scenes/**
project.godot
data/catalog/**
data/levels/**
scripts/app/**
scripts/simulation/**
scripts/solver/**
scripts/gameplay files other than the two authorized presentation files
schemas/**
tests/run_all.gd
tests/simulation/**
tests/levels/**
tests/curriculum/**
tests/playtest/**
tests/presentation/task_0024af_schema_v2_minimum_visual_validation.gd
tests/presentation/task_0024ag_schema_v2_visual_correction_validation.gd
tests/presentation/task_0024ah_dynamic_lifecycle_signature_validation.gd
tests/tools/run_owned_godot_headless.ps1
all Task 0024T–0024AH historical evidence
localization/**
assets/**
audio/**
build/export/release/submission surfaces
```

Do not change input, collision, transitions, restart, history, completion, routes, catalog, finale or Profile behavior.

## 8. Task 0024AI wrapper adapter and parser qualification

Create a new Task 0024AI adapter rather than editing historical tools, for example:

```text
tests/tools/task_0024ai_invoke_wrapper_case.ps1
```

Preserve the accepted parsing doctrine:

1. invoke `tests/tools/run_owned_godot_headless.ps1` unchanged;
2. collect pipeline output as raw values;
3. select exactly one nonempty compact JSON object record;
4. explicitly run `ConvertFrom-Json -Depth 100`;
5. require schema version, status, underlying status, native exit, marker result, stderr, cleanup count, owned/unproven PID arrays and stdout to exist with correct non-null types;
6. reject missing, duplicate, non-JSON, truncated, array-root, null-field, wrong-version and wrong-type inputs;
7. retain raw wrapper JSON SHA-256, byte count and parsed record;
8. never infer missing data.

Create Task 0024AI negative probes and statically compare the new adapter contract with Task 0024AG/AH adapters. Differences must be limited to task identity, output paths and stricter fail-closed checks.

## 9. Candidate Gameplay route-payload preflight

Before lifecycle sampling, the focused validator must run a dedicated preflight for exactly these candidate files:

```text
res://data/levels/crate_shadow_timing.json
res://data/levels/key_route_commitment.json
res://data/levels/identity_shift_bridge.json
res://data/levels/lasting_echo_latch.json
```

For each candidate:

1. load through public `LevelLoader.load_file(path)`;
2. require `ok=true` and a nonempty loaded `level.level_id`;
3. instantiate real `res://scenes/vertical_slice/vertical_slice.tscn` outside the tree;
4. configure route payload using exactly:
   - `level_id = loaded.level.level_id`;
   - `level_path = the exact load path`;
   - valid `hud_mode`, classification and final flag;
5. require `configure_route_payload()` returns true;
6. add the scene to the tree and wait for readiness;
7. require `load_error == ""`;
8. require `is_runtime_ready() == true`;
9. require `get_route_payload().level_id == loaded.level.level_id`;
10. require `get_route_payload().level_path == path`;
11. require the real Gameplay schema-v2 snapshot exactly equals the helper output for the loaded level and initial shared-Simulation state;
12. require no `LEVEL_ID_MISMATCH` stderr;
13. deep-copy mutation of the returned snapshot must not affect the next snapshot;
14. free the scene and return to a clean tree before the next candidate.

The test may use aliases only as internal dictionary labels. Aliases must never enter route payload fields. Emit one bounded record containing alias, actual loaded level ID, exact path, configure status, runtime status and snapshot equality for all four candidates.

Fail immediately before lifecycle registry work if any candidate preflight fails.

## 10. Comprehensive validator

Create:

```text
res://tests/presentation/task_0024ai_candidate_route_and_dynamic_signature_validation.gd
```

Pass marker:

```text
TASK_0024AI_CANDIDATE_ROUTE_DYNAMIC_SIGNATURE_PASS
```

Failure prefix:

```text
TASK_0024AI_CANDIDATE_ROUTE_DYNAMIC_SIGNATURE_FAIL
```

Use only public LevelLoader, shared Simulation, accepted witness actions and the real presentation helper/Gameplay snapshot. Do not duplicate transition logic.

## 11. Exact thirteen-entry lifecycle-signature registry

Create one explicit registry with exactly these labels:

```text
crate
floor_key
held_key
lock
sensor_you_inactive
sensor_you_active
sensor_echo_inactive
sensor_echo_active
barrier_closed
barrier_open
barrier_open_deferred
latch_inactive
latch_activated
```

For every label retain:

- source level ID;
- exact turn/action index or state classification;
- descriptor family, stable ID and state variant;
- shape token, glyph token and type/group token;
- exact `monochrome_signature`;
- evidence that the descriptor came from the intended lifecycle state.

Require all thirteen labels exactly once, all signatures nonempty and pairwise distinct, descriptor contract validity and a complete label-to-signature machine record.

### 11.1 Sequence 9 crate

Use accepted witness:

```text
DOWN RIGHT DOWN DOWN DOWN RIGHT RIGHT UP RIGHT UP RIGHT UP RIGHT UP
```

Validate initial crate, action-2 movement `[2,2] → [3,2]`, action-3 ECHO contact inertness and restart restoration.

### 11.2 Sequence 10 key/lock/inventory

Use accepted witness:

```text
DOWN DOWN RIGHT RIGHT LEFT LEFT UP UP RIGHT RIGHT UP RIGHT RIGHT RIGHT RIGHT UP UP LEFT LEFT
```

Validate initial floor key/lock common type token but distinct structures, action-4 held inventory, ECHO-item inertness, action-12 same-turn lock/key consumption and restart restoration.

### 11.3 Sequence 11 sensors/barrier

Use accepted witness:

```text
RIGHT RIGHT UP UP LEFT LEFT UP UP RIGHT RIGHT RIGHT RIGHT RIGHT RIGHT RIGHT RIGHT DOWN DOWN
```

Validate initial typed inactive sensors/closed group; action-10 typed active sensors/open group; action-11 and action-12 occupied closure deferral for the intended bridge cells; action-13 closed after vacancy; wrong-actor/control identity does not impersonate correct active state; restart restoration.

### 11.4 Sequence 12 latch

Use accepted witness:

```text
RIGHT UP RIGHT UP UP UP UP LEFT LEFT UP UP RIGHT RIGHT RIGHT RIGHT RIGHT RIGHT RIGHT RIGHT DOWN DOWN
```

Validate initial latch; action-3 activation/persistent phase opening; action-13 typed group opening with latch persistence; action-16 typed closure after vacancy while latch and phase opening persist; later-turn persistent activated signature; distinct phase/typed group tokens; restart restoration.

## 12. Fit, layering, purity and regressions

Prove:

- deterministic ordering and helper purity;
- exact descriptor counts for currently visible v2 entities;
- Gameplay/helper equality for initial candidate states after route preflight;
- deep-copy mutation resistance;
- all descriptor bounds inside the existing logical gameplay area;
- required layer ordering;
- actor occupancy does not erase sensor/barrier identity;
- sequences 1–8 load and produce no spurious v2 descriptors;
- accepted Task 0024AD focused validator preserves all twelve `(L*, N*)` values;
- no route, input, mechanics, restart, completion or Profile regression.

Do not claim human clarity or final art quality from these tests.

## 13. Evidence format

Emit independently parseable prefixed JSON records below 1800 UTF-8 bytes for:

- multi-subagent ledger;
- static/frozen preservation;
- wrapper parser qualification;
- candidate Gameplay route preflight;
- per-level descriptor inventory;
- crate lifecycle;
- key/lock/inventory lifecycle;
- sensor/barrier lifecycle;
- latch lifecycle;
- thirteen-entry signature registry;
- fit/layering/v1 regression;
- summary.

The capture parser must reject missing, duplicate, truncated, invalid-JSON and over-bound records and retain exact byte counts.

## 14. Repeatable qualification

Before final retention, repeatable qualification is repairable.

Require at minimum:

1. passing adapter negative probes;
2. passing historical frozen audit;
3. passing four-candidate route-payload preflight;
4. two independent passing real focused adapter qualifications with complete non-null records;
5. exact thirteen-label registry and pairwise uniqueness;
6. passing static semantic-preservation qualification;
7. PRE_FINAL frozen audit with zero mismatches;
8. `.codex/config.toml` remains byte-identical and unstaged;
9. final Godot count zero.

No subagent may run Godot.

## 15. Fresh retained Task 0024AI cycle

Only after all qualification is stable, execute exactly once each under one new Task 0024AI manifest:

1. focused Task 0024AI candidate-route/dynamic-signature validator;
   - marker `TASK_0024AI_CANDIDATE_ROUTE_DYNAMIC_SIGNATURE_PASS`;
   - stderr empty;
2. accepted Task 0024AE isolated staging route qualification;
   - marker `TASK_0024AE_ISOLATED_STAGING_OWNER_ROUTE_PASS`;
   - stderr empty;
3. accepted Task 0024AD 1–12 batch validator;
   - marker `TASK_0024AD_SUBMISSION_CORE_1_12_BATCH_PASS`;
   - stderr empty;
4. Aggregate;
   - exact marker `TASK_0003_TESTS_PASS assertions=621 vectors=9`;
   - stderr exactly `LEVEL_ID_MISMATCH`;
5. corrected complete-v2;
   - marker `TASK_0024V_V2_COMPLETE_PASS`;
   - stderr empty.

All wrapper-driven cases must use the new adapter and retain raw JSON SHA-256, parsed statuses, native exit, marker result, stderr, cleanup count/order, owned/unproven PID arrays and Godot before/after.

The route case may use its accepted runner, but its returned file must be parsed and validated explicitly under the Task 0024AI driver.

Fail immediately on any null required field or any failed case. Do not proceed to later cases after failure. Final Godot count must be zero. Once declared consumed, do not rerun Task 0024AI.

## 16. Report and status update

Create:

```text
docs/reports/0024AI_CANDIDATE_GAMEPLAY_ROUTE_AND_DYNAMIC_SCHEMA_V2_VISUAL_VALIDATION_REPORT.md
```

The report must include:

- exact implementation hashes before/after;
- whether provisional product files changed and exact failed labels if so;
- multi-subagent routing ledger;
- Task 0024AF–AH historical preservation evidence;
- exact Task 0024AH alias/actual-ID root cause;
- four-candidate route preflight evidence;
- adapter probes and repeatable qualifications;
- full thirteen-label registry and lifecycle evidence;
- retained cycle records/raw JSON hashes;
- frozen audits and `.codex/config.toml` identity;
- explicit human/final-art/catalog boundaries.

On pass, update owner worksheet only to:

```text
owner_route_status = MACHINE_QUALIFIED_ISOLATED_STAGING_ROUTE
owner_playthrough_status = NOT_YET_REVIEWED
presentation_readiness = MACHINE_QUALIFIED_SCHEMA_V2_MINIMUM_OWNER_REVIEW_REQUIRED
visual_art_direction_status = NOT_FINAL
catalog_admission_status = NOT_AUTHORIZED
```

Do not populate subjective answers.

## 17. Commit and push

On pass or honest blocker:

1. stage only authorized repository paths;
2. never stage `.codex/config.toml`;
3. inspect staged names and run `git diff --cached --check`;
4. create one single-topic result commit without amend/rebase/squash;
5. fetch before push and stop on drift;
6. push only a clean fast-forward to `origin/main`;
7. require `HEAD == origin/main`, divergence `0/0`, no tracked/staged changes, only unchanged `.codex/config.toml` untracked and Godot zero;
8. report any correction commit transparently.

Suggested pass commit:

```text
test: correct candidate gameplay harness and validate schema v2 visuals
```

## 18. Terminal verdict

Return exactly one:

```text
SCHEMA_V2_MINIMUM_OWNER_REVIEW_PRESENTATION_ROUTE_CORRECTED_READY_FOR_GPT_REVIEW
BLOCKED_SCHEMA_V2_CANDIDATE_GAMEPLAY_ROUTE_VALIDATION
PROTECTED_DATA_BOUNDARY_BREACH
```

A pass authorizes only GPT review of corrected minimum schema-v2 presentation evidence. It does not authorize owner subjective acceptance, final visual-art acceptance, production catalog/progression/finale/Profile changes, localization, assets/audio, build/export, release, upload or submission.
